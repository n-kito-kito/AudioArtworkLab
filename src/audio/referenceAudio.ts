/**
 * 確認用音源のパス。DESIGN.md「8. 確認用音源」を参照。
 *
 * このファイルを同名で上書きすれば、コードを変えずに音源を差し替えられる。
 * 生成し直す場合は `node scripts/generate-reference-audio.mjs`。
 */
export const REFERENCE_AUDIO_URL = '/audio/reference.wav';

export const REFERENCE_AUDIO_NAME = 'reference.wav';
