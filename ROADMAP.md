# ROADMAP

Audio Artwork Lab を、音源に反応する Generative Art 制作環境へ育てるためのロードマップ。

実装は `AI_RULES.md` に従い、1 回の変更を 1 つの部品または小さな基盤機能に限定する。

**現在地: Phase 12（サイマティクス表現の完成度）。** Phase 0〜11 は完了、または
Phase 12 の構造整理で置き換え済み。以下の Phase 0〜11 は履歴として残している。
要求は `PRD.md`、実装の地図は `CLAUDE.md` を参照する。

## Phase 0 — 基盤整理

- [x] ROADMAP.md を作成する
- [ ] 現在の SineWaveBasic の表示を基準画像として記録する
- [ ] App の終了処理を整備する

## Phase 1 — 音楽ファイル再生

- [x] AudioEngine の型を具体化する
- [x] FileAudioEngine を実装する
- [x] MP3 / WAV のファイル選択 UI を追加する
- [x] ドラッグ＆ドロップに対応する
- [x] 再生、一時停止、シーク、音量操作を追加する

## Phase 2 — 音声解析

- [x] 全体音量を解析する
- [x] Bass / Mid / Treble を解析する
- [x] 波形データを取得する
- [x] Beat を検出する
- [x] 解析値を確認できるモニターを追加する

## Phase 3 — SineWave 音楽連携

- [x] SineWave のパラメーター API を追加する
- [x] 音声特徴を SineWave へマッピングする
- [x] 反応量、範囲、スムージングを調整可能にする
- [x] 無音・停止時の動作を整える

ここまでを最初の MVP とする。

**MVP 完了条件:** MP3 または WAV を読み込み、再生中の Bass / Mid / Treble に応じて SineWave の振幅、周波数、見た目が変化する。

## Phase 4 — GUI

- [x] 開閉可能な GUI 基盤を追加する
- [x] 左右の編集パネルを個別に開閉する
- [x] Audio パネルを作る
- [x] Generator パネルを作る
- [x] Modifier パネルを作る
- [x] Effect パネルを作る
- [x] プリセットを保存・復元する

## Phase 5 — 表現ライブラリ

### Generator

- [x] Waveform
- [x] Grid
- [x] Bitmap
- [x] Mosaic
- [x] Lissajous
- [x] ParticleField

### Modifier

- [ ] Warp
- [ ] Repeat
- [x] PixelStretch
- [x] GridReveal
- [ ] ScanDrift

### Effect

- [x] Grain
- [x] Blur
- [x] PaletteMap
- [x] Glitch
- [x] Halftone
- [x] Glass
- [x] RGBSplit
- [x] Bloom
- [x] Warp
- [x] ScanDrift
- [x] Repeat

表現の実装優先度は `Grain → Blur → PaletteMap → Repeat → Warp → ScanDrift → Glitch → PixelStretch → Glass` とする。

## Phase 6 — Effect Pipeline

- [x] EffectComposer を導入する
- [x] Effect をリアルタイムに有効・無効化する
- [x] Effect の適用順序を変更可能にする
- [x] Effect パラメーターへ音声値を接続する
- [x] Effect パラメータースキーマを導入する
- [x] Effect の各パラメーターへ Audio Mapping を設定可能にする
- [x] Effect の Dry/Wet・Blend Mode・Opacity を共通化する
- [ ] Effect チェーンの複製・グループ化・部分バイパスを追加する

## Phase 7 — Composition

- [x] ReactiveSineWave
- [x] ScanDriftWave
- [x] NeonGrid
- [x] MosaicField
- [x] GlitchSpectrogram
- [x] 実行中の Composition 切り替え

## Phase 8 — 保存と出力

- [x] 現在フレームを PNG 保存する
- [x] 設定を LocalStorage へ自動保存する
- [x] プリセット JSON を入出力する
- [x] Canvas と音声を WebM 録画する

## Phase 8.5 — デザインレイヤー

- [x] 画像を背景・素材レイヤーとして読み込む
- [x] 円、波形、テキストをキャンバスへ追加する
- [x] レイヤーを選択、移動、拡大縮小、回転する
- [x] 色、透明度、Blur、Contrastを調整する
- [x] レイヤーの重なり順を変更する
- [x] デフォルトの生成波形をレイヤーとして並べ替え・削除する
- [x] 各レイヤーを音声帯域とBeatへ反応させる
- [x] デザインレイヤーを含めてPNG出力する
- [x] レイヤー設定をLocalStorageへ保存する
- [x] テキストのフォント、ウェイト、行間を選択可能にする
- [x] 矩形、線、ポリゴン、自由描画を追加する
- [x] 複数選択、整列、グループ化を追加する

## Phase 9 — 外部音源

- [x] マイク・ライン入力に対応する
- [x] Spotify 連携の実現可能性を調査する
- [x] Apple Music 連携の実現可能性を調査する

Spotify と Apple Music は、認証、DRM、音声解析の可否、利用規約を調査してから実装範囲を決める。

## Phase 10 — 品質とパフォーマンス

- [x] FPS を表示する
- [x] 描画解像度倍率を設定可能にする
- [x] Effect ごとの GPU 負荷を計測する
- [ ] リサイズと音源交換の耐久テストを行う
- [ ] モバイルと Safari で動作確認する
- [x] WebGL Context Lost に対応する

## Phase 11 — Field × Renderer への再構築

`DESIGN.md` で方針を確定し、生成（① Field）と表現（② Renderer）を分離する構造へ作り直す。
Effect チェーン（③）と Audio、Core は流用する。

- [x] `engine/` — Field × Renderer のシェーダー合成
- [x] 音声解析の追加（dominant frequency / spectral centroid / flatness / onset / sustain）
- [x] Field: Cymatics へ L1・L2 写像を接続する
- [x] Renderer: ミニマルな図形の閾値を音へ接続する
- [x] L3 ハッシュ写像（音のスペクトル形状をシードにする）
- [x] Renderer: 光と波 / グラフ
- [x] 既存 Effect チェーンを接続する（LabControls: Field / Renderer 選択・スキーマ駆動 Effect UI・録画・品質モニター再接続)
- [x] VHS 統合 Effect / 色テーマの横断化
- [x] 奥行き → トランジション（多層の場 + Renderer 間リニアクロスフェード）
- [x] Preset v4（Field / Renderer / テーマ / 奥行き / Effect 状態の保存・復元。旧形式は破棄）

再構築後の UI は LabControls に集約した。旧 StudioControls・LayerEditor は D1 により未接続のまま温存し、
RecordingController・QualityMonitor は再接続済み。

## Phase 12 — サイマティクス表現の完成度

1 表現 = 1 見え方（PRD D16）。まずサイマティクス 1 つの完成度を極める。
新しい模様より、境界のレンダリング品質を優先する（PRD D10）。
**サイマティクス単体の完成度は 2026-07-27 に一区切り（PRD §1 当面のゴール参照）。
ここからは表現の追加・VJ 実践・UI の主従合わせが並行ゴールになる。**

- [x] 構造整理 — Renderer 選択と Design タブを撤去し、表現単位の UI にする
- [x] 生成方式を Granular Plate Model（GPU 密度場シミュレーション）へ刷新（PRD D18）
- [x] 固有振動モードバンク（16 種）とスペクトル励起によるモード選択（PRD D19。?debug=1 で確認）
- [x] G1 チューニングモード — `?tune=1` で内部定数を調整し、確定値を `engine/tuning.ts` へ焼き込む
- [x] 砂の物理を実機構へ（移動度ベース・場の補間廃止・保存形フラックス。PRD D21）
- [x] V2 の分離基盤 — 表現カタログ（cymatics-v1 / v2）・Expression セレクト・Preset v6（旧データは V1）
- [x] V2 モードライブラリ — 自由端の正方形板の固有モード 18 種（Waller 近似・中央固定端・個体差。PRD D22）
- [x] V2 の質感を焼き込み（TUNING を版ごとに分離。V1 の値は据え置き）
- [x] V2 モード切替の跳ね上げ — 散ってから新しい節へ集まり直す（PRD D23。現在は切ってある）
- [ ] V2 の質感調整と V1 との比較評価 → どちらかへ収斂（D16 へ戻す）
- [ ] G1b 質感の確定 — 帯の太さ・粒・集積の速さを `?tune=1` で追い込み焼き込む
- [ ] G2 動き — 揺れ・モーフ速度・構図の引き直し頻度を決める
- [ ] G3 実楽曲での音反応 — L1〜L3 の効き方を実際の曲で調整する（移動窓の自動較正を含む。PRD D17）
- [x] 表現ごとの機能宣言に UI を合わせる — サイマティクスから奥行きスライダーを外す（PRD D25。Preset v7）
- [x] UI を理解できるところまで削る — フッター表記・FPS/解像度・Effect の Dry/Wet・Blend・Opacity・Audio Mapping を非表示（機能は温存。MTG 2026-07-27）
- [x] 動画書き出しを WebM から MP4 へ移行する（MediaRecorder の MP4/H.264 を優先し、非対応環境のみ WebM へフォールバック。PRD D25）
- [x] 表現セレクトの 2 段化 — ファミリー（Cymatics）を選び、版（V1/V2）はボタンで切替
- [x] ツールバーの整理 — 主 CTA を Record MP4 に。Export PNG はその左に控えめに配置
- [x] 画角 — 長方形の板として 7 比率に対応（1:1 / 16:9 / 9:16 / 4:3 / 3:4 / 3:2 / 2:3。PRD D26。Preset v9）
- [x] 反応マッピングの演奏 UI — Bass/Mid/Treble の励振ゲイン 3 本（PRD D24 案 1。Preset v8）
- [x] フルスクリーン出力 — Output ボタンで絵だけのポップアップを開き、第 2 画面でクリック → フルスクリーン
- [ ] UI の本設計 — **ユーザーが UI デザインを作成し、それをもとに実装へ反映する**フローで進める（2026-07-28 決定）。試作 3 案のうち案 1（Effect を引き出しに）が「曲の入れ方が分かりやすい」と評価。試作は `?ui=1|2|3` に温存
- [x] 左パネルの仕切り（旧レイヤーパネル用スプリッター）を撤去
- [ ] キーボードショートカット — 表現・テーマ・Effect の切替や反応の調整をキーで直感的に操作する。制約のあるツールだからこそ割当を固定できる（UI 本設計と合わせて設計。2026-07-28）
- Effect は絞り込まない（2026-07-27 決定）。現状の 14 個は全て残す。今後はむしろ各 Effect の詰め・細かい調整の方向
- [ ] G4 2 つ目の表現を追加し、表現の切り替え（トランジション）を作る
  - [x] 試作: Modular Pattern Field（`modular-v1`）— 円の重なりを偶奇で抜く段と、角丸セルのグリッド段を
        1 枚のフラグメントシェーダーで繋いだ表現。Spawn → Expand → GridMorph → RowCascade → Merge →
        Subdivide → FinalCluster → Clear の周期を CPU 側のフェーズ機械が音で進める。
        質感の定数は表現内の `MODULAR`（`TUNING` には混ぜない）。持つ機能は色のテーマと Restart cycle のみ
  - [ ] Modular の詰め — 周期の長さ（現状 1 周 10 秒前後）・90°回転の見え方・花型の抜きの読みやすさ
  - [x] 試作: Light Traces（`light-traces-v1`）— 黒い空間にオンセットで光源が生まれ、短い曲線移動が
        軌跡を残す表現。光源は CPU 側（最大 24 個・決定論ハッシュのみ）、軌跡は GPU の
        ping-pong Feedback Buffer（HalfFloat）で `feedback = previous * decay + emission`。
        表示は 1 - exp(-x) のトーンマッピング。Trail スライダーが decay だけを差し替えるので、
        点 → 線 → 帯 → 雲 をシミュレーションを止めずに行き来できる。
        表現ごとの調整つまみの仕組み（`LabExpression.getExpressionParams`。PRD D25）をここで追加した
  - [ ] Light Traces の詰め — テーマ非対応（黒背景固定）・表現つまみが Preset 未保存・軌跡の曲がり方
  - [x] 試作: Reactive Geometry（`reactive-geometry-v1`）— 黒い空間に線と幾何学図形が生まれ、育ち、消える表現。
        内部は 2 系統で、Line System は持続音・音程に反応する固定点数のリボン（Wave / Straight / Orbit /
        Freeform を CPU のパラメトリック関数で毎フレーム更新・最大 20 本）、Shape System はオンセットに反応する
        InstancedMesh 1 枚（Dot / Circle / Capsule / Bar / Ring をフラグメントの SDF で描き分け・最大 200 個）。
        目標密度はエネルギー履歴の蓄積（buildup）の単調増加関数で、満ちるとクライマックスとして
        一部または全部が消える。固定周期を持たない。
        表現つまみの仕組みを select（Mode / Line Type / Shape Type）と action（Clear / New Variation）へ拡張した
  - [ ] Reactive Geometry の詰め — Preset 未保存・消滅遷移中の線のにじみ・線種ごとの密度バランス
  - [x] 検証: Light Traces — Core Study（`light-core-study-v1`）— 音と光の因果関係だけを見るための
        実験表現。黒背景 + 白い Core のみで、位置は常に中央・サイズ固定・動くのは明るさだけ。
        onset の立ち上がりエッジ + 閾値 + クールダウンで発火し、Attack / Hold / Decay を
        秒で管理する（フレームレート非依存）。定数は `CORE_STUDY` に集約。
        既存の Light Traces とはコードも状態も共有しない
  - [x] 検証: Audio Feature Inspector（`src/ui/AudioInspector.ts`）— 左パネルの開閉セクション（既定は閉）。
        Volume / Bass / Mid / Treble / Onset Strength のメーターと、検出の瞬間だけ点く Onset ランプ。
        Core Study 選択中は開発用スライダーと直近 Core の値も出す
  - [x] 検証: Core Study の Onset を帯域別スペクトルフラックスへ置き換え（`BandFluxAnalyzer` / `OnsetGate`）。
        engine の onset（広帯域 Volume の差分）は、正規化 Volume が飽和する盛り上がりで発火が消え、
        持続音の上のハイハットも拾えなかった。`getSpectrum()` の生 FFT から帯域別に
        `Σ max(0, mag − prev)` を取り、ビン数で割って `max(bass, mid, treble)` で合成する。
        測る側（`BandFluxAnalyzer`）と決める側（`OnsetGate`）を分けてあり、
        局所適応閾値（方式 A）は `OnsetGate` の差し替えだけで入る。
        reference.wav の 15〜18 秒（旧方式は発火 0）で 6 発火を確認。FileAudioEngine は無変更
  - [x] 検証: 発火判定の局所適応（方式 A）。`OnsetGate` の差し替えだけで完結させ、
        `BandFluxAnalyzer` と表現本体は触っていない。閾値は直近 2.5 秒の窓から
        `max(absoluteFloor, 中央値 + k × (山の中央値 − 中央値))` で毎回作る。
        山（局所最大）だけを別に集めるのは、打撃が全フレームの数 % しかなく、
        どんな上側分位点でも疎な曲では打撃の下に潜ってしまうため。
        strength の局所正規化は別トグル。スペクトルを 0.3 倍に縮めた入力でも
        発火位置がフル音量と 57/60 一致（固定閾値は 60 → 8 に崩れる）
  - [x] 検証: Bass / Mid / Treble の独立 Onset 検出（**観察モード**）。合成 Gate から Core が
        生まれる仕組みは 1 行も変えず、`OnsetGate` を 3 本並走させて数えるだけ。
        同時発火の 5 分類（単独 3 種 / 2 帯域 / 3 帯域）と直近イベントを集計する。
        reference.wav 実測: 66 イベント中 3 帯域 48% / 2 帯域 33% / 単独 18% で、
        **81% が複数帯域**。ただしキックの 8.02 秒は bass+mid+treble でも
        treble の strength が 0.16 しかなく、同時発火の多くは打撃の漏れ込み。
        帯域ごとに Core を出すなら「最強帯域のみ」か「相対マージンで間引き」が妥当
  - [x] Core の発生源を帯域イベントへ切替。帯域 Gate の発火を 30ms の結合窓でまとめて
        1 打 = 1 イベントにし、**素のフラックスが最大の帯域**を選んで Core を出す
        （strength は帯域ごとに別々の参照値で正規化されるため帯域間で比較できない。
        明るさには従来どおり正規化後の strength を使う）。`relativeStrengthFloor`
        （既定 1.0 = 最強のみ）を下げると同時 Core が増える。合成 Gate は観察専用に反転。
        reference.wav は Core 60 個（結合前の帯域イベント 63 → 60）で、区間分布も従来どおり
  - [x] 検出処理を描画から分離（`src/engine/bandLightEvents.ts` の `BandLightEventDetector`）。
        帯域別フラックス → 帯域別 Gate → 結合窓 → 帯域選択までを 2D と 3D で共有する。
        抽出前後で 2D のイベント列は完全一致（合成シナリオのハッシュが一致）
  - [x] 試作: Light Traces — Spatial Study（`light-spatial-study-v1`）— 固定 PerspectiveCamera の
        3D 空間へ光を置く検証表現。**2D Core Study は回帰確認用に完全温存**。
        Core はカメラを向く板を InstancedMesh 1 ドローで描き、PointLight は使わない。
        位置は `SpatialPositionResolver` が音（シード・帯域・通し番号・重心）から
        決定論的に決め、カメラのフラスタムから逆算するのでどの画角でも画面外へ出ない。
        同時発光は最低距離を満たすまで引き直す。距離減衰は入れず遠近法だけで奥行きを見る
  - [x] Spatial Study を Mapping 層で組み直す（`src/expressions/spatialMapping.ts`）。
        発光の瞬間の音を `AudioEventSnapshot`（volume / 3 帯域 / 素の bandFlux /
        winningBand / centroid / onsetStrength / flatness / seed / eventIndex）で凍らせ、
        位置・明るさ・大きさ・色・速度・寿命・軌跡をここだけで決める。
        **RGB の比率は素の bandFlux から作る**（適応後の strength は帯域ごとに
        参照値が違うので比較できない）。明るさは onsetStrength だけが決めるので、
        音量が大きくても色が白へ飽和しない
  - [x] Spatial Study に大きさ・RGB・3D 移動・3D 軌跡・光学的質感を追加。
        軌跡は 3D の位置履歴（節 12 個）から作り、Core 本体と同じ InstancedMesh の
        後ろ半分として **1 ドロー**で描く。質感は核 / 中距離の滲み / 広く弱い散乱 /
        RGB の微小分離 / 距離のごく弱いコントラスト差を別々の定数で持ち、1 つずつ切れる
  - [x] Spatial Study の生成単位と色構造を作り替え（プリズム / レンズフレアのリファレンスに寄せる）。
        1 イベント = 1 Core → **Light Burst**（メイン 1 + サブ N が 5〜150ms ずれて連鎖）。
        発火閾値は表現ごとに倍率で緩められるようにし（2D は 1.0 のまま据え置き）、
        時間特性は Attack ほぼゼロ・Decay 175ms の瞬間的な点滅へ。
        色は **band→RGB を廃止し seed 由来の個別色**にして、白は加算の重なりで創発させる。
        サブは針 / スパーク / 短い弧の 3 形状（出やすさは帯域比率、細さは centroid、
        うねりの乱れは flatness）。**波形をそのまま光の形にはしない** — うねりは
        「まっすぐな光条は不自然」ぶんの微小な変形としてだけ使う
  - [x] Spatial Study の質感を「プリズムを通った光」へ寄せる（4 点）。
        ① **層の濃度** — 形ごとに 1 枚あたりの寄与を定数化（`SPATIAL_STUDY.layering`）。
        単層で白へ張り付かず、重なった段が読める濃さに落とした
        （実測: 同じスパークを重ねて 1 枚 60 → 2 枚 134 → 3 枚 184 → 4 枚 235）。
        ② **1 要素の内部の分光** — 色を RGB ではなく**色相 4 ストップ + 彩度 4 ストップ**で送り、
        フラグメントで補間する（`LightGradient`）。形式は放射状 / 放射状反転 / 軸方向 /
        軸直交 / 角度方向の 5 種を seed が選ぶ。ストップ数 2〜4・走る幅・折り返し・
        彩度の並び（白→色 / 色→白 / 一定）もすべて音由来の決定論ハッシュ。
        ③ **軸平面フラッシュを不均一な多角形へ** — 起動時に 5〜7 頂点の非対称多角形を
        12 種、**符号つき距離場**としてアトラスへ焼く（`src/expressions/polygonAtlas.ts`）。
        SDF なので 16 倍まで開いても輪郭が鋭いまま。縁のわずかなにじみは 0.5 の外側を薄く拾う。
        ④ **画面を貫く針（ray）** — 中心から垂直・水平（seed でわずかに傾く）へ、
        画面対角の 1.15 倍まで一気に伸びる極細の光。芯の太さは**画素で指定**するので
        奥行きや画角が変わっても 1〜2px を保つ。3 フレームで全長に達し、短い減衰で消える。
        強い打撃（onset ≥ 0.6）のときだけ 1〜3 本。
        いずれもインスタンス属性の追加だけで済ませ、**ドローコールは 1 のまま**
        （ピーク 38 インスタンス / バーストフレームの render は p95 0.3ms）。
        2D Core Study・`LightTraces`・`FileAudioEngine`・Effect は 1 バイトも触っていない
  - [x] Spatial Study に**プリズム質感素材（Macro layer）**を実装
        （素材は `public/assets/light-traces/`。実装指示は同梱の README / manifest）。
        1 バーストにつき**質感レイヤーを 1〜3 枚だけ**、Burst 全体を包む大きな板として置く
        （中の Spark / Needle / Ray へ個別に画像を貼らない）。
        **アトラス 1 枚（5×2 / 512px セル）+ Instancing で Draw Call は +1**。
        素材ごとの Material は作らず、素材番号・UV クロップ・回転・反転・比率・歪み・色を
        インスタンス属性で渡す（`src/expressions/prismAtlas.ts`）。
        素材は完成画像ではなく**輝度マスク**として読み、音から作った分光で再着色する。
        板の四角い輪郭は**円窓 + 多角形 SDF マスク**の二重で消し、多角形フラッシュは
        「硬い面」から質感レイヤーの外形マスクへ役割を移した。
        時間軸は Transient と分離 — Macro は 20〜160ms 遅れて開き、
        Attack 10〜80ms / Hold 40〜250ms / Decay 350〜1800ms（Bass・Sustain で長く、
        Treble 優勢で短く）。**黒へ戻る時間と細い光の芯・発光タイミングは維持。**
        発火は既存の `BandLightEventDetector` のまま（`AudioEngine.onset` は使わない）。
        `sustain` は検出層と共有する `AudioEventSnapshot` を汚さないよう
        `LightMappingSettings` 経由で渡す（2D Core Study のバイト不変を守るため）。
        実測: reference.wav 全編で 10 素材すべてが登場 / 同じ音・同じ seed で
        Macro layer のパラメーターが完全一致 / 帯域で素材の役割が偏る
        （Bass → parallel-curtains・wide-haze、Mid → curved-volume・caustic-fan、
        Treble → segmented-rays・fine-filaments）/ flatness 0.2→0.8 で歪み量が約 2.7 倍 /
        Bass+Sustain 高で Decay 1.31〜1.80s、Treble+Sustain 低で 0.25〜0.43s /
        画面の点灯率 p50 0.20・最大 0.78、最も明るい瞬間でも 12% 以上が純黒 /
        無音は完全な黒 / 60fps・Draw Call 16（うち質感レイヤーは 1）
  - [x] Spatial Study の「光の斬撃」からの脱却 — **主役を広い半透明の膜・Haze・回折線へ**。
        ① **太さ（thickness）を新設** — `LightShape.thickness` をインスタンス属性で渡し、
        頂点シェーダーが**軸と直交する方向へ実際に板を広げる**（長さを縮めるのではない）。
        `低 centroid 55% + Bass 比率 30% + Volume 15%` から作る。
        実測: Bass+低 centroid で Needle 2.20 / Arc 2.76 / Ray 芯 2.9px、
        Treble+高 centroid で 0.78 / 1.15 / 1.0px（約 2.8 倍の幅）。
        光条の伸びも 3.4→2.6 / 9.5→7.0 へ短くして斬撃感を弱めた
        ② **Macro layer を画面全体へ** — 大きさ 0.55〜1.25、1 バースト最大 4 枚、
        `macroOffsetSpread` は固定値をやめ `Volume + Sustain + Novelty` で 0.25〜0.80 を動かす。
        中心に主役を残しつつ 38% を周辺へ飛ばす Hybrid 配置
        ③ **中心と周辺の明るさ再配分** — mainScale 1.2→0.9、散乱 3.8→5.0 / 0.028→0.05、
        Bloom を threshold 0.22 / strength 0.92 / radius 0.48 へ。
        **Bloom で広げるのではなく Macro layer 自体に広い輝度分布**を持たせた
        ④ **横へ画面外まで抜ける Ray** — 発火敷居 0.6→0.45、最大 4 本、
        横に走る確率 0.68、長さ 1.15→1.30 倍。芯の太さは thickness と連動
        ⑤ **2D 感の解消（構造修正）** — Macro layer のカメラ正面固定をやめ、seed から
        決定論の法線を作って**接線・従法線から実 3D 平面として配置**。
        板のワールドサイズを深度で割るのもやめた（割ると遠近が相殺されて平面に見える）。
        Near 3〜6 / Middle 7〜12 / Far 13〜22 の 3 段へ分散し、手前ほど大きく明るい。
        実測: 画面に占める幅が 手前 1.21 / 中間 0.75 / 奥 0.50（半画面比）、
        傾き 13.8°〜64.4°、Bass・Sustain 由来の Z ドリフト 0.11〜0.48 単位/秒
        ⑥ **素材選択** — 帯域外の重み 0.28→0.12、**必ず 1 枚は Haze / Sheet 系**を確保。
        回転にも役割傾向（Curtain = 縦 / Segmented ray = 水平垂直 / Curved volume = 3D 傾斜強め）
        つまみを 4 本追加（Thickness / Macro spread / Depth / Horizontal rays）。
        **`disposed` を `setup` で戻す修正**も入れた — 表現を作り直すと素材が結び直らず
        Macro layer が黙って真っ黒になっていた。
        検証: 構成比は Macro 中央値 約 70%（実音源）、明るい画素（≥60/255）は中央値 6.6%・
        最大 26% で灰色の背景にはならない / 白飽和は最大でも画面の 0.6〜2.9% /
        無音は完全な黒 / Draw Call 16（増えていない）/ 60Hz 駆動で 1 フレーム p50 1.6ms・p90 3.6ms /
        画角 3 種・他表現の切替・2D Core Study のバイト不変を確認
  - [x] Spatial Study の中心・周囲を**ひとつのプリズム光**に統一（動画確認からの 4 点）。
        ① **中心も同じ質感レイヤーにする** — Burst ごとに原点へ
        **Prismatic Anchor** を 1 枚生成（`MacroLayerTraits.anchor`）。周囲の膜と
        まったく同じ Instanced draw・アトラス・分光・クロップ・多角形マスクを通り、
        大きさ 0.42 倍 / Attack 6ms / Hold 28ms / Decay 0.42 倍で先に消える。
        丸い Main Spark は主役から降ろし、size 0.3 倍・layering 1.2 → 0.55 の
        **ごく小さな白い芯**として残した（実測: main 0.32〜0.39 に対しサブは 0.09〜1.31）
        ② **斬撃感の除去** — 頂点シェーダーの sway を削除（フラグメントの bend
        0.22 → 0.08 だけ残す）。waviness 0.18/0.85 → **0.03/0.15**（旧比 17%）、
        弧の追加うねり倍率 1.6 を廃止、`needleAxisDeviation` 0.22 → 0.04
        （水平・垂直から **±7 度**）、Macro の UV 歪み 0.015/0.11 → 0.005/0.028。
        素材も curved 系から **layered-sheets / parallel-curtains / wide-haze /
        segmented-rays** 優先へ。実測: waviness 0〜0.146（平均 0.055）、
        軸のずれ 0〜7.17°（平均 3.36°）
        ③ **前後移動の廃止** — Transient の `velocity.z` を常に 0、Macro の drift を 0 にして
        `driftZ` の加算も削除。発生時の Near/Mid/Far 分布と 3D 法線・傾いた平面配置は温存。
        実測: 追跡した 58 層すべてで z が 1 フレームも動かない
        ④ **白飛びの抑制** — Macro の 1 枚ごとに**ソフトニー**
        `x / (1 + x/0.8)` を掛け、広い面が単独で内部 Bloom の敷居（0.22）を
        一斉に越えられないようにした。色相は変えないので、
        **違う色の層が狭い範囲で重なった場所だけ**が白へ寄る。
        実測（動画 5 秒相当の強打を含む区間）: 白飽和画素 **0**（16:9 / 9:16）、
        1:1 でも 1110px（0.08%）、最大輝度 246〜254、黒は 17〜51% 残る
  - [x] Spatial Study の膜に**面内のゆっくりした漂い**と**配置の偏り崩し**を入れた。
        ① **Membrane motion** — 発光の瞬間に Snapshot + seed で
        スクロール u/v・せん断・面内回転の速さを確定し、以後は**経過秒の純関数**として動かす
        （毎フレームの生の音響値は入れないので、音が揺れても軌道は変わらない）。
        動きは**面内だけ** — 面法線は変えず、Z 方向にもカメラ方向にも動かない。
        Sustain が長いほど大きく、Treble 優勢の短命な層はほぼ静止（×0.25）。
        実測（1 層の寿命あたり）: UV 変位 中央値 0.015 / 最大 0.150（クロップ座標）、
        面内回転 中央値 0.9° / 最大 6.8°。
        つまみ `Membrane motion`（0〜1）を追加し、**0 で完全静止**（画素差 0.00 で従来と一致）
        ② **anti-clustering** — 生きている層の画面正規化位置を直近 14 個まで覚え、
        近すぎる候補は**ハッシュ列の次の値**で最大 5 回引き直す（`Math.random()` は使わない）。
        中心の Anchor は対象外。
        実測（同一シナリオ 600 フレーム）: 重心が中央から 0.35 を超えたフレームの割合
        **21.2% → 11.6%**、3×3 象限の occupancy 変動係数 **1.73 → 0.90**、
        中央セルの占有率 **63.8% → 36.2%**。
        重心を中央へ引き戻す項も試したが、片寄りは改善せず散らばりだけ悪化したので入れていない
        検証: 決定論一致 / 発光後に音を大きく振っても軌道不変 / z は 1 フレームも動かない /
        無音 = 黒 / 白飽和 0（画角 3 種）/ Draw Call 16（増えていない）/
        1 フレーム p50 1.2ms・p90 2.0ms / 2D Core Study バイト不変
  - [x] 検証: Light Element Lab — リファレンスの光を一度に調整せず、
        **Core / Ray / Sheet / Haze / Prism / Depth / Envelope / Composite** の
        8 モードへ分解して固定条件で比較する独立表現。Expression の Version ボタンで
        即座に切り替えられる。Spatial Study の実装・状態は共有せず、安定している
        `prismAtlas` と 10 枚の素材、EffectPipeline だけを再利用した。
        Core / Sheet / Depth / Composite は同じアトラス・同じ分光シェーダーを通すため、
        中心だけ別の光に見える問題を起こさない。Ray は直線のみ、Depth は Near / Mid / Far の
        位置を固定して Z 方向へ移動させない。Envelope だけが 5 秒で
        Attack → Hold → Decay → Off を繰り返し、時間形状を単独評価できる。
        この段階では音へ接続しない。Composite の見た目を基準として採用した後に、
        各要素へ音響パラメーターを 1 関係ずつ接続する
  - [x] Light Reactive Lab — 静的な Light Element Lab の光学を**音イベント反応**へ進めた表現。
        Version ボタンで Trigger / Texture / Variation / Composite の 4 段を切り替える。
        データの流れは AudioEngine → `BandLightEventDetector` → `AudioEventSnapshot`
        → **`PrismaticBurstPlanner`（`reactiveBurst.ts`。音 → 見え方はここだけ）**
        → 各層 Envelope → 共通プリズム Shader（**1 ドロー**）→ 内部 Bloom → Effect。
        `LightReactiveLab` は Planner が返した値を描くだけで、AudioEngine を形や色へ直接入れない。
        **発生時にすべて固定する**のが芯 — 素材・色・クロップ・大きさ・太さ・位置・奥行き・
        傾き・寿命・構図タイプは発光の瞬間に確定し、以後変わるのは A/H/D の明るさ・
        発生時に決めた拡大・ごく弱い面内移動・素材内のスクロール / せん断 / 回転だけ。
        **Z 方向とカメラ方向へは動かさない**（実測: 追跡 149 層すべてで z が不動）。
        Stage 1 Trigger … Core のみ。立ち上がりで 1 回だけ発生し A/H/D で消える
        Stage 2 Texture … Core + Sheet を同じ seed・同じ基準色・同じ発生位置で。
          Core が先、Sheet は約 100ms 遅れて開き 1.68 倍長く残る。Ray はまだ出さない
        Stage 3 Variation … 構図タイプ 6 種（Vertical Veil / Diagonal Fan / Prismatic Cross /
          Depth Corridor / Wide Haze / Layered Membrane）と、素材・クロップ・比率・回転・反転・
          色相・グラデーション形式・Near-Mid-Far・傾き・A/H/D をイベントごとに変える。
          直近履歴で連続を防ぐ（実測: 構図・素材・角度・クロップ・色相帯の連続 0、
          同じ位置への集中 46% → 0%、左右比 22:7 → 17:12）
        Stage 4 Composite … Haze / Ray / Depth を追加。Wide Sheet は毎回必須、
          Haze は余韻と空気感、**Ray は強い onset（≥0.78）か Treble 優勢（≥0.62）かつ
          打撃が強い時だけ**・水平垂直基本・傾き ±0.07rad・同時 2 本まで（斬撃にしない）。
          白は異色の層の重なりだけで作り、1 枚では超えられない soft-knee を再利用。
          Bloom と Exposure は音量で直接動かさない
        検証: 4 モードすべて無音 = 完全な黒 / 決定論一致 / 白飽和 0（画角 3 種すべて）/
        Draw Call 15（層の数と素材の枚数に依らず 1 ドロー）/ 1 フレーム p50 0.2ms /
        表現を切り替えて戻しても Atlas が黒くならない / 既存の Light Element Lab と
        Light Spatial Study は比較用にそのまま残してある
  - [x] 検証: Light Element Lab 2 — **色を「1 つの光の R/G/B をわずかにずらして重ねる」
        （CRT 的構造）だけから作れるか**を見る 2 台目の実験室。V1（発光ごとに色相
        グラデーションを割り当てる方式）は無改変で温存し、比較対象として残す。
        Version ボタンで Core / Sheet / Haze / Ray / All を切り替える。**音へは繋がない**
        （静止画・要素分解で見え方だけを見る段階）。素材は作り直さず、V1 と同じ
        `prismAtlas` の 10 枚をそのまま輝度マスクとして使う。
        方式: 各要素の輝度マスクを**チャンネルごとに微小オフセットしたローカル座標で
        3 回評価**し（`elementMask(p ± dir·offset)`）、R/G/B 強度で合成する。
        オフセットは素材の UV だけでなく**窓と芯も含めた光ごと**動かすので、
        大きくすると 3 枚に分離する。インスタンスは増やさず **Draw Call 1**。
        分離方向は ① バースト中心から放射状 ② 要素自身の軸沿い（面は長辺、
        Ray だけは芯を横切る向き — 芯沿いは直線が自分に重なるだけで色が出ない）。
        色相グラデーションは既定 OFF で、比較用トグルでだけ V1 方式を被せられる。
        つまみ（開発ブロック）: R / G / B 強度・Channel offset（0〜0.5）・
        Offset direction・Channel decorrelation（0 で 3 チャンネル同一歪み）・Intensity・
        Hue gradient。
        実測（画素計測。彩度 ≥ 0.15 を「色付き」、3 チャンネル中 1 つだけが立つ画素を
        「分離」とした。全モード共通で **offset 0・decorrelation 0 は彩度ちょうど 0**）:
        色が出始める（色付き 10%）… Core 0.002 / Sheet 0.004 / Haze 0.004 /
        Ray 0.004（軸沿い）・0.011（放射状）
        内部まで色が回る（色付き 50%）… Core 0.010 / Sheet 0.013 / Haze 0.015 / Ray 0.017
        **目で明らかに色と見えるのは 0.03〜0.06**（平均彩度 0.36〜0.57）。
        画素計測はその 10 倍手前から色を検出するので、数値の立ち上がりを
        「見える境界」と読まないこと。
        3 分離が見え始める… Core 0.12 / Sheet・Haze 0.25 / Ray 0.3 以上
        白い芯は offset 0.05 くらいまで残り（Core: 白画素 13.9% → 6.6%）、0.25 でほぼ消える
        放射状は R が外・B が内を読むため**大きくすると全体が青芯・赤縁へ寄る**
        （Core 平均 RGB 102/102/102 → offset 0.5 で 1/42/167）。
        軸沿いは対称な平行移動なので**全体の色バランスは無彩色のまま**（59/61/60）局所だけ色が出る
        強度を偏らせるだけでも色は乗る（1/1/0.5 → 平均 91/91/47・彩度 0.49。
        光の広がりは不変で色だけが変わる）
        非相関だけでも色は出る（**幾何的なずれ 0 のまま**膜の内部に色の流れが出る。
        Sheet: 非相関 0.05 で色付き 15.6% / 0.2 で 56% / 1.0 で 90%。
        平均 RGB は 79/86/84 のままなので全体が染まるのではなく筋ごとに分かれる）
        V1 方式との比較（All・9:16）: グラデーション ON は平均彩度 0.66・白画素 0.7% で
        **芯まで染まる**。OFF（チャンネルのみ）は平均彩度 0.34・白画素 9.0% で
        **芯は白く残り、色はチャンネルが食い違う場所（縁と内部の筋）にだけ出る**
        検証: 決定論一致（同一設定で画素ハッシュ一致・表現を往復しても一致）/
        Draw Call 1（All の 5 層でも 1・三角 10）/ 1 フレーム p50 0.0ms・p90 0.2ms・
        最大 1.1ms（1342×754）/ rAF 間隔 17.3ms（≒60fps）/ 画角 3 種（1:1・16:9・9:16）
        すべて Draw Call 1・同じ色挙動 / コンソールエラー 0 /
        表現を往復しても geometries 1・textures 3 で増えない（dispose 完備）/
        極端な値（intensity 0・全 gain 0・offset 0.5 + 非相関 1）で NaN・黒画面なし /
        既存表現は無影響（Element Lab V1 Core / Composite・Reactive Lab Trigger /
        Composite・Spatial Study・Cymatics V1 をすべて再確認。音なしの表現は黒のまま）
  - [x] Light Element Lab 2 — **リファレンスの頂点フレーム 1 枚へ寄せる静止画修正**。
        連番フレーム分析で確定した「4 層の時間構造」をそのまま構造にした。
        ① **不動の骨格**（縦の細い線 + 横の帯 + 中央の十字。中央軸に固定で回転・移動・
        うねりなし）② **コアのエネルギー脈動**（膨らんで引く。**白へ到達してよい唯一の層**）
        ③ **周縁の一時的断片**（三角のヴェール片。**不規則さを担うのはこの層だけ**で、
        位置・大きさ・向きは決定論ハッシュ）④ **放射の扇**（高エネルギー時のみの閾値ゲート）。
        Version ボタンをこの 4 層 + All に再編した。
        設計思想は「多数の光を個別に動かす」のではなく
        **「固定された光学系に、音がエネルギーと波長を注ぎ込む」**。
        修正 4 点:
        1 **白の予算制** — 非コア層は天井（0.30）とチャンネル分離の**下限**
          （オフセット 0.02〜0.05 / 非相関 0.12〜0.30）を持ち、つまみを 0 にしても
          3 チャンネルが重なりきらない。実測: Skeleton / Fragment / Fan を単独表示すると
          **白画素 0・準白（min ≥ 235）も 0**。Intensity を最大 4.0 にしても 0 のまま。
          All の白 1074px は正規化 [0.473, 0.472]–[0.535, 0.524]（画面の 6.2% × 5.3%）に収まり、
          Core 単独の白の箱と一致する（白は全部コア由来）
        2 **奥行きの手がかり** — `depthCue(z)` の**1 本の式**だけで「遠いほど暗く・鈍く」。
          層ごとの個別調整はしない。鈍りはアトラスを 5 タップの平均で読む半径になる。
          実測（Depth probe で見かけの大きさと位置を保ったまま z だけ動かす）:
          z −5.07 → −14 で平均輝度 27.30 → 8.23（dim 0.965 → 0.30 と一致）、
          平均勾配 3.71 → 0.72。**どちらも単調減少**（勾配は減光ぶんの 1.15 より下なので、
          ぼけが独立に効いている）
        3 **構図** — 骨格と扇は中央軸に固定。断片だけが決定論ハッシュで散る。
          実測: 同 seed で画素ハッシュ完全一致（3 回）、seed 0 / 7 / 23 で別配置、
          **骨格は seed を変えても画素ハッシュ不変**
        4 **グローバル位相 H（1 変数）** — 各層は `spectrum(H + 層の小オフセット + 勾配)` で
          発色し、独立の色を持たない。分光は白へ 0.72 だけ寄せてある（光は「染まった白」で
          絵の具ではない。ここを浅くするとチャンネル分離の縁の色が波長の下から見える）。
          実測（円周平均色相）: H 0 → 0.875 で All 325° → 32° を**単調に一周**し、
          同じ H での 4 層の食い違いは **13〜35°**（層ごとの独立色は出ない）。
          画面内の色相の広がりは 0.025〜0.33（大半 0.13 未満）＝ 1 枚が 1 つの波長状態
        **音 → 見え方の対応は `lightOpticsMapping.ts` の 1 ファイルに集約**
        （`spatialMapping.ts` と同じ方式）。描画側は traits と uniform を受け取るだけで、
        シェーダーに音の前提も時間アニメーションも入らない（静止画なので時計を持たない）。
        次フェーズの配線口は `OpticsDrive` の 6 入力:
        音量（持続）→ skeletonLevel（0 で骨格ごと消えて黒 = D5）/ onset 強度 → corePulse /
        帯域イベント → fragmentEnergy / 強 onset 閾値 → fanGate /
        音色の持続値 → huePhase（補間せずイベント的に切替）/ 音由来のシード → seed
        検証: 頂点フレーム（All・9:16）は黒 78.1%・白はコアだけ・平均彩度 0.60・
        色相ヒストグラムは 1 山（緑〜シアン）/ 画角 3 種すべて **Draw Call 1**（三角 22。
        層 11 枚でも 1 ドロー）・白は 3 種とも中央の箱に収まる（黒 78〜89%）/
        決定論一致（同一設定 2 回 + 表現往復で画素ハッシュ一致）/ 1 フレーム p50 0.0ms・
        p90 0.1ms・最大 0.3ms / rAF p50 8.8ms（120Hz 表示で追随。60fps 以上）/
        コンソールエラー 0 / 表現往復で geometries 1・textures 2 のまま（dispose 完備）/
        極端値（intensity 0・全 gain 0・intensity 4 + offset 0.5 + 非相関 1）で NaN・黒画面なし /
        **全ドライブ 0 + 扇ゲート閉で層 0 枚・黒 100%**（無音 = 黒の経路が通っている）/
        既存表現は無影響（Element Lab V1 Core / Composite・Reactive Lab Composite・
        Spatial Study・Cymatics V1・Modular V1 を再確認。数値は修正前と一致）
  - [x] Light Element Lab 2 — **5 番目の種別「膜（haze）」を固定光学リグへ追加**。
        core / beam / veil / fan の 4 種別だけでは**画面全体をまとめる大面積・低輝度の霞が
        欠けており、要素が黒に浮いて孤立して見えた**。役割は 3 つ —
        ① 明るさの階層の床（黒と中輝度の間を埋める）② 光が散乱する媒質の存在感
        ③ 最遠の奥行き層。
        実装: **2 枚**（コア中心の大きな放射グロー / 交差の高さに横たわる水平の帯）。
        どちらも **z = −13.6（リグ最遠。断片の最奥 −10.4 より奥）** で、板は画面の
        1.25 倍・1.4 倍。テクスチャは低周波のムラ（sin×cos）+ 細かい粒 +
        プリズムアトラスの `wide-haze` / `wide-caustic`（ベタの放射グラデーションにしない）。
        既存 4 種別と同じ **1 ドローのフラグメント分岐**（`kind > 3.5`）。
        **明るさの階層を天井として明文化した** — 従来の真偽値 `whiteAllowed` を
        層ごとの `ceiling` に置き換え、コア 1.0（白へ届く）> 骨格・断片・扇 0.30 >
        **膜 0.11**。実測ピーク（画角 9:16・単独表示）は
        コア 255 > 骨格 189 > 断片 106 > 扇 77 > **膜 22**（= コアの 1/11.6。目標の 1/8 以下）。
        Intensity を最大 4.0 にしても膜のピークは 54・白画素 0。
        **画面の縁で黒へ落とすフォールオフ**を必ず通す（板が画面より大きいので、
        窓が 0 になる半径は画面の内側にある）。実測: 膜単独で画面端 2% 帯の最大値 **0**、
        All でも膜の有無で端の統計が**完全に一致**（最大 109 / 平均 1.587。
        端の 109 は骨格の縦線が画面を貫くぶんで、膜の寄与は 0）。
        **音入力は増やしていない** — 膜は骨格と同じ `skeletonLevel` を受け、
        速度だけが違う。速度の階層を `RESPONSE_SECONDS` に定数として置いた
        （膜 2.6s → 骨格 0.9s → コア脈動 0.12s → 断片 0.05s）。次フェーズの平滑化時定数になる。
        色はグローバル位相 H に従い、分光の深さを 0.5〜0.55 倍に落として彩度を下げてある
        （実測の平均彩度は膜 0.39 で全層中もっとも低い。コア 0.60 / 断片 0.63 / 扇 0.61）。
        Version セレクタに **Haze** を追加（膜単独表示ができる）。
        膜あり・なしの A/B は開発用トグル `Haze layer (dev A/B)` で往復する
        （音入力ではなく表示の間引き）。
        検証（All・9:16・膜なし → あり）: 黒 78.1% → **70.9%**（暗部の支配は保たれる）/
        全画面の平均輝度 7.91 → **9.93**（+2.0/255 = フルスケールの 0.8%。濁っていない）/
        白画素 1074 → 1133（+59・+5.5%）・準白 1187 → 1243 で、**白の箱は
        [0.471, 0.470]–[0.536, 0.524] のまま**（増分もコアの箱の中）/
        画角 3 種すべて **Draw Call 1**（三角 26。層 13 枚でも 1 ドロー）・黒 70.9〜81.0% /
        H を 0 / 0.25 / 0.5 / 0.75 と回すと膜も全体と協調してスライド
        （All との差は +4.6〜+24.4° で、他層が占める ±35° の帯の中）/
        決定論一致・表現往復で geometries 1・textures 2 のまま /
        1 フレーム p50 0.0ms・p90 0.1ms・最大 0.4ms（13 層でも 11 層と同値）/
        コンソールエラー 0 / **全ドライブ 0 + 扇ゲート閉で層 0 枚・黒 100%**
        （膜は骨格と同じ入力なので一緒に消える = 無音は黒）/
        既存表現は無影響（Element Lab V1 Core / Composite・Reactive Lab Composite・
        Spatial Study・Cymatics V1・Modular V1。数値は追加前と一致）
  - [x] Light Element Lab 2 — **カーテン（`kind: 'curtain'`）の追加と、断片の形状多様化**。
        ① **カーテン** — 膜よりはっきりした形を持つが薄い層。旧 Light Element Lab V1 の
        Depth に相当する要素がリグに無く、要素が黒に浮いて孤立していたため足した。
        **形状族 3 つ**（`CURTAIN_FAMILIES`）— standing-veil（縦に立つ襞のあるヴェール）/
        drifting-band（斜めに流れる帯）/ folded-ribbon（折れたリボン状）。
        3 枚で、族・位置・大きさ・傾き・襞の周期・折れの量は **seed が決める**。
        素材はアトラスの `parallel-curtains` / `filament-and-curtain` / `wide-caustic` /
        `folded-ribbon` / `curved-volume` を族ごとに選び分ける。z は −8.2〜−11.8（中間〜遠め。
        断片と膜の間）。**天井 0.20**（膜 0.11 と断片 0.30 の間）で白へは到達しない。
        音入力は増やさず、骨格と同じ `skeletonLevel` を受ける
        （時定数は `RESPONSE_SECONDS.curtain = 1.4s`。膜 2.6 > **カーテン 1.4** > 骨格 0.9 >
        コア 0.12 > 断片 0.05）。
        ② **断片の形状多様化** — 三角 1 種類だと「同じ形が飛び回っている」ようにしか
        見えなかったので、輪郭の作り方から **4 族**にした（`FRAGMENT_FAMILIES`）—
        shard（三角シャード）/ sliver（細長いスリヴァー。2 円の交わりで両端が尖る）/
        plate（不等辺の四辺形の板片）/ chip（角の欠けた小片）。
        伸び（縦横比）は板が持ち、輪郭だけを族が作る。個体差は縦横比・形の中の伸び・
        欠けの深さ・傾き。
        **族の抽選は層化した** — 素のハッシュだと seed によっては 6 枚とも同じ族になり
        （実測: seed 0 で shard × 6・全 36 枚で shard 19 / chip 2 と偏った）、
        意味がなくなる。並び順を seed でずらしつつ、ときどき 1 つ飛ばして規則性も消す。
        層化後の実測（6 seed × 6 枚 = 36 枚）: plate 10 / sliver 10 / chip 8 / shard 8 と
        ほぼ均等、1 seed あたりの族数は 3〜4（全部同じ族になる seed は無い）。
        カーテンも同様に層化（6 seed × 3 枚 = 18 枚で folded 7 / standing 6 / drifting 5）。
        なお `whiteAllowed` の真偽値は前段で層ごとの `ceiling` に置き換え済みで、
        **明るさの階層**は コア 1.0 > 断片・骨格・扇 0.30 > **カーテン 0.20** > 膜 0.11。
        検証: カーテン単独で **白画素 0**（Intensity 最大 4.0 でも 0・ピーク 68）、
        ピーク 29〜64 で膜（22）と断片（40〜77）の間 / 同 seed で画素ハッシュ再現・
        別 seed で別の族構成 / H を 0 / 0.25 / 0.5 / 0.75 と回すとカーテンも協調してスライド
        （All との差は −3.9〜+13.1°。断片の ±23° より狭い）/
        断片単独も全 seed で白画素 0 / **All（9:16・seed 7）は 16 層**
        （膜 2 + カーテン 3 + 骨格 3 + 断片 6 + 扇 1 + コア 1）で
        黒 0.7088 → **0.7173**・白 1133 → 1223・全画面平均輝度 9.93 → 10.94（+0.4%）
        — 暗部の支配は保たれ、白の箱は中央のまま（頂点フレームの構図は不変）/
        画角 3 種すべて **Draw Call 1**（三角 32）・黒 0.717〜0.753 /
        決定論一致・表現往復で geometries 1・textures 2 のまま /
        1 フレーム p50 0.0ms・p90 0.1ms・最大 0.2ms / コンソールエラー 0 /
        全ドライブ 0 + 扇ゲート閉で層 0 枚・黒 100% /
        既存表現は無影響（Element Lab V1 Core / Composite・Reactive Lab Composite・
        Spatial Study・Cymatics V1・Modular V1。数値は追加前と一致）
  - [x] Light Element Lab 2 の音接続 **Step 0 — ドライブ切替の骨組み**（見た目の変化なし）。
        開発ブロックに **`Drive: Manual / Audio`** を追加（既定 Manual）。
        **音 → `OpticsDrive` の変換は新設した `opticsAudioDrive.ts` 1 ファイルに集約**し、
        リグ（`lightOpticsMapping.ts`）も描画（`LightElementLab2.ts`）も
        `AudioParameters` を直接読まない構造にした。Step 0 の時点では層を出すドライブを
        すべて 0 で返す骨組みで、Step 1〜5 の配線予定はファイル冒頭に書いてある。
        `OpticsDrive` に `curtainLevel` / `hazeLevel` を足したが、**音の入力が増えたのではない**
        — 骨格・カーテン・膜は同じソースを受け、**時定数だけが違う**（`RESPONSE_SECONDS`）。
        検証: **Manual の画素ハッシュが実装前と全 7 Version で完全一致**
        （Haze / Curtain / Skeleton / Core / Fragment / Fan / All）。
        Manual → Audio → Manual の往復後も一致。Audio + 無音は層 0 枚・黒 100%
  - [x] Light Element Lab 2 の音接続 **Step 1 — 音量の持続 → 骨格・カーテン・膜の基礎輝度**。
        `AudioEngine` の `volume`（正規化済み）を 1 つのソースとし、
        **3 層が同じソースをそれぞれの時定数で受ける**（骨格 0.9s / カーテン 1.4s / 膜 2.6s）。
        平滑は **dt ベースの指数平滑** `alpha = 1 − exp(−dt/τ)` でフレームレート非依存。
        `active !== 1` ではソース 0 になり、3 層が各時定数で黒へ沈む（PRD D5）。
        onset・帯域イベント・H・断片・扇・コアは**まだ繋がない**
        （Audio では corePulse / fragmentEnergy / fanGate は 0、huePhase と seed は開発つまみの値）。
        検証（合成の持続音・時計を手で進めて実測）:
        **立ち上がりの 63% 到達 = 骨格 0.90s / カーテン 1.40s / 膜 2.60s**（設計値と一致）、
        **減衰の 63% も同じ 0.90 / 1.40 / 2.60**。
        音を切ってから画素が消えるまで（0.031 = 8/255 相当を下回るまで）は
        骨格 3.13s → カーテン 4.87s → **膜 8.98s** で、**膜が最後に消える**。
        ちらつき耐性: volume を毎フレーム 0〜1 で乱高下させても 1 フレームあたりの変化は
        骨格 0.0111 / カーテン 0.0078 / 膜 0.0048（≦ 1.1%）で滑らか。
        フレームレート非依存: dt = 1/30・1/60・1/120・1/240 のいずれでも
        3 秒後の値が **小数 4 桁まで一致**（0.9643 / 0.8827 / 0.6846）。
        reference.wav の実再生でも曲の強弱に追従し
        （持続部で骨格 0.43 / カーテン 0.43 / 膜 0.41、静かな区間で 3 層とも減衰して
        **膜だけが 0.173 と最後まで残る**、強打で骨格 0.699 がいちばん速く跳ねる）、
        停止後は黒へ戻る。Audio では層は 8 枚（骨格 3 + カーテン 3 + 膜 2）だけが出る。
        回帰: **Manual の静止画スタディは全 7 Version で画素ハッシュ不変** /
        画角 3 種すべて Draw Call 1（三角 32）・黒 0.717〜0.753 /
        1 フレーム p50 0.1ms・p90 0.3ms / コンソールエラー 0 /
        既存表現は無影響（Element Lab V1 Core / Composite・Reactive Lab Composite・
        Spatial Study・Cymatics V1・Modular V1。数値は追加前と一致）
  - [x] Light Element Lab 2 の音接続 **Step 2 — onset 強度 → コア脈動**。
        Step 1 までは「ゆっくり明るく / 暗くなるだけ」だったので、
        **音の強さに合わせた瞬間的な明るさの変化**をここで入れる。
        `opticsAudioDrive.ts` が `BandLightEventDetector`（`bandLightEvents.ts`）を持ち、
        発火イベントの `strength` で `corePulse` を駆動する。**検出器・リグ・シェーダーは無改変**。
        エンベロープ: 発火したフレームで **即座に** `max(現在値, strength)` へ跳ね、
        短い Hold のあと `RESPONSE_SECONDS.core = 0.12s` で戻る。
        持続の平滑（Step 1）より先に脈動を進めてから発火を適用するので、
        **同じフレームで来た打撃が減衰に食われない**。
        検出器の運転設定は Light Reactive Lab と同じ既定値を焼き込み（開発つまみには出さない）。
        弱すぎる打撃も「1 打あった」と分かるよう下限 0.22 を置いた。
        実測（合成の単発音・時計を手で進めて計測）:
        **attack = 0 フレーム**（発火フレームでいきなり頂点 1.0）、
        Hold 約 3 フレーム（0.05s）、**減衰の 63% = 0.116s**（設計値 0.12s と一致。
        頂点からの通算は 0.183s）。
        **1 打 = 1 脈動** — 単発で総脈動数 1、0.5 秒間隔の 8 打で 8 脈動、
        打の直前の谷は 0.036 まで落ちるので「点滅」として読める。
        **一定音では再発火しない** — 10 秒鳴らしっぱなしで脈動 1 回（立ち上がりのみ）、最終値 0。
        **strength 比例** — 振幅 0.12 / 0.25 / 0.45 → ピーク 0.255 / 0.580 / 1.0
        （検出器の局所正規化が飽和するところまで比例。ピークは strength と完全一致）。
        band-demo.wav（12 秒）: **91 脈動**・帯域内訳 mid 61 / treble 22 / bass 8 で、
        キック区間もハット区間も毎秒 6〜10 回発火する。曲の終端で脈動 0。
        reference.wav（20 秒）: **52 脈動**・発火間隔が **0.48〜0.51 秒**（≒ 拍）に揃い、
        次の区間では 0.24〜0.27 秒（倍テンポ）へ切り替わる。
        描画された中心の輝度は 1 サンプルで 0 → 255 まで跳ね（最大上昇 245/255）、
        そのあと約 0.5 秒かけて落ちる — **拍ごとに芯が閃く**見え方になった。
        決定論（同じ台本 3 回で脈動列が完全一致）/ フレームレート非依存
        （dt = 1/30〜1/240 で発火数 1・ピーク 1.0 とも同一。Hold がフレーム量子化されるぶん
        減衰の途中値だけ 1 フレーム未満ずれる）/
        回帰: **Manual の静止画スタディは全 7 Version で画素ハッシュ不変** /
        Audio + 無音は層 0 枚・脈動 0・黒 100% / 画角 3 種すべて Draw Call 1（三角 32）/
        1 フレーム p50 0.0ms・p90 0.1ms / コンソールエラー 0 /
        既存表現は無影響（Element Lab V1 Core / Composite・Reactive Lab Composite・
        Spatial Study・Cymatics V1・Modular V1。数値は追加前と一致）
  - [x] **時間表現の恒久方針 — 光は連続量として動かさない**（今後のすべての光に適用）。
        すべての光は「その瞬間の音に対応した状態」で瞬間的に現れ、瞬間的に消える。
        フェード・補間・連続的なスケール変化は描かず、**変化は表示 / 非表示の出し入れ**で表す
        （リファレンス GIF のコマ送り感）。
        ① **光学クロックの量子化** — `OpticsAudioDrive` が **24fps 相当（41.7ms）のティック**を
        刻み、ティックの頭でだけ全層の表示状態を再サンプルする。ティックの間は保持し、
        値は動かない。dt ベースなので実フレームレートに依存しない。
        **遅い時定数（0.9 / 1.4 / 2.6s）は「状態の量」として連続で回し続ける** —
        膜がゆっくり満ちて最後まで残る階層は保ったまま、見え方だけが離散になる。
        ② **表示 / 非表示の交互と位相分散** — 各層はティック単位で on / off を交互に切り替える。
        位相は seed 由来のオフセット + **グループ内で層化**（`index` で 1 ずつずらす）なので、
        2 枚以上ある層は必ずどれかが点いており、**全画面が同時に消える瞬間はない**。
        ③ **表示のたびに再サンプル** — on になった瞬間のドライブ値でサイズ・輝度が確定し、
        その表示期間は固定。適用範囲は膜・カーテンを含む全種別。
        開発ブロックに `Strobe: On/Off`（A/B 比較）と `Strobe rate`（既定 24）を追加。
        **Manual の静止画スタディは連続表示のまま**（`tick = -1`。計測器として温存）。
        実測: ティック間隔は 60fps 実フレームで 0.0333 / 0.0500s を交互に取り、
        **平均 0.0417s = ちょうど 1/24** / **ティック内での表示状態の変化は 0 回**
        （71 回の変化はすべて境界）/ 全要素同時ブラックアウトのフレームは
        **冒頭の立ち上がり 3 フレームのみ**で、以後は常に層が出ている /
        ティック速度は dt = 1/30〜1/240 のいずれでも 23.67 回/秒（つまみ 12 / 24 / 48 も追従）/
        Strobe Off では tick = −1・全 8 層が常時表示・値は毎フレーム連続変化（A/B が効く）
  - [x] Light Element Lab 2 — **コアの絞り込みと形状バリエーション**。
        弱い音でも明るすぎるという指摘を受け、**強い打撃のときだけ大きく強く発光**させる。
        `OpticsAudioDrive` に **strength 閾値 0.55** を置き、越えたぶんを
        `0.28 + 0.72 × above^1.6` の曲線で強い側へ寄せる（`buildCore` 側は線形のまま）。
        コアは打撃の **1 ティック（実フレーム 2〜3 枚）だけ表示 → 消灯**し、打撃間は完全に消える
        （場は骨格・カーテン・膜が保つ）。
        **形状族 4 種**（`CORE_SHAPES`）— cross-flare（十字フレア）/ vertical-spike（縦スパイク優勢）/
        horizontal-flare（横フレア優勢）/ compact-burst（コンパクトな星形）。
        打撃の音のシードと通し番号から層化して選ぶので「毎回同じコア」にならない。
        静止画スタディ用に **素の芯（−1）** を残し、開発つまみ `Core shape (Manual)` で
        5 通りを見比べられる（既定は素の芯なので Manual の見え方は不変）。
        実測: 打撃の振幅 0.1 / 0.2 では **コアが出ない**（打撃は検出されるが脈動 0）、
        0.3 で 0.414、0.45 以上で 1.0 / コアの表示は 2 フレームで消灯 /
        形状族 16 打の分布は {0:5, 1:4, 2:4, 3:3} で 4 種すべて出る /
        reference.wav 実再生 13 秒では **39 打のうち 32 打がコアを出し**（7 打は閾値で落ちる）、
        形状の内訳は {0:6, 1:7, 2:12, 3:7}、発火間隔 0.49〜0.51 秒（拍に乗っている）/
        Manual の 5 通りは画素ハッシュがすべて異なる
        共通の検証: **Manual の静止画スタディは全 7 Version で画素ハッシュ不変** /
        決定論（同じ台本 3 回でティック・脈動・形状・層数が完全一致）/
        Audio + 無音は層 0 枚・黒 100% / 画角 3 種すべて Draw Call 1（三角 32）・
        黒 0.717〜0.753 / 1 フレーム p50 0.0ms・p90 0.1ms / コンソールエラー 0 /
        既存表現は無影響（Element Lab V1 Core / Composite・Reactive Lab Composite・
        Spatial Study・Cymatics V1・Modular V1。数値は追加前と一致）
  - [x] Light Element Lab 2 の音接続 **Step 3 — 帯域イベント → 断片の誕生 + seed**。
        アダプタの `BandLightEventDetector` が拾った打撃 1 つが**断片を 1〜4 枚生む**。
        枚数は `1 + strength × 1.6 + novelty × 1.4`（上限 4）、寿命は
        `1 + strength × 3` ティック（上限 4）。同時に生きられるのは 12 枚までで、
        **上限では新規を抑制する**（最古を突然消すと「消えた」ことが見えてしまう）。
        形状族・位置・縦横比・欠け・傾きは**イベント固有の音シード**
        （`audioSeed × 100003 + eventIndex × 7919`）だけから決まる。
        発火帯域は**控えめな寄せ**にだけ使う — bass はやや大きめ（×1.26）でやや低い位置、
        treble は小さめ（×0.78）でやや高い位置、族の並びも 1 つずつずらす。
        時間特性は恒久方針どおり: **誕生は次のティックの頭**（出入りをティック境界に揃える）、
        生きているあいだ on / off を交互に繰り返し、最後のティックで**フェードなしに消える**。
        Manual（`Fragment energy` / `seed`）の作り方は温存し、同じ `makeFragment` を通す。
        実測: 誕生数と寿命が strength に追従（振幅 0.1 → 3 枚 / 2 ティック、
        0.4 以上で 4 枚 / 4 ティックで頭打ち）/ **表示の変化はティック境界のみ**
        （境界 40 回・ティック内 0 回）/ 形状族 4 種が均等に出て、誕生セットは打撃ごとに変わる
        （`chip+plate+shard+sliver` → `chip+plate+plate+shard` → `shard+shard+sliver` …）/
        連打 40 回でも live は 8 枚で頭打ち・抑制 0 /
        band-demo.wav 12 秒で **110 枚 / 43 イベント**（bass 53・treble 47・mid 10。
        キック区間もハット区間も毎秒 7〜13 枚）/ reference.wav 12 秒で **104 枚 / 36 イベント**、
        誕生は 0.08 / 0.58 / 1.08 … と **0.5 秒間隔＝拍**に乗る /
        無音で live 0・層 0・黒 100% / 決定論一致 / Manual 全 7 Version の画素ハッシュ不変
  - [x] Light Element Lab 2 — **打撃に同期したアームの閃光と、方向のランダム化**。
        コアが閃く瞬間、**コアから伸びる光条（アーム）も一緒に強く光る**。
        骨格の常設の十字（音量で光る層）はそのままで、その上に**一時的なアーム**を重ねる。
        新種別 `kind: 'arm'` を追加（片側だけへ伸びる直線。1 ドローの分岐は変えない）。
        **方向は打撃ごとに変わる** — `ARM_SETS` の重み表（1 方向が出やすく、2 方向が次点、
        3 方向はまれ、全方向は 1/29）から、`(eventIndex × 7 + 音シード由来のオフセット) % 29`
        で層化して引く。歩幅 7 が表長 29 と互いに素なので同じ組が続けて出ない。
        長さ・太さ・明るさには 1 本ごとの個体差を入れ、毎回同じ十字にならないようにした。
        **役割の分担**: コアの形状族（`CORE_SHAPES`）が**アームの太さ・長さのスタイル**を決め、
        **どの方向へ出るかは独立に打撃のシード**が決める。芯まわりの小さなフレアは
        アームの根元なので、**アームが出ている軸だけ**光るように絞ってある（重複しない）。
        明るさはコアと同じ閾値・曲線の世界（`armStrength = corePulse`）で、
        天井は 0.62（コア 1.0 と他層 0.30 の間）。強打では 1 ティック長く残る。
        実測: 20 打の方向列は `U, UL, D, URL, DL, D, UDL, DL, D, RDL, RD, R, RL, UR, R, RL,
        UR, U, UD, L` で **同じ組の連続 0 回**、内訳は 1 方向 8 / 2 方向 9 / 3 方向 3 /
        全方向 0（重み表どおり全方向はまれ）/ **コアとアームの点灯フレームが完全一致**
        （どちらも 20 フレーム・不一致 0）/ アームの明るさは pulse 0.2 → 1.0 で
        点灯画素 0.028 → 0.057・白画素 43 → 1942 と単調に追従 /
        **弱打（振幅 0.1・0.2 = コア閾値未満）ではアームも出ない** /
        アームの消灯はティック境界（出現はコアと同じ打撃フレーム＝完全同期）/
        決定論一致 / Manual は `Arms (Manual)` が既定 0 なので**全 7 Version で画素ハッシュ不変**
        共通の検証: 画角 3 種すべて Draw Call 1（三角 32）・黒 0.717〜0.753 /
        1 フレーム p50 0.0ms・p90 0.1ms / Audio + 無音は層 0 枚・黒 100% /
        コンソールエラー 0 / 既存表現は無影響（Element Lab V1 Composite・
        Reactive Lab Composite・Spatial Study・Cymatics V1・Modular V1。数値は追加前と一致）
  - [x] Light Element Lab 2 — **コアの閾値を緩めて閃きの頻度を上げる**。
        コアが出る打撃が少なすぎて閃きが疎だったので、strength 閾値を
        **0.55 → 0.38** へ下げた。強い側を強調する曲線（`above^1.6`）は据え置きなので、
        「弱い打撃も光るが、頂点は強打だけ」という階層は保たれる。
        閾値は焼き込みのままだが、切り分け用に **開発つまみ `Core threshold`**
        （0〜0.8・既定 0.38）を AudioInspector の dev ブロックへ足した（本番 UI には出さない）。
        実測: 同じ再生の打撃列で **コア出現率 48.6%（18/37・旧 0.55）→ 70.3%（26/37）**、
        別走でも 31.6% → 57.9%。band-demo.wav では 57.5% → **75%（30/40）**。
        振幅掃引では strength 0.339 で出ず・0.455 で出る（閾値どおり）
  - [x] Light Element Lab 2 の音接続 **Step 4 — 強打 → 放射の扇**。
        **コアより高い閾値**（strength 0.75。開発つまみ `Fan threshold`）を越えた強打だけが
        既存の `kind: 'fan'` を開く。これで発光の階層が 3 段になった —
        **弱打 = 無 / 中打 = コア + アーム / 強打 = コア + アーム + 扇**。
        時間特性は恒久方針どおり: **打撃ティックで即出現**し、寿命は
        `2 + above × 2` ティック（2〜4・強打ほど長い）、生きているあいだ on / off を
        交互に繰り返して**フェードなしに消灯**する。誕生直後の on だけは実フレーム下限
        （2 枚）を満たすまで歳を取らせないので、**消灯は必ずティック境界**に揃う。
        広がり・到達・強さは打撃の強さに追従（`half` ×0.82〜1.0 / `reach` 0.46〜0.66 /
        `intensity` 0.44 × power）。**向き・角度幅には打撃シードの個体差**を入れたが、
        基準角は下向きのまま ±0.25 rad（≒ ±14°）しか振れず、広がりは 0.62〜1.02、
        本数は 2.6〜3.8。`fanSeed < 0` は**素の扇**で、Manual の静止画スタディは不変。
        扇は 1 枚しかないので `strobed()` の間引きをやめ、断片と同じく寿命と on/off を
        アダプタ側で決める（`STROBE_KEY.fan` は廃止）。
        実測: 振幅掃引で 3 段階が閾値どおり（strength 0.339 = 無 / 0.455・0.688 = コア + アーム /
        0.911・1.0 = + 扇）/ reference.wav 13 秒は 37 打中 **扇 13 回**（35.1%）で
        **表示時間割合 8.5%**（775 フレーム中 66）、弱い区間（0〜1s・5〜7s）は扇 0 /
        band-demo.wav 12 秒は 40 打中 **13 回**・表示割合 8.6% で、4s・6s・7s・11s は
        4 打あっても扇 0（強い区間だけ開く）/ 個体差は 12 回の強打で
        **基準角が全て異なり**（−93.3°〜−69.3°）広がり 0.622〜1.018・本数 2.69〜3.61 /
        **扇の出入りはティック境界と打撃フレームのみ**（ティック内の切り替わり 0 回）
        共通の検証: 決定論（同じ台本 3 回でティック・脈動・アーム・扇・形が完全一致）/
        **Manual の静止画スタディは全 7 Version で画素ハッシュ不変**（変更前後を実測して一致。
        Manual の扇の traits も従来値と同一: shape `[-1.42, 0.82, 3.2, 0.66]` / half `[2.5, 2.5]` /
        intensity 0.44）/ 無音は扇 0・コア 0・黒 100% / 画角 3 種すべて Draw Call 1（三角 20）・
        最大 10 層 / 1 フレーム p50 0.1ms・p90 0.2ms / コンソールエラー 0 /
        既存表現は無影響（Element Lab V1 Core / Composite・Reactive Lab Composite・
        Spatial Study・Cymatics V1・Modular V1）
  - [x] Light Element Lab 2 の音接続 **Step 5 — 音色の持続値 → グローバル H のイベント的切替**。
        **これで写像 5 本の配線が完了した** — 音量の持続 → 骨格・カーテン・膜 /
        打撃 → コア + アーム / 帯域イベント → 断片 / 強打 → 扇 / **音色 → H**。
        H は 0〜1 の連続量として動かさず、**8 個の離散状態**のどれかに留まる。
        状態 → H は色相環を 8 等分し、8 と互いに素な**歩幅 3** で歩く
        （`(0.62 + 3n/8) mod 1`）。状態 0 は静止画スタディの既定 0.62 で、
        歩幅のおかげで**隣の状態でも色は大きく変わる**（「緑の回・赤の回」が丸ごと入れ替わる）。
        音色は `0.62 × tone + 0.38 × centroid`（`tone` は帯域の傾き
        `(treble − bass) / (bass + mid + treble)` を利得 1.5 で広げたもの）を
        **時定数 1.6 秒**で平滑した持続値。打撃 1 発では動かない。
        切替の規律はサイマティクスのモード選択と同じ 3 段構え —
        **ヒステリシス**（いまの区画から 0.35 区画ぶん外へ出るまで候補を変えない）+
        **確認時間**（既定 1.0s・開発つまみ `Hue confirm`）+
        **最短保持**（既定 6.0s・開発つまみ `Hue hold`）。
        さらに**落ち着き判定**を足した — 符号つきのずれ（`raw − timbre` を 0.6s で均したもの）が
        毎秒 0.05 を超えているあいだは確認を数えない。これがないと、音色が区画を
        横断していく途中の区画で**寄り道の色**が数秒出てしまう。
        適用は**ティック境界**（光の出し入れと同じ規律）。無音では最後の H を保ち、
        確認だけ捨てるので再開はそのときの音色から数え直しになる。
        `uHue` は Audio では音色が選んだ状態、**Manual では従来どおり `Global hue H` つまみ**。
        実測: 合成音色 3 種が別々の安定状態へ落ち着く（低域優勢 tone 0.046 → 状態 0 / H 0.62、
        中域 tone 0.465 → 状態 3 / H 0.745、高域優勢 tone 0.943 → 状態 7 / H 0.245。
        いずれも最後の 10 秒で状態が 1 つだけ）/ 低域 → 高域の切り替えは
        **切替 1 回・中間の H は 1 フレームも出ない**（H の値は 0.62 と 0.245 の 2 つだけ）で、
        カットの 5.6 秒後にティック境界で瞬時に跳ぶ / **ヒステリシスの実測** — 区画境界
        （tone 0.5）を ±0.02・±0.03 でまたぐ 3 秒周期の揺らぎ 60 秒で**切替 0 回**、
        対照の ±0.11 では切り替わる / reference.wav 22 秒は**切替 2 回**
        （11.5s 状態 0→4・17.6s 状態 4→1。セクション相当）/ band-demo.wav は
        キック区間（0〜6s・treble 0.005〜0.014・tone 0.27〜0.32）が**状態 0**、
        ハット区間（6〜13s・treble 0.68〜0.76・tone 0.70〜0.94）が 11.1s に**状態 7** へ移る
        （確認 + 最短保持 + 落ち着き判定のぶん、区間頭から約 5 秒遅れて切り替わる）/
        すべての切替がティック境界（ティック内の切り替わり 0 回）で、
        `uHue` は常に 8 状態のいずれか（補間値は 1 フレームもない）
        共通の検証: 決定論（同じ台本 3 回で状態列・切替回数・ティックが完全一致）/
        **Manual の静止画スタディは全 7 Version で画素ハッシュ不変**（変更前後を同じ画面で実測して一致。
        Manual の `uHue` は 7 Version とも 0.62）/ 無音は最後の H を保持（10 秒間 状態 7 のまま・
        確認は 0 にリセット）/ 画角 3 種すべて Draw Call 1（三角 22）・最大 11 層 /
        1 フレーム p50 0.0〜0.1ms・p90 0.1〜0.2ms / コンソールエラー 0 /
        既存表現は無影響（変更は `opticsAudioDrive.ts` と `LightElementLab2.ts` だけで、
        この 2 つを読むのは Lab 2 のみ）
  - [x] Light Element Lab 2 — **実楽曲での通し確認と、既定値の焼き込み（PRD D17）**。
        ユーザー自身の楽曲 2 曲（`InALifelongSleepFix` / `FunzoueFix`）+ 参考 mp3 3 本 +
        `reference.wav` / `band-demo.wav` を **実再生**して通しで計測し、既定値を決めた。
        （楽曲は `public/audio/tmp-*` へ一時コピーして読ませ、**コミットしていない**。
        `.git/info/exclude` にも入れてある）
        **判明した構造的な事実** — 静止画スタディ（黒 0.707・平均輝度 10.9）は
        「全要素が最大で同時に、連続で出ている 1 枚」であって、
        **ストロボが毎ティック各グループの層を半分しか見せない Audio の 1 フレーム**とは
        別物である。実測の内訳は 静止画 全部入り 0.707 → 場だけ（コア・扇・断片を落とす）
        0.781 → 場を level 0.55 に落とす 0.853 → さらにストロボで 0.94。
        つまり**音量を上げるだけでは黒 60〜85% には届かない**。
        そこで持続の場（膜・カーテン・骨格）だけ **1 を超えて注げるように**した
        （`fieldLevel` の上限 0〜3）。**天井（膜 0.11 など）は 1 つも変えていない**ので、
        増えるのは白ではなく「天井に届く面積」である。Manual はつまみが 0〜1 なので
        この経路を通らず、**静止画スタディは 1 画素も変わらない**。
        **焼き込んだ定数（前 → 後）**:
        `sustainGamma` 1.0 相当 → **0.25**（実楽曲の正規化音量の中央値は 0.17〜0.55 しかなく、
        そのままでは場が立ち上がらない）/ `fieldGain` 1.0 → **2.8**（ストロボぶんの面積補償。
        1.0/1.6/2.2/2.8 の実測で黒 0.93/0.90/0.88/**0.86**）/
        `coreStrengthGate` 0.38 → **0.30**（実楽曲の打撃強度の中央値は 0.24〜0.40 で、
        0.38 では静かな曲のコア出現率が 12% しかなかった）/
        `fragmentLifeBase` 1 → **2** と `fragmentLifeFromStrength` 3 → **2**
        （同時生存断片の中央値が 0 枚 ＝ 半分の時間は周縁が空だった。上限 4 ティックは維持）/
        `hueHoldSeconds` 6 → **9**（切替をセクション相当へ）。
        `fanStrengthGate` は **0.75 のまま**（実楽曲の表示時間割合が既に 0.19〜4.4% で小さい）。
        **通し計測（焼き込み後・9:16 を 720×1280 固定で読み出し）**:
        黒の中央値は InALifelongSleep **0.836** / Funzoue **0.838** / arcade 0.822 /
        bate 0.829 / hands 0.826 / reference 0.836 — **実楽曲 5 本すべて目標 60〜85% の内側**
        （band-demo だけ 0.884。合成の試験音源で持続音が薄いため）。
        調整前は 0.94〜0.96 だったので、**同じ物差しで 0.10 以上黒が減った**。
        乳白化なし（平均輝度 2.5〜4.9 で、静止画スタディの 10.9 の半分以下。
        中間帯の面積も 0.10〜0.14 で静止画の 0.196 より小さい）。
        **白はコアの箱に限局**（縦 0.44〜0.56・最大面積 0.16〜0.48%。横の広がりは
        アームの横フレアぶん）。コアは毎分 8〜165 発（打撃の 22〜74%）で拍の密度に乗り、
        扇は実楽曲で毎分 1.3〜36 回・表示時間割合 0.19〜4.4%（＝たまの強打の演出）。
        H の切替は 35〜45 秒あたり 1〜2 回（うち 1 回は曲頭の初期確定）。
        **破綻は 1 曲もない**（真っ白・真っ黒張り付き・点滅しっぱなしのいずれも無し。
        黒の 90 パーセンタイルも 0.86〜0.92 に収まる）。
        **正直に残る差**: ① `reference.wav` / `band-demo.wav` は合成音源で打撃強度が
        1.0 に飽和するため、コア出現率 92%・扇の表示割合 12〜13% と実楽曲より過剰に出る
        （実楽曲へ寄せた結果で、試験音源には過適合させていない）。
        ② Funzoue は静かで打撃が少なく（毎分 36 発）、コアは毎分 8 発・扇は毎分 1.3 回と疎い。
        曲の性格どおりだが、局所適応では吸収しきれていない差である
        共通の検証: **Manual の静止画スタディは全 7 Version で画素ハッシュ不変**
        （HEAD と焼き込み後を同じ 720×1280 で実測して一致）/ 決定論（同じ台本 3 回で
        ティック・コア・アーム・扇・H・断片が完全一致）/ 画角 3 種すべて Draw Call 1
        （三角 22・最大 11 層）/ 1 フレーム p50 0.0〜0.1ms・p90 0.1〜0.2ms /
        コンソールエラー 0 / 既存表現は無影響（Element Lab V1 Core / Composite・
        Reactive Lab Composite・Spatial Study・Cymatics V1・Modular V1）
  - [x] Light Element Lab 2 — **撤去した開発つまみを全部戻す（方針修正 2026-07-30 夜）**。
        メンター MTG の設計指導により、**「ツールを 1 つの見え方に最適化しない。
        接続は調整可能に保ち、好みの見え方はプリセットとして実現する」**へ方針が変わった。
        **変数の数がそのまま「出せる絵の幅」になる**ので、探索が終わって値が確定したものでも
        つまみ自体は残す（確定したのは**既定値**であって、動かせないという意味ではない）。
        いったん撤去した 9 本 — `Strobe rate` / `Arms (Manual)` / `Haze layer (dev A/B)` /
        `Red` `Green` `Blue` / `Offset direction` / `Global tint (H)` / `Depth probe` —
        を `getExpressionParams` へ戻した。**既定値は実楽曲での通し計測で決めた値のまま**で、
        受け口（`setExpressionParam`）も変えていないので**見え方は 1 画素も変わらない**
  - [x] Light Element Lab 2 — **蓄積 1 本: 断片の痕跡場（physarum 型）**。
        写像だけでは「同じ入力 → 常に同じ絵」で終わるので、**場への書き込みと読み出し**を
        1 本入れた。**断片が消えるとき、その位置に痕跡を場へ置く**（量は打撃の強さと
        断片の大きさに比例）。場は **32 × 18 の決定論グリッド**（CPU 側・正規化座標 ±1.25）で、
        **半減期 18 秒**でゆっくり減衰し、1 セルの上限は 6。
        **次の断片の発生位置がこの痕跡へ引き寄せられる** — セルは場の量に**比例して**
        音のシードから引き（一様に候補を撒くと薄い場でほとんど効かなかった）、
        引き寄せの強さは `Trace amount × 0.8 × √(そのセルの濃さ / 場の最大値)`。
        **場はイベント履歴の純関数**（`Math.random()` を使わない）なので決定論は保つ。
        位置の式は `fragmentPlacement` 1 か所に集約したので、
        **場へ書き込む位置と実際に描かれる位置がずれない**。
        **`Trace amount` つまみ（0〜1・既定 0.5）で調整可能。0 で蓄積を完全に切る。**
        実測: 40 打で場は peak 5.20 / 総量 163 / 96 セルまで積もり、打撃を止めると
        **18 秒ごとにきっちり半減**（5.19 → 2.60 → 1.30 → 0.65）/
        引き寄せの効きは単調 — 発生位置にあった痕跡の濃さ（場の最大値比）の平均が
        amount 0 / 0.3 / 0.6 / 1.0 で **0.174 / 0.201 / 0.246 / 0.304**、
        痕跡の占めるセル数は 125 → 121 → 113 → **87** と集まっていく /
        **5 分の連続打撃でも発散しない**（総量は 80 秒で 437 に達したあと 445〜457 で平衡、
        セル数も 196〜203 で頭打ち。天井に当たるのは最も濃いセルだけ）/
        **`Trace amount` 0 では蓄積前のコードと画素ハッシュが完全一致**（22 フレーム）/
        決定論（同じ台本 3 回で場の推移が完全一致）/
        **Manual の静止画スタディは全 7 Version で画素ハッシュ不変**
        （位置の式を共通化しても Manual の見え方は 1 画素も動かない）/
        ストロボ規律は不変（断片の出入りは従来どおりティック境界だけ）。
        明るさへの影響は今回入れていない（位置だけ）
  - [x] Light Element Lab 2 — **Bass / Mid / Treble → R/G/B チャンネル駆動**。
        元からある RGB チャンネル機構（1 つの光を 3 チャンネルへ分けてずらす CRT 的構造）を
        音へ繋いだ。**帯域の持続値**（時定数 1.2 秒。打撃では動かない）が
        **Bass = R / Mid = G / Treble = B** のチャンネル利得を決める。
        値は**ティックで latch** する（光と同じ規律。連続では動かさない）。
        利得の作り方は「鳴っている帯域を持ち上げる」のではなく
        **「鳴っていない帯域を落とす」**（`1 − 0.45 ×(1 − その帯域 / 最大帯域)`）。
        こうすると**利得は必ず 1 以下**に収まるので、**白の予算は 1 mm も動かない**
        （持ち上げる作りにすると、コア以外の層が白へ届いてしまう）。
        **`Channel drive` つまみ（0〜1・既定 0.5）**: 0 = 手動 RGB つまみのまま /
        1 = 完全に帯域駆動 / 中間はブレンド。**接続の強さ自体を調整できる**ので、
        「Bass = 鈍い光」のような対応を焼き込まない。
        オフセット量と非相関量の音駆動は今回やらない（次の設計判断に残す）。
        実測: 利得は bass 優勢で **(1, 0.70, 0.60)**、mid 優勢で **(0.675, 1, 0.675)**、
        treble 優勢で **(0.60, 0.70, 1)**、**帯域が均等なら (1, 1, 1) ＝ 無彩**（チルトなし）/
        実際の画素でも傾く — bass 優勢の画面平均 RGB が drive 0 / 0.5 / 1 で
        (12.8, 24.9, 17.2) → (13.5, 22.4, 14.4) → **(14.4, 19.8, 11.5)** と暖色へ、
        treble 優勢は **(8.7, 10.1, 30.2)** と寒色へ。**均等では drive 0 と 1 の画面が完全一致** /
        音色を 70 秒かけて往復させた通しで、**チャンネルの変化 1620 回はすべてティック境界**
        （ティック内の変化 0 回）/ **H の協調は壊れない** — 同じ通しで H は 6 状態を訪れ、
        切替 6 回はすべてティック境界。チルトは全層に同じ倍率で掛かるので
        「1 枚 = 1 波長状態」のままである /
        **`Channel drive` 0 では接続前のコードと画素ハッシュが完全一致**（22 フレーム）
        共通の検証: 決定論（同じ台本 3 回で完全一致）/ **Manual の静止画スタディは
        全 7 Version で画素ハッシュ不変** / 画角 3 種すべて Draw Call 1（三角 22・最大 11 層）/
        1 フレーム p50 0.0〜0.1ms・p90 0.1〜0.2ms / コンソールエラー 0
  - [x] **音から取れる特徴を「表示だけ」増やす（設計フェーズ① の道具）**。
        どの特徴を何に繋ぐかを決める前に、**実曲を流して値と聴感の対応を観察する**ための道具。
        **表現・描画・`TUNING` へは 1 本も繋いでいない**（接続は次のフェーズ）。
        **追加した特徴**（既存 FFT・波形から派生する安いものだけ。BPM・chroma・MFCC は入れない）:
        `rolloff`（エネルギー 85% の上限周波数・Hz 併記）/ `spread`（重心まわりの標準偏差・Hz 併記）/
        `tilt`（500Hz 以下と 2kHz 以上の比。**0.5 が中立**）/ `crest`（ピーク ÷ RMS のパンチ感・生比併記）/
        `onsetRate`（直近 1 秒のイベント数・生の個数併記）/ `envelopeFast`（50ms）・`envelopeSlow`（2s）と
        その差 `envelopeDelta`（**0.5 が中立**で上が盛り上がり・下が引き）/
        **7 帯域の対数分割エネルギー** — Sub(20–60) / Bass(60–250) / Low-mid(250–500) / Mid(500–2k) /
        High-mid(2–4k) / Presence(4–6k) / Air(6–16k)。
        **公開形式**: `AudioParameters` を**拡張しない**。`getSpectrum()` と同じ流儀で
        `AudioEngine.getFeatures()` という**別口**にした（既存の写像はこの型を知らないので、
        **既存 10 特徴に値が混ざる余地がない**）。値は既存に合わせて全部 0〜1 で、
        符号のある量は 0.5 中立に写し、生の値は別フィールドで併記する。
        **解析は `FileAudioEngine` の既存解析ブロックと同じ 1 フレーム 1 回**（`AudioFeatureAnalyzer`）。
        FFT 2 周 + 波形 1 周だけで、表示が 2 か所あっても解析は 1 つを共有する。
        **表示 2 か所**: ① Audio Inspector の折りたたみ `Observation features (dev)`
        ② **解析専用ページ `?audio=1`** — 表現を描かず（`setGeneratorsVisible(false)`）、
        7 帯域と全特徴を大きく並べる。どちらも同じ `AudioFeatureView` を使う。
        DEV ガードは `?audio=1` のページだけに掛けた（`?tune=1` と同じ扱い）。
        Inspector 側の折りたたみは**既存 Inspector の流儀に合わせて掛けていない**
        — この Inspector 自体が「常にあるが畳んである開発用セクション」で、
        中身だけ DEV 限定にすると流儀が割れるため。
        実測: 実曲（InALifelongSleep 30 秒）で全特徴が意味のある範囲に散る —
        rolloff 1.2k〜4.3kHz / spread 0.07〜0.36 / tilt 0.011〜0.764（中央 0.392 = やや低域寄り）/
        crest ×2.25〜×3.4 / onset 0〜7 回毎秒 / 7 帯域は sub 0.58 → air 0.02 と素直に落ちる /
        **負荷**: `getParameters()` 1 回あたり **平均 0.340ms → 0.431ms（+0.091ms）**、
        中央値 0.3 → 0.4ms。60fps の 16.7ms に対して **+0.55%** で、フレーム時間は
        p50 0.1ms・p90 0.2ms のまま（計測は同じ曲・同じ区間 862 フレーム）/
        `?audio=1` では表現の **Draw Call が 0**（三角 0）で、描いていないことを実測で確認
        **既存出力が 1 バイトも変わらないこと**: 差分は「`rising && !this.wasRising` を
        名前付き定数に出しただけ」で、10 特徴の代入は 1 行も触れていない。実測でも
        **Audio モードの決定論トレース**（ティック・コア・アーム・扇・H・断片・痕跡・チャンネルの
        31,234 文字）が**変更前後で完全一致**（ハッシュ `a475dad6`）/
        **Manual の静止画スタディは全 7 Version で画素ハッシュ不変** /
        既存表現も不変（Element Lab V1 Core / Composite・Reactive Lab Composite・
        Spatial Study・Cymatics V1・Modular V1）/ 画角 3 種すべて Draw Call 1（三角 22）/
        コンソールエラー 0 / **本番ビルドに `?audio=1` は入らない**
        （`AudioLabPage` / `audio-lab` / `観察専用` の出現数はいずれも 0。`?tune=1` と同じ扱い）
  - [x] **音 × 表現パラメーターの結線 — 契約と解決器（ステップ 1）**。
        `src/engine/binding/` を新設。**音の側は `AudioSource` を差し出すだけ、
        表現の側は `ParamDecl` を宣言するだけ**で、両者を結ぶのは `Binding` 1 本と
        解決式だけ、という契約にした。
        解決式は仕様どおり `value = clamp(base + depth × signal × (max − min), min, max)`。
        **基準値は結線後も生きている**（揺れの中心になる）。
        変換は `gate`（閾値未満 0）と `envelope`（立ち上がり → 減衰。語プリセット
        Sharp / Default / Soft を定数で持ち、数値の微調整もできる）。
        **種類違いには既定変換を自動挿入** — 発火 → 連続量は envelope(Default)、
        連続量 → 引き金は gate。時間は呼び出し側から渡すので `Date.now()` も
        `Math.random()` も使わない（決定論）。
        実測（ダミーソースで解決式）: 未結線 = 基準値 0.25 / 固定 0 で 0.25（動かない）/
        固定 1・depth +1 で 1.0 / **depth −1 で 0.0（逆方向）** / depth 0 で 0.25 /
        上下とも clamp が効く / **範囲 −2〜2・基準 0・信号 0.5・depth 0.5 で 1.0**
        （`max − min` が効いている）/ gate 0.6 は信号 0.5 を止め、gate 0.4 は通す /
        envelope 3 プリセットが明確に別物（1 発の推移: Sharp 0.98 → 0.008、
        Default 0.75 → 0.18、Soft はゆっくり立ち上がって長く残る）/
        自動挿入の対応が仕様どおり / **同じ入力列 3 回で出力列が完全一致**
  - [x] **内部ソースの棚（ステップ 2）**。**新しい解析はしない** — engine が
        1 フレーム 1 回計算済みのものを 0〜1 に包むだけ。
        `volume-fast`（level・観察用特徴の 50ms 包絡）/ `volume-slow`（level・2s 包絡）/
        `onset-strength`（event・既存 onset を保持して減衰）/
        `band-bass` `band-mid` `band-treble`（event・既存の帯域値の立ち上がりで 1 に張り付き、
        時定数 0.22 秒で落ちるパルス）。時定数と閾値は `binding/sources.ts` の `SHELF` に集約。
        **二重計算しない仕掛け**: engine が返すパラメーター object の同一性で
        「そのフレームで既に進んだか」を見るので、表現と `?audio=1` の両方が呼んでも
        進むのは 1 回だけ。棚は engine ごとに 1 つ（`WeakMap`）。
        `?audio=1` に **Binding sources** として 6 本すべてを表示追加。
        実測（実曲 20 秒）: volume-fast p10〜p90 = 0.08〜0.53、volume-slow = 0.08〜0.33
        （遅いほうが素直に狭い）/ onset-strength は毎分 93 回・p90 0.79 /
        band-bass 54 回・band-mid 102 回・band-treble 48 回（いずれも 1.0 に到達）/
        **無音では 6 本すべて厳密に 0**（＝結線しても黒が保たれる土台）/
        同一フレームで 3 回呼んでも値が進まない（二重更新を弾く）
  - [x] **音 × 表現パラメーターの結線 — Lab 2 の宣言化（ステップ 3）**。
        中断点の論点はユーザー判断で**案 1 + 案 2 の併用**に決着した。
        **案 1（レイヤーの責務）**: 接続層は**値だけ**を運び、**時間の規律は表現側に残す**。
        Binding が解決した値を、アダプタが従来どおり光学クロックへ載せる
        （ティック量子化・1 ティック保持・on/off の交互・H の 8 状態機械）。
        **案 2（既定の再現）**: アダプタ自身の加工済み信号を棚に足し、
        既定の束をそこへ繋いで**現行挙動をビット単位で再現**する。
        **宣言した ParamDecl（4 本）**: `fieldDrive`（continuous・場の駆動）/
        `coreStrike`（trigger・コアの打撃）/ `fanStrike`（trigger・扇の打撃）/
        `hueTimbre`（continuous・H を選ぶ音色）。
        `fragmentEnergy` は Audio で未使用（断片は spawn 機構）、`seed` は 0〜1 の
        変調対象ではないので**宣言しない**。
        **アダプタ由来のソース（3 本）**: `optics-field`（level・`pow(volume, gamma)`）/
        `optics-strike`（event・**発火フレームだけ値が入る衝撃**。減衰させると
        「1 ティックだけ光る」現行挙動が再現できない）/ `optics-timbre`（level・平滑前の音色）。
        棚は合計 **9 本**（アダプタ 3 + 内部棚 6）。
        **繋ぎ替えてもストロボ規律が壊れない仕掛け**: 駆動が閾値を越えている間ずっと
        再アームされても、**位相は「駆動が続いているあいだ」だけ進む**ので、
        点きっぱなしにならずティックの上で on / off を繰り返す。
        打撃 1 発では位相が 0 のままなので、従来どおり 1 ティックだけ光る。
        実測: **既定の束で決定論トレースが完全一致**（ハッシュ `a475dad6`・31,234 文字。
        これが案 2 の受け入れ条件）/ `coreStrike` を繋ぎ替えると
        既定 4.08%（15 発火）→ onset-strength 15.72%（45）→ **volume-slow 30.07%（120）**
        → volume-fast 39.98%（161）と**脈動 ⇄ 常時明滅**が出る
        （連続ソースでも点灯率が 50% を超えないのは、ストロボの on/off が効いているため）/
        depth 0 と未結線はどちらも 0.00%（＝基準値だけ）/ 決定論一致 /
        **Manual の静止画スタディは全 7 Version で画素ハッシュ不変**
  - [x] **音 × 表現パラメーターの結線 — 最小の結線 UI（ステップ 4）**。
        **触る場所と繋ぐ場所を分けない** — 基準値スライダーの**そこに**
        ソース選択（なし + 棚 9 本）・深さ（−1〜1）・変換（Auto / None / Gate /
        Envelope Sharp・Default・Soft）を直付けした。下部パネル方式にはしない。
        見た目と機構は Effect 層の Audio Mapping（`effect-audio-mapping`）を流用。
        **結線中も基準値スライダーは操作でき、動作中の実効値がスライダーの溝に重なって見える**
        （基準の周りで揺れているのが視覚的にわかる）。
        `ExpressionParam` に `type: 'binding'` を足し、宣言 1 本 = UI 1 行で出る。
        変換の既定閾値は実測で見直した — 2 秒包絡の 90 パーセンタイルが 0.33 しかなく
        `gate 0.5` では**繋いでも何も起きない**ので、既定を **0.25** に下げた。
        **完了条件の実測（すべて UI と同じ `setExpressionParam` 経由・実曲 15 秒ずつ）**:
        ① `Core strike` の繋ぎ替えで見え方が変わる — 既定 optics-strike **点灯 3.69% / 毎分 52 回（脈動）**
        → onset-strength 15.2% / 200 回 → volume-slow(base 0.35) **点灯 49.86% / 毎分 720 回（常時明滅）**。
        **49.86% は 1 ティック on / 1 ティック off のちょうど半分**で、
        連続ソースへ繋いでも**ストロボ規律の内側に留まる**ことが数字で出ている /
        ② 変換の切替で挙動が変わる — onset-strength を envelope-soft 22.3%（312 回）
        ⇄ envelope-sharp 17.33%（240 回）/ gate は閾値未満を落とす /
        ③ depth 0 と未結線はどちらも点灯 0.00%、depth 負は基準値から下げる方向に効く /
        無音では棚の 9 本すべてが 0 なので黒が保たれる（黒率 0.85 前後で不変）
        共通の検証: **既定の束で決定論トレースが完全一致**（`a475dad6`）/ 決定論一致 /
        **Manual 全 7 Version 画素ハッシュ不変** / **他表現 6 種すべて不変**
        （Element Lab V1 Core / Composite・Reactive Lab Composite・Spatial Study・
        Cymatics V1・Modular V1）/ 画角 3 種すべて Draw Call 1（三角 22・最大 11 層）/
        1 フレーム p50 0.1ms・p90 0.1〜0.2ms / コンソールエラー 0。
        **今回やらないと決めたもの**: 結線のプリセット保存（リロードで既定の束に戻る）/
        聴取語彙の公開棚 / 他表現への展開
        ユーザー指定の中断点に該当したため、実装せずに停止して報告した。
        **論点**: 契約の「基準値 ± 変調」は連続量の手触りだが、Lab 2 の Audio は
        「無 → 打撃の瞬間 → 無」という**イベント機構**（ティック保持・寿命・形状・シード）で
        出来ている。6 入力のうち **`corePulse` / `fanGate` / `huePhase` を線形式で駆動すると
        恒久方針に反する**（減衰カーブ = フェード禁止 / H の連続ドリフト禁止）。
        また `fragmentEnergy` と `seed` は Audio では未使用、場は 3 つの時定数
        （0.9 / 1.4 / 2.6 秒）+ ガンマ + 利得 2.8 で作られており、
        棚の 2 本の level ソースでは**既定接続で現行挙動を再現できない**。
        判断待ちの選択肢は報告に記載
  - [x] **結線のユーザー改修 — 発光を 1 本に、対象を広げ、棚を代表 7 本へ**。
        物を触ってのフィードバックによる 3 点の改修。
        **① 発光の結線を「発光 All」1 行に統合** — パーツ別（場 / コア / 扇 / 音色）に
        分ける必要はないという指摘を受け、UI 行は 1 本にした。選んだ音が
        **リグ全体の発光**を駆動する（下流の時間規律 — 場の時定数・打撃検出・扇の閾値・
        ストロボ・H の 8 状態機械 — はすべて従来どおり表現側）。
        `None` で**内部の既定ドライブへ戻る**＝現行挙動の再現。
        **② 紐付け対象の拡大（既存スライダーに直付け）** — `Global hue H` / `Intensity` /
        `Channel decorrelation` / `Depth`（奥行き量 = `depthProbe`。見かけの位置と大きさを
        保ったまま全層を移すつまみで、Lab 2 の唯一の奥行き操作）。
        **新しい行は作らず**、スライダーの隣にセレクトを 1 つ足すだけ。既定は `None` で、
        ソースを選んだときだけ深さ（−1〜1）が現れる。
        `Global hue H` だけは特別で、**H の 8 状態機械の入力（音色）を差し替える** —
        つまり色は音で「切り替わる」のであって、連続にスライドはしない。
        **③ ソース棚を代表 7 本へ再編** — 「細かすぎてどの音に反応させているのか分からない」
        という指摘を受け、`Volume (音量)` / `Bass (低域)` / `Mid (中域)` / `Treble (高域)` /
        `Onset (打撃)` / `Brightness (音色の明るさ)` / `Rise (盛り上がり)` の 7 本に絞った。
        level は 1 本の時定数（0.25 秒）で素直に追い、速い / 遅いの差は変換と下流で吸収する。
        旧棚（volume-fast/slow・帯域イベントパルス・optics-*）は**セレクトから隠し**、
        内部の既定ドライブとしてだけ温存する。`?audio=1` の表示も同じ 7 本に揃えた
        （観察と結線で同じ語彙）。
        実測: **既定（None）で決定論トレースが完全一致**（`a99216a6`）。ソースを外して
        戻したときも一致する / **発光 All の繋ぎ替え** — 既定 コア点灯 4.08%（15 発火）→
        `Volume` 60.15%（160）→ `Bass` 31.06%（83）→ `Onset` 15.72%（45）、
        場の基礎輝度（skeleton 中央値）も 0.881 → 0.607 / 0.439 / 0.37 と音量追従へ変わる /
        depth −1 では発光が 0（逆方向）/ 点灯フレームの割合が 40〜60% に収まるのは、
        24fps のティックが 60fps の 2〜3 フレームに当たるためで、**1 ティック on /
        1 ティック off のストロボ規律は保たれている** /
        **新 4 対象**: `Intensity ← Volume` で 1.6 固定 → 2.82〜4.00 に揺れる /
        `Channel decorrelation ← Onset` で 0.25 固定 → 0.25〜1.00 /
        `Depth ← Rise` で膜の z が −13.6 固定 → −10.9〜−8.24 に動く /
        `Global hue H ← Treble` で H の状態列が {0.62, 0.245} → {0.62, 0.87} に変わる
        （**離散のまま切り替わる**）/ 4 つとも `None` で従来どおり（値が動かない）/
        **棚 7 本が実曲で意味と一致** — Bass p50 0.838（低域の厚い曲で高い）・
        Treble p50 0.004（この曲は高域がほぼ無い。観察機能の 7 帯域と一致）・
        Mid 0.538・Volume 0.134・Onset は 0.05〜1.0 のイベント・
        Brightness 0.445・Rise は 0.5 中立の周り（0.45〜0.58）
        共通の検証: 決定論一致 / **Manual 全 7 Version 画素ハッシュ不変** /
        **他表現 6 種すべて不変** / 画角 3 種すべて Draw Call 1（三角 22・最大 11 層）/
        1 フレーム p50 0.0〜0.1ms・p90 0.1〜0.2ms / コンソールエラー 0
  - [ ] **Light Unified — 3 表現を連続軸で行き来する統合表現**（P1 完了・P2〜P5 継続中）。
        Spatial Study / Reactive Lab / Element Lab 2 を**混ぜず・合成せず**、
        新規に書いたレンダラーで「3 つの見え方を通る 1 つの連続空間」を作る。
        既存 3 表現は比較用に**無改変で温存**（差分ゼロを確認済み）。
        再利用するのは共有部品だけ — 帯域イベント検出・prismAtlas・結線・痕跡場
        （`OpticsAudioDrive` をそのまま生成核として使う）。
        **原則**: すべての軸は「コードパスの切替」ではなく
        **描画数式の中の連続な混合係数**。`if (axis > 0.5)` で見え方を切り替えない。
        だからスライダーの途中に、3 つのどれでもない見え方が現れる。
        **要素の語彙（和集合・6 種）**: 核 core / 光条 beam（針・Ray・アーム・骨格軸）/
        膜 membrane（Sheet・カーテン・マクロ膜）/ 靄 haze / 破片 fragment（4 形状族）/ 扇 fan。
        **P1（この段階）**: カタログ登録（`light-unified-v1`・ファミリー `light-unified`）+
        統合レンダラーの骨格。20 本の軸を宣言して既定値（3 プリセットの中間）で固定し、
        音の生成核を接続した。
        実測: **無音で層 0 枚・Draw Call 0・黒 100%** / 合成音で**6 種すべてが出る**
        （haze / membrane / beam / fan / core / fragment）/ **Draw Call 1**（三角 30・最大 15 層）/
        黒の中央値 0.67（3 画角で 0.665〜0.691）/ 白は最大 0.05% で**核だけ** /
        決定論一致 / コンソールエラー 0 / 既存表現は差分ゼロ
        **P2（時間軸 + 光学軸）**: `Strobe` / `Tick rate` / `Attack` / `Decay` / `Blur` を
        描画数式の係数として入れた（`unifiedTime.ts` 新設）。
        **Strobe は 2 つの効果を 1 本で同時に補間する** — ①ティックへの**ラッチ量**
        （`EmissionShape.read`: 連続値とティック頭の値の混合）と、②**off ティックの
        消灯深さ**（`strobePhaseGain`: 層ごとに位相をずらし、off 側を `strobe` の
        深さだけ暗くする）。0 で連続エンベロープ・1 でフルティック明滅・
        **0.5 に「半分明滅」が実在する**。`Attack` / `Decay` は素の held 値を追う
        エンベロープの時定数（0.004〜0.34 秒 / 0.028〜1.9 秒）で、**破片は 1 枚ずつが
        自分のエンベロープを持ち、死んだあとも尾を引いているあいだ残る**
        （尾が枠を占めて新しい破片を締め出さないよう、生きているものを先に並べる）。
        `Blur` は縁の `smoothstep` 幅とハロ（散乱）を 1 本で束ねる（種別ごとの上限つき。
        面の大きい膜と靄には載せない）。共有部品 `OpticsAudioDrive` には
        **読み取り専用の `sustained()` を足しただけ**（59 行の純増・削除 0 行）なので、
        Lab 2 ほか既存表現は 1 画素も変わらない。
        実測（合成音・決定論・1:1・100 フレーム）:
        **Strobe 0→1 の 11 段階が滑らかで単調** — フレーム間の変動係数 cv
        0.154 → 0.162 → 0.172 → 0.190 → 0.215 → **0.250（0.5）** → 0.294 → 0.349 →
        0.416 → 0.501 → 0.587、谷の輝度 7.92 → 1.64、山 14.41 → 11.05（跳びなし）/
        `Attack` 0→1 で cv 0.114 → 0.273・平均 12.79 → 9.84（単調）/
        `Decay` は谷 7.00 → 8.99・cv 0.200 → 0.131（単調。尾が伸びるほど谷が浅くなる）/
        `Tick rate` 12→48fps で cv 0.202 → 0.245 /
        `Blur` 0→1 で点灯率 0.233 → 0.390・平均 6.62 → 15.15・白 0.002% → 0.45%
        （**白はどの値でも核だけ**）。
        **無音 = 黒は極端な軸設定でも厳密**（`strobe 0 / blur 1 / decay 1 / intensity 1`
        と `strobe 1 / blur 0 / attack 1` の両方で点灯 0.0%・Draw Call 0）/
        画角 3 種すべて Draw Call 1・層 15 枚 / **決定論はページ再読込をまたいで一致**
        （画素ハッシュ `3b56908c`）/ コンソールエラー 0
        **P3（配置軸 + 空間軸）**: `Spread X/Y` / `Anchor pull` / `Depth spread` / `Tilt` を
        仕上げた。**`Anchor pull` の分岐を消した** — P1 は「|nx| と |ny| のどちらが小さいか」
        で寄せる軸を選んでいたので、`Spread` を動かすと大小が入れ替わる瞬間に位置が跳んだ。
        大きさの比で連続に重みを配る式（`weight = |ny| / (|nx| + |ny|)`）へ置き換え、
        境目でも跳ばないようにした。**核と扇も配置軸に従う**ようにした（P1 は中心固定）。
        空間に散る側では白熱もあちこちで起きるべきで、居場所は打撃のシードから決まるので
        決定論は保たれる。軸を 0 にすれば従来どおり真ん中へ戻る。
        実測（合成音・決定論・80〜90 フレーム・輝度で重みづけした空間の広がり sx / sy）:
        `Spread X` 0→1 の 11 段階で sx 0.138 → 0.207（単調・跳びなし）・
        点灯率 0.260 → 0.328・sy はほぼ不変 /
        `Spread Y` 0→1 で sy 0.124 → 0.172・sx はほぼ不変（**軸が独立している**）/
        `Anchor pull` 0→1（Spread 0.9 の上で）で sx 0.206 → 0.170・sy 0.171 → 0.143・
        点灯率 0.354 → 0.300（十字へ収束）/
        `Depth spread` 0→1 で白 0.128% → 0.197%（近い層が大きく明るくなる）/
        `Tilt` 0→1 で平均輝度 9.14 → 8.84（見込み角で面積が減る）・sx 0.1569 → 0.1595。
        **無音 = 黒は配置軸を極端にしても厳密**（`spread 1 / tilt 1 / depth 1` と
        `spread 0 / anchorPull 1` の両方で点灯 0.0%・Draw Call 0）/
        画角 3 種すべて Draw Call 1・層 15 枚 / 同一セッション内の反復で画素ハッシュ一致
        （`87a9faef`）/ コンソールエラー 0
        **P4（色軸 + 構成軸 + 動き）**: `Hue coherence` / `Hue stickiness` / `Dispersion` /
        **`Channel balance`（新規）** / `Membrane–Beam` / `Fragments` / `Haze floor` /
        `Skeleton` / `Motion` / `Trace`。
        **`Channel balance` — R/G/B の 3 本を 1 本の非循環な経路へ畳んだ**（`channelBalance.ts`）。
        0 = R 優勢 / 0.5 = G 優勢 / 1 = B 優勢で、3 つの山（バンプ）の裾を重ねて
        連続に配るので、0.25 のような**途中にも固有の色**がある。
        **利得の最大は常にちょうど 1**（動くのは他 2 本をどれだけ落とすかだけ）なので、
        この軸は明るさを足さず**白の予算は動かない**。谷の下限は 0.72 —
        0.55 まで深くしたら既定値で**核が白へ届かなくなった**（白 0%）ので戻した。
        **Lab 2 の R / G / B スライダー 3 本も撤去して同じ 1 本に置き換えた**（ユーザーの明示決定）。
        既存の `Channel drive`（帯域 → R/G/B）とは**乗算で共存**する
        （`channelTilt()` が Channel balance を基準値、帯域チルトを掛け算として合成）。
        既定 0.5 の利得は (0.82, 1, 0.82) で、旧既定の (1, 1, 1) から**わずかに緑寄り**に変わる。
        **`Hue stickiness`** は「同じ道の上を混ぜる」ことで実装した — 連続版の H
        （`hueOfPhase`。8 状態と同じ式を丸めずに通す・純増）と離散の状態 H を軸で混ぜ、
        同時に確認時間（0.2〜1.8 秒）と最短保持（1〜10 秒）も伸ばす。
        **枚数の段を消した** — 膜と破片の枚数は整数なので軸を動かすと必ず段になる。
        最後の 1 枚だけを端数の明るさで出す（`countFade`）ことにして、画素の上では連続に増える。
        実測（合成音・決定論）:
        `Channel balance` 0→1 の 11 段階で R 3.10 → 2.14（単調減）・B 5.18 → 7.33（単調増）・
        **G は 0.5 で山**（6.33 → 7.44 → 6.14）＝ **R → G → B の非循環な経路**が数字で出ている。
        色相角も 0.426 → 0.526 と滑らかに動き、Draw Call は 1・層 15 枚のまま /
        `Hue coherence` 0→1 で彩度 0.260 → 0.576（要素ごとの色 ⇄ 全体 1 色）/
        `Hue stickiness` 0→1 で **400 フレーム中の相異なる H が 297 → 180 → 164 → 120 → 1**
        （滑らかな追従 ⇄ 離散 1 色に固定）・0 では 1 フレームの最大変化 0.014（連続）・
        1 では 0（保持）/ `Dispersion` 0→1 で点灯率 0.256 → 0.288・彩度 0.527 → 0.580 /
        `Membrane–Beam` 0→1 で点灯率 0.457 → 0.145・平均輝度 17.9 → 4.87（膜 ⇄ 光条）/
        `Fragments` 0→1 で層 11 → 19（`countFade` で段なし）/
        `Haze floor` 0→1 で平均輝度 6.50 → 9.78 / `Skeleton` 0→1 で 7.80 → 8.09 /
        `Motion` 0→1 で重心が 0.506 → 0.471 へ漂う /
        `Trace` は痕跡場の内部量で効きが見える（引き寄せ 0 → 581 回・
        痕跡セル 237 → 151 ＝ 破片が集まる）。破片だけを残した条件では
        空間の広がり sy 0.1414 → 0.1355。
        **無音 = 黒は色軸・構成軸を極端にしても厳密**（点灯 0.0%・Draw Call 0）/
        画角 3 種すべて Draw Call 1・層 15 枚 / **決定論はページ再読込をまたいで一致**
        （画素ハッシュ `8936eca1`）/ コンソールエラー 0。
        **既知**: 同一セッション内で表現を往復すると画素が完全一致しない。
        原因は engine ごとに 1 つの解析棚（`binding/sources.ts`）が表現の切替をまたいで
        値を持ち越すことで、統合表現の側の状態ではない（再読込では厳密に一致する）
        **P5（プリセット + 結線 + 連続歩行の検証）**:
        **結線を統合表現へ引いた** — `発光 All`（選んだ音がリグ全体の発光を駆動。
        `None` で内部の既定ドライブへ戻る）+ スライダー直付けの 5 本
        （`Intensity` / `Channel decorrelation`（Dispersion）/ `Depth`（Depth spread）/
        **`Channel balance`** / `Global hue H`（Hue coherence の行に H の音色を差し替え））。
        行は増やさず、既存の `bind:<軸>:source|depth` の仕組みをそのまま使う。
        軸の実効値は「基準値 ± 変調」なので、繋いだ瞬間に見え方が飛ばない。
        20 本の軸と結線は `?audio=1` の開発パネルに出す（`SHOW_EXPRESSION_PARAMS` が
        false なので本番 UI には出ない）。
        **3 プリセットの座標を見え方で調整した** — spatial は縁が硬かったので Blur 0.8 → 0.92、
        reactive は明るすぎたので Intensity を 0.34 で宣言、3 つとも Trace と Intensity を
        座標に含めた。
        実測: **プリセットが 3 つとも別物として立っている** —
        spatial 点灯 0.567 / 変動 0.109 / 広がり (0.215, 0.174) / 彩度 0.379、
        reactive 点灯 0.577 / 変動 0.122 / 平均 21.3（膜が優勢で最も明るい）、
        optics 点灯 0.181 / **変動 0.542（コマ送り）** / 広がり (0.152, 0.089)（中心に集まる）/
        彩度 0.574、既定（中間）は点灯 0.309 / 変動 0.193 で**ちょうど 3 つの間に入る** /
        **発光 All の繋ぎ替え**: 既定（None）平均 9.23 → `Volume` 4.52 → `Onset` 8.84 →
        `Bass` を深さ −1 で 0.098（ほぼ黒＝逆向きに効く）/
        **5 対象がすべて動く**: `Intensity ← Volume` で平均 8.85 → 14.19 /
        `Channel decorrelation ← Onset` で彩度 0.490 → 0.495・B 4.15 → 5.01 /
        `Depth ← Rise` / `Global hue H ← Treble` で色相 0.303 → 0.343（離散のまま切り替わる）/
        **`Channel balance` の色軌跡は音の種類で変わる**（深さ +1・300 フレームの実効値の幅）—
        未結線 0.500 固定 / `Treble` 0.737〜0.837（幅 0.099・最も狭い）/
        `Volume` 0.876〜1.000（0.124）/ `Bass` 0.809〜0.951（0.142）/
        `Mid` 0.746〜0.950（0.204・連続の掃引としては最も広い）/
        **`Onset` 0.500〜1.000（0.500・打撃で経路を端まで振る）**。
        彩度も 0.452（未結線）→ 0.279（Onset）と、経路を広く振るほど平均の色が中和される。
        深さを負にすれば同じ経路を G → R 側へ振れる /
        `Intensity` 0→1 の歩行で点灯 0.081 → 0.329・平均 2.59 → 12.68・白 0% → 0.16%（単調）。
        **白は核だけ**が数字で確定した — 4 プリセットとも 200 フレーム中、
        **核の層が無いフレームの白は例外なく 0.000%**（核があるフレームだけ 0.13〜1.00%）/
        画角 3 種すべて Draw Call 1 / 決定論はページ再読込をまたいで一致（`8936eca1`）/
        既存表現（Element Lab 2 / Reactive Composite / Spatial Study / Cymatics V1）は
        すべて描画され Draw Call 1 / コンソールエラー 0
        **P6-1（バグ: 四角い枠の除去）**: 設定次第で「うっすら明るい正方形のパッチ」が出る。
        **原因は板（1 枚のクアッド）の縁で要素の裾が切られていたこと。**
        円形に広がるハロを四角い板で切ると、切り口がそのまま直線として見える。
        対策は 2 段構え。**① 板に余白を持たせる**（`pad = 1 + blur × 1.4`）—
        板だけを広げ、描画側は座標を余白で割り直すので**要素の大きさは変わらない**。
        **② 縁のガード**（`frame = 1 - smoothstep(0.86, 1, max(|qx|, |qy|))` を全種別の
        マスクに掛ける）— これで**どの軸設定でも板の縁で必ず 0** になる。
        実測（要素 1 枚だけを描いて板の縁からの相対位置で最大輝度を測る・`blur = 1`）:
        **余白なし（旧）のコアは板の縁の 70% の地点でまだ 255（飽和）・86% で 134・93% で 49**
        ＝ 縁のすぐ内側まで明るく、切り口が正方形として見えていた。
        **余白ありでは 70% で 5・86% で 1・93% で 0**。
        **6 種別すべて板の縁の輝度は 0**（core / haze / membrane / beam / fragment / fan）。
        haze と membrane は 70% の地点で既に 0、fragment は 86% で 0、beam は 93% で 2
        **P6-2/3（十字をバーストの原点へ + `Beam length` / `Density` の 2 軸）**:
        **十字（骨格）を画面中央から外した。** 核・十字・閃光・扇は共通の
        `burstAnchor()`（その打撃のシードから決まる位置）を中心にするので、
        **光が生まれた場所で十字が交差し、バーストごとに移動する**。
        `spreadX/Y` を 0 にすれば従来どおり真ん中へ戻る。
        **`Beam length`**（新軸）は光条の到達距離を 0.3 〜 2.8 倍で連続に振る。
        原点が端に寄っていても貫通するよう、到達距離は
        「画面の半分 + 原点のずれ」に掛ける。
        **`Density`**（新軸）は 1 本で 3 つを同時にスケールする —
        ① 1 イベントで生まれる断片の枚数 ② 同時に生きられる枚数
        ③ **打撃の不応期**（0.06 秒 ÷ 倍率 ＝ コアがより短い間隔で再発光する）。
        生成核の側は `setDensity()` の 1 本だけ足した（既定 1 ＝ 従来どおりなので Lab 2 は不変）。
        層の上限は 32 → 48、破片の上限は 8 → 16 枚へ引き上げた。
        実測: `Beam length` 0→1 で点灯 0.256 → 0.284・平均 7.49 → 9.67・
        縦の広がり 0.142 → 0.163（単調・跳びなし）/
        `Density` 0→1 で**層が 19 → 25 → 30 → 36 → 40 → 45 枚**・平均 8.82 → 11.12（単調）/
        **十字がバーストへ追従する** — `Spread` 0 で重心 (0.500, 0.478)、
        1 で (0.445, 0.404) へ動く /
        **最大負荷でも余裕がある** — 9:16・48 層・`density` `fragments` `blur` `intensity` 最大で
        **1 フレーム p50 0.6ms / p90 1.4ms**（60fps の 16.7ms に対して 1 割以下）・
        **Draw Call 1**・三角 96 / 無音 = 黒（点灯 0.0%・Draw Call 0）
        **P6-4/5（核の大小と質感の幅 + 破片の形状モーフ）**:
        **核の大きさに軸は足さなかった。** 大小は**その打撃のシード**が決め、
        `Blur` が分布を寄せる（`pow(seed, mix(3.4, 0.75, blur))`）。
        軸で決めると「大きい絵にする」という作者の意図になるが、
        リファレンスは同じ設定のまま大小が入れ替わる — だから**生成側の個体差**にした。
        大きい塊は半径に反比例して薄め、ハロも割り戻す（画面が白い塗りにならないため）。
        **シャープ側の核を作り直した** — 「強烈な白の点（`exp(-r² × 900)`）+
        髪の毛のように細い貫通線（`exp(-|y| × 220)`）+ 点在するフレア片」。
        にじみ側は同じ式の係数が動いて「大きく滲んだ塊」になる。
        **`Fragment character`（新軸）** — 0 = 角のある破片 ⇄ 1 = 引っ掻き傷のような羽毛・筋。
        **`Blur` とは別軸**にした（鋭い筋と滲んだ破片を独立に出せるようにするため。
        Blur に載せると「シャープな引っ掻き傷」が作れない）。
        筋の側では板そのものが 3.6 倍に伸び、芯・裾・羽毛の濃淡が 1 本の式で混ざる。
        実測: **核の半径の分布（実測 300 フレーム・ワールド単位。画面の半分は約 3.3）** —
        `blur 0` で 25% 点 0.24 / 中央 0.46 / 75% 点 0.97 / 95% 点 2.03（**小さい点が主体**）、
        `blur 0.5` で 0.40 / 0.66 / 1.65、
        `blur 1` で **25% 点 1.11 / 中央 1.46 / 75% 点 2.18**（**大きく滲んだ塊が主体**）。
        最大 2.58 は画面の半分の 78% ＝ リファレンスの「画面の 1/4 級の光の塊」に相当 /
        `Fragment character` 0→1 で点灯 0.202 → 0.135・広がり sx 0.133 → 0.148（0.6 で山）・
        平均 7.24 → 4.68（筋は面積が広く淡い。単調・跳びなし）/
        **プリセットを 3 枚とも作り直した** — spatial は `beamLength 0.9` `density 0.85`
        `fragmentCharacter 0.85` `skeleton 0.25`、reactive は `0.45 / 0.8 / 0.7 / 0.15`、
        optics は `1.0 / 0.5 / 0.15 / 1.0`。
        実測（9:16）: spatial 点灯 0.557 / 変動 0.130 / 広がり (0.215, 0.198) / 彩度 0.327、
        reactive 0.623 / 0.098 / (0.223, 0.181)、optics 0.176 / **0.594** / (0.169, 0.099)、
        既定 0.324 / 0.210 で 3 つの間に入る /
        **白は核だけ**（spatial・optics とも核の無いフレームの白は 0.000%）/
        無音 = 黒（新軸を両端に振っても点灯 0.0%・Draw Call 0）/
        画角 3 種すべて Draw Call 1・層 23 枚 /
        **決定論はページ再読込をまたいで一致**（画素ハッシュ `d7bfb2e6`）/ コンソールエラー 0

        **P7（Spatial / Reactive の構造を軸として足す。軸は 24 → 30 本）**:
        **層の上限のバグを直した。** `buildUnifiedRig()` の並びの末尾は扇と核なので、
        単純に先頭 48 枚を取ると**密度を上げたとき白へ届いてよい唯一の層が落ちて**いた。
        種別ごとの枠を先取りする `capUnifiedRig()` へ置き換えた。
        実測（合成リグ 50 枚）: 旧 `slice` は core 0 / fan 0（`whiteAllowed` 0 枚）、
        新 cap は core 1 / fan 1（同 1 枚）で fragment だけが 36 → 34 に減る。
        実曲でも層が 48 に達する設定で**核が全フレームに存在**する。

        **`Membrane scale`（新軸）** — 膜の大きさを可視範囲から逆算するのをやめられるようにした。
        可視範囲で割ると**奥の膜も手前の膜も画面の同じ割合を占めて遠近が相殺され**、
        板の集合が 1 枚の平面に見える（Spatial の `spatialMapping.ts` の警告と同じ罠）。
        0 = 従来（画面基準）⇄ 1 = ワールド固定（半径 3.4・画面を越える）。
        天井（0.22 → 0.44）と帯の厚みも同じ 1 本で広がる。
        実測 0→1: 点灯 0.558 → 0.952・平均 10.4 → 58.7・縦の広がり 0.177 → 0.281（単調）。

        **`Texture grain`（新軸）** — アトラス 10 枚のうち**1 枚しか使っておらず**、
        全種別・全インスタンスが同じ向き・同じ切り口で読み、灰色にして ±22% 振るだけだった。
        タイル・クロップ・UV 回転・反転を**要素ごと**にし、素材の色も比率として混ぜる。
        タイル選択は種別ごとの役割（`rolesByKind`）＋破片は帯域（`rolesByBand`）＋
        役割外の余地 0.14 で、リグは番号を知らず 0〜1 の抽選値だけを渡す。
        **2 つのバグを潰した**: ① 列数 4 では 10 枚が 3 行に収まらず空きマスが 2 つ残り、
        そこを引いたインスタンスが真っ黒になっていた（列数 5 = 5×2 へ）/
        ② アトラスは Canvas 由来で flipY が効くので**行は下から数える**必要がある
        （上から数えると素材の割り当てが行ごと入れ替わる）。
        黒浮きの敷居と曲げは**実測に合わせ直した** — アトラスの輝度は中央値 0.004・
        p90 0.103 しかなく、シェーダーへは線形で届く（0.1 の階調が 0.009 に沈む）ので、
        一度 sRGB へ戻してから敷居 0.01・曲げ 0.45・利得 2.4 を掛ける。
        実測 0→1: 点灯 0.61 → 0.61（0.2 で 0.67 の山）・平均 16.5 → 24.1・彩度 0.67 → 0.49・
        1 フレームに 5 種類のタイルが同居（跳びなし）。

        **`Event membrane`（新軸・Spatial 感の本体）** — 膜と靄は `seed = 7` の固定値から
        引かれ、**曲や打撃に関係なく常に同じ場所・同じ形**だった。
        0 = 固定のリグの膜 ⇄ 1 = **打撃ごとに 2〜5 枚生まれ、遅れて開き、
        0.3〜2.1 秒生きて死ぬ膜**。中間は両方を出して明るさを配分するので連続。
        リグは純関数のままなので、プールと誕生の予定は `LightUnified` 側に置いた。
        生成核には**読み出し専用の窓（`strikes()`）を 1 つ足しただけ**で、
        検出・結線・時間規律には触っていない。
        実測 0→1: プール 0 → 21 枚・膜の層 3 → 18.6 枚・点灯 0.63 → 0.92・
        平均 20.8 → 56.9（単調・白は 0.000%）。

        **`Stagger`（新軸・二重の時間軸）** — 全層が同じ時計で明滅すると、
        層が何枚あっても「1 枚の絵が点滅する」ようにしか見えない。
        0 = 全層同時 ⇄ 1 = 種別ごとに 0〜0.22 秒ずれて開き、
        **尾の長さも 10 倍の幅で分かれる**（光条 0.42 / 核 1 / 膜 2.1 / 靄 3.2 倍）。
        素の駆動を短い遅延線（`DelayLine`）に貯めて、種別ごとの時刻を読むだけで作る。
        実測 0→1: 点灯 0.77 → 0.88 → 0.86 → 0.83 → 0.84・平均 29.6 → 36.7・
        膜の層 19.6 → 23.1（長生き）/ 光条 5.0 → 5.3（跳びなし）。

        **`Hue depth`（新軸）** — 1 要素の中の色相幅は最大 0.13 で、彩度は定数だった。
        0 = ほぼ単色 ⇄ 1 = **等間隔 4 点 + 往復 + 彩度ランプ 3 種**（Spatial の
        `hues[4]` / `saturations[4]` / `resample4` の流儀を per-instance 属性へ）。
        軸 0 では 4 点が直線上に並ぶので従来とまったく同じ並びに戻る。
        `Hue coherence`（要素**どうし**）とは直交する。
        実測 0→1: 彩度 0.61 → 0.38（単調）・明るさはほぼ一定。

        **`Silhouette`（新軸）** — `polygonAtlas.ts`（12 種の非対称多角形 SDF）を流用し、
        板の四角い外形を要素ごとに削る。核だけは削らない（白熱の芯が欠ける）。
        実測 0→1: 点灯 0.83 → 0.73・平均 41.7 → 28.3（単調）。

        **無音 = 黒を厳密にした。** 平滑は 0 へ漸近するだけなので場が厳密には 0 にならず、
        層が数枚だけ残っていた。下限（0.004）を切って 0 と見なす。
        実測: **全 30 軸を 0 に振っても 1 に振っても、層 0 枚・Draw Call 0・最大画素 0**。

        **全 30 軸の連続歩行（0→1 を 11 段）**: 平均輝度の 1 段あたりの最大変化は
        `Membrane–Beam` 5.9（37.5 → 6.0 の単調な減少幅の 16%）、`Intensity` 3.7（同 17%）、
        `Spread Y` 3.1、`Stagger` 2.3、`Texture grain` / `Membrane scale` / `Hue depth` 2.0、
        **残る 23 本はすべて 2.0 以下**で、どの軸にも段は出ない。

        **プリセット 3 枚を新軸込みで組み直した。**
        spatial は `membraneScale 0.9` `eventMembrane 0.9` `textureGrain 0.82`
        `silhouette 0.7` `stagger 0.85` `hueDepth 0.72`、
        にじみと光条は逆に**下げた**（`blur 0.92 → 0.6` / `beamLength 0.9 → 0.6` /
        `skeleton 0.25 → 0.12`）— 0.9 を超えるにじみは層の縁を溶かして 1 枚の靄にし、
        長い光条は空間ではなく図形を主役にしてしまう。
        実測（1:1・同じ小節）: spatial 点灯 0.608 / 平均 17.2 / 彩度 0.365 / 膜 12.1 枚、
        reactive 0.864 / 29.5 / 0.543 / 膜 15.7 枚、optics 0.226 / 6.3 / 0.636 / 膜 3 枚
        （＝ Lab 2 と同じ「固定の膜だけ」）、既定 0.512 / 13.6 で 3 つの間に入る /
        **核の無いフレームの白は 3 枚とも 0.000%**（90 / 90 フレーム）/
        Draw Call 1・**1 フレーム p50 1.3〜1.5ms / p90 1.9〜2.4ms**（60fps の 1 割）/
        9:16・全軸最大でも p50 1.5ms / p90 2.1ms・Draw Call 1 /
        **既存表現 6 種と共有の対応づけは差分ゼロ**（`git diff` で確認。
        触ったのは `opticsAudioDrive.ts` の読み出し窓 23 行だけ）/ コンソールエラー 0

        **P8（コアの精査。軸は 30 → 35 本）**:
        精査で分かった構造的な事実が 6 つあり、すべて塞いだ。

        **① Unified にはブルームも露出も無かった。** Spatial / Reactive は
        `UnrealBloomPass`（閾値 0.22 / 強さ 0.9 / 半径 0.5）と `1 - exp(-x·0.95)` を
        自前で持っていて、コアがテクスチャ板なので**面ごと滲む**。
        **`Core bloom`（新軸）** で同じ構成を Unified の中に置いた
        （`EffectComposer` → `RenderPass` → `UnrealBloomPass` → 露出板 → `EffectPipeline`）。
        軸 0 では滲みの強さが 0・トーンマップの混合も 0 なので**現状と厳密に一致**し、
        5 段のぼかしも 1 枚も走らない（`enabled = false`）。
        実測: Draw Call は軸 0 で **2**（場面 + 表示板）、軸 > 0 で **15**。
        1 フレーム p50 1.8〜2.3ms / p90 2.5〜3.7ms。

        **② `dilute = min(0.2 / size + 0.1, 1)` が size をちょうど打ち消していた** —
        つまり**「広い」と「白い」が両立しなかった**。
        **`Core size`（新軸）** は大きさの幅（0.10〜0.60 ⇄ 0.90〜3.60）と
        **薄め方の指数**（1.36 ⇄ 0.46）を同じ 1 本に載せる。
        既定 0.4 が従来の 0.20 / 2.30 と指数 1.0 をちょうど通るよう曲げてあるので
        （0.4^2.27 = 0.125 / 0.4^0.62 = 0.567・誤差 0.03% 以下）、**既定では 1 画素も変わらない**。
        大きい側では板が **1 → 3 枚**に増え、少しずつずらして重なる（`countFade` で段を消す）。
        実測 0→1: 半径 0.27 → 1.86・**明るさ 1.13 → 0.70 で下げ止まる**（従来は size に反比例して沈む一方だった）。

        **③ コアだけ完全な等方（真円）だった**（他 3 表現は 0.68〜1.45 の異方）。
        **`Core shape`（新軸）** は ① 縦横比 1 : 1 ⇄ 1.45 : 0.78（面積保存）
        ② **平頂化**（超ガウス `exp(-pow(r², 1 → 1.9) × …)`）を同時に動かす。
        平らな頂 + 急な縁は「点が光る」ではなく「面が光る」見えになる。
        芯の白い点（spark）は指数 1 のまま残すので、**面の中に白熱した芯**が居る形になる。
        実測 0→1: 白の画素が 0.007% → 0.059%（**白が「点」から「面積」になった**）。

        **④ コアの素材が乗算 0.22（削るだけ）だった**（Reactive は 1.38・Lab 2 は 0.8 の加算）。
        核だけ**加算**へ変えた（`Texture grain` 軸に載せる・種別の分岐なので軸は連続のまま）。
        併せて核へ緩い楕円窓（0.58 → 1.03）を足し、縁が板の内側で溶けるようにした。
        実測 `Texture grain` 0→1（核の軸を上げた状態）: 白 0.22% → 5.5%・平均 36 → 50。

        **⑤ 十字が消えない穴が 3 つあった** — 骨格・閃光は長さの下限 0.3 で残り、
        **核自身の貫通線は `Beam length` にも `Skeleton` にも繋がっていなかった**。
        `crossGain = smoothstep(0, 0.12, beamLength)` を骨格・閃光・核の貫通線の**明るさ**へ、
        `smoothstep(0, 0.1, skeleton)` を核の貫通線へ掛けた（長さは 0 にしない＝退化四角形を作らない）。
        実測: `Beam length` 0 で **光条の層 0 枚・核の貫通線 0**、`Skeleton` 0 で核の貫通線 0。
        プリセット 4 枚はどれも `beamLength ≥ 0.45` / `skeleton ≥ 0.12` なので**係数はすべて 1**（不変）。

        **⑥ `UNIFIED.reserve.core = 4` を確保しているのに `buildCore` は 1 枚しか返していなかった** —
        ② の複数枚化で枠を使い切れるようにした。

        **`Cross rotation`（新軸）** 0 = 現状の向き ⇄ 1 = π 回転。原点を中心にする層
        （核・骨格・閃光・扇）の面内回転へ一律に足す。扇は基準角が板ローカルなので `shape[0]` にも足す。
        **`Cross angle`（新軸）** 0 = 上下左右固定 ⇄ 1 = 1 本ずつ ±π の自由な向き
        （向きは打撃のシード由来なので決定論）。**「斜めだけ」の絵が作れるようになった。**

        **閃光を片側化した（P5）。** 両側で描くと 4 本が中心で重なって**点が 4 重に加算**され、
        面ではなく点が強調されていた。Lab 2 と同じ片側化 + 根元フェード
        （`smoothstep(0, 0.09, p.x)`）を引き継ぐ。常設の骨格は十字の桁なので両側のまま。
        併せて Lab 2 の `ARM_STYLE`（族ごとの長さ・太さ・ハロ）を引き継ぎ、
        **閃光の質を核の形状族が決める**ようにした（族 0 は従来と同じ値）。

        **コアが「面」に見えるようになったか**: なった。
        `Core size 0.6` + `Core shape 0.55` + `Core bloom 0.4` で、
        中心は**白い面（不揃いな輪郭・平らな頂）+ その中の白熱した芯**になり、
        周囲へ斜めのプリズム条が伸びる。以前の「小さな白い点 + 十字」とは別物。

        **プリセット 4 枚を新軸込みで組み直した。**
        spatial `coreSize 0.6 / coreShape 0.55 / coreBloom 0.4 / crossAngle 0.55`、
        reactive `0.5 / 0.45 / 0.45 / 0.35`、optics `0.35 / 0.12 / 0.18 / 0.08`（Lab 2 のまま）、
        既定 `0.45 / 0.3 / 0.3 / 0.25`。ブルームと核の加算素材が明るさを持ち上げるので
        `Intensity` を spatial 0.40 → 0.24 / reactive 0.22 → 0.11 へ下げた。
        実測（1:1・同じ小節）: spatial 点灯 0.780 / 平均 39.8 / 彩度 0.283 /
        **核の無いフレームの白 0.000%（80 / 80 フレーム）**、
        reactive 0.911 / 50.5 / 0.489、optics 0.267 / 7.6 / 0.685（膜 4.6 枚 = Lab 2 のまま）、
        既定 0.476 / 24.1 / 0.551（核の板 2 枚）。

        **全 35 軸の連続歩行（0→1 を 11 段）**: 1 段あたりの平均輝度の最大変化は
        `Membrane scale` 13.2（10.8 → 48.1 の単調な増加幅の 35%）、
        `Membrane–Beam` 12.7（60.4 → 7.0 の 24%）、`Haze floor` 8.2、`Intensity` 8.1、`Blur` 7.0、
        **残る 30 本はすべて 6.8 以下**。どの軸にも段は出ない
        （新軸は `Core bloom` 6.5 / `Cross angle` 5.3 / `Core shape` 4.7 / `Core size` 3.3 / `Cross rotation` 2.8）。

        **無音 = 黒はブルームを通しても厳密**（`1 - exp(0) = 0`）:
        全 35 軸を 0 に振っても 1 に振っても**層 0 枚・最大画素 0**。
        9:16・全軸最大でも p50 1.8ms / p90 2.5ms・Draw Call 15 /
        **既存表現 6 種は差分ゼロ**（この回は共有ファイルも 1 行も触っていない）/ コンソールエラー 0

        **P9-1（明るさの中立化。軸の統合の下ごしらえ）**:
        35 軸の調整が終わらない原因を数式レベルで洗ったところ、**軸の本数ではなく
        「明るさに効く軸が 6 本以上あり、`Intensity` が補正ノブとして消費されている」**
        ことが本体だと分かった。証拠は実装の履歴そのもので、P8 でブルームと
        核の加算素材を入れたとき、プリセットの `Intensity` を
        spatial 0.40 → 0.24 / reactive 0.22 → 0.11 と**下げて辻褄を合わせている**。

        `Membrane scale` / `Haze floor` / `Texture grain` の 3 本を、
        `Core shape` の面積保存（1.45 × 0.78）や `grain.gain = 2.4` と同じ流儀で
        **局所の補償係数**を置いて明るさ中立にした（`UNIFIED.neutral`）。

        ① **`Membrane scale`** — 板の面積と帯の太さを同時に広げるので総光量が跳ねていた
        （1 段あたり 13.2 ＝ 全 35 軸で最大）。`membraneHalf()` が
        「広がった比（面積 × 帯の太さ）」を返すようにし、そのぶんを割り戻す。
        **比は基準点（既定値 0.45）で 1 になるよう作ってある**ので、
        既定のままなら中立化の前後で膜の濃さは変わらない（動くのは軸の両端の側）。
        ② **`Haze floor`** — 靄は画面全体を覆う 1 枚で、明るさと面積を取り引きする余地が無い。
        **完全に中立にすると軸そのものが消える**（0 = 無し ⇄ 1 = 厚い が保てない）ので、
        上側だけを圧縮し（`hazeCompression`）、減らした光量は裾の広がりへ回した
        （`hazeReach`。「厚い」が明るさではなく広がりとして読める）。0 での傾きは 1 のまま。
        ③ **`Texture grain`** — 核だけは素材を**加算**する（他は乗算でマスクの平均が 1）ので、
        軸を上げると核の光量そのものが増えていた（P8 実測で白 0.22% → 5.5%・平均 36 → 50）。
        加算のぶんだけ土台を割り戻す係数をシェーダーへ足した（`uCoreGrainNeutral`）。
        `grain = 0` では厳密に 1 なので、素材が無いときの見え方は変わらない。

        併せて **`Intensity` の実寸を定数へ出した**（`UNIFIED.intensityRange`）。

        実測（合成音・決定論・1:1・420×420・120 フレーム / ウォーム 180）。
        **軸を 0→1 に振ったときの平均輝度の幅**:
        `Membrane scale` 59.5 → 52.4（幅 7.0 = −12%。中立化前は 10.8 → 48.1 の +345%）/
        `Texture grain` 62.9 → 59.1（幅 4）/ `Haze floor` 49.4 → 59.4（幅 10）に対し、
        **`Intensity` は 9.7 → 88.1（幅 78 = 9.0 倍）** ＝
        **明るさを決める軸は `Intensity` 1 本**という状態に戻った（次点の 8 倍の権限）。

        **プリセットの `Intensity` を補正値から素直な値へ戻した** —
        spatial 0.24 → 0.42 / reactive 0.11 → 0.20 / optics 0.56 → 0.30 / 既定 0.50 → 0.545。
        **見え方は中立化の前後で同等**（同じ合成音・同じ小節）:
        既定 平均 53.11 → 53.69（+1.1%）・点灯 0.717 → 0.714・白 0.108% → 0.103%・彩度 0.538 → 0.523、
        spatial 60.86 → 59.00（−3.1%）・0.885 → 0.930、
        reactive 77.16 → 71.34（−7.5%）・0.958 → 0.948、
        optics 10.09 → 11.02（+9.2%）・0.251 → 0.236。
        広がり (sx, sy) は 4 枚とも ±0.01 以内。
        **白の予算は不変** — 4 枚とも**核の無いフレームの白は 0.000%**。
        **無音 = 黒は厳密**（全 35 軸を 0 に振っても 1 に振っても層 0 枚・最大画素 0）/
        lint・build 通過 / コンソールエラー 0

        **P9-2（焼き込み 2 本 + 吸収 2 本。軸は 35 → 31 本）**:
        3 プリセットの座標を集計したところ、**35 軸は実質 7 つの束としてしか使われておらず**、
        どのプリセットでも動いていない軸が 3 本あった
        （`Cross rotation` レンジ 0.00 / `Tick rate` 0.05 / `Fragments` 0.05）。

        **焼き込み**:
        ① **`Fragments`** — `Density` と数式が**完全な積**だった
        （`fragmentCount × fragments × mix(0.5, 2.2, density)`）。濃さを定数 0.55 へ焼き
        （3 プリセット + 既定の 0.5 / 0.6 / 0.6 / 0.55 の中央）、
        **`Density` の下限を 0 まで下げて**「破片を出さない」（旧 `Fragments = 0`）を移した。
        上限は `mix(0, 2.61, density)` で、既定 0.55 が旧 `mix(0.5, 2.2, 0.55) = 1.435` を
        ちょうど通る。**到達点はむしろ広がる**（旧上限 2.2 → 2.61）。
        ② **`Cross rotation`** — 3 プリセットも既定も 0 のまま一度も使われず、
        かつ**作者が画の向きを決めるノブ**でもあった（D17 に反する）。
        `Cross angle` へ畳み、**向きは打撃のシードが決める**ようにした
        （`Cross angle` は「どれだけ自由に振れてよいか」だけを持つ）。
        軸 0 では面内回転がすべて厳密に 0 ＝ 従来の向き。

        **吸収**:
        ③ **`Tick rate` → `Strobe`** — `strobe = 0` ではラッチ量も off ティックの消灯も 0 で、
        **ティックがどこにも効かない**（`strobePhaseGain` は即 1 を返す）。
        コマ送りが深くなるほど時計も速くなる（12 → 48fps）。
        ④ **`Tilt` → `Depth spread`** — 独立軸としての実測は平均輝度 9.14 → 8.84・
        広がり 0.1569 → 0.1595 で**画素の上ではほとんど見えず**、
        3 プリセットでも `Depth spread` と完全に同じ順で動いていた
        （(0.9, 0.75) / (0.5, 0.35) / (0.2, 0.05) ＝ 傾き ≈ 奥行き × 0.8）。

        実測（移し替えの前後・同じ合成音・同じ小節）:
        既定 53.69 → 53.47（−0.4%）/ spatial 59.00 → 57.88（−1.9%）/
        reactive 71.34 → 68.92（−3.4%）/ optics 11.02 → 11.88（+7.8%。
        ティックが 30 → 48fps へ上がったぶんで、変動係数は 0.525 → 0.466）。
        点灯率・彩度・広がり (sx, sy) は 4 枚とも誤差の内。
        **到達点の確認**: `Density = 0` で**破片の層 0 枚**（旧 `Fragments = 0` と同じ）・
        0.55 で 13 枚（旧 11.5 の見込みどおり）・1 で 20 枚（枠の上限）/
        `Cross angle = 0` で核・骨格・扇の面内回転が**厳密に 0 / ±π/2**（従来の向き）、
        1 でシード由来の向きへ一律に回る（十字はほどけない）。
        **無音 = 黒は厳密**（全 31 軸を 0 に振っても 1 に振っても層 0 枚・最大画素 0）/
        白の予算は不変（核の無いフレームの白 0.000%）/ lint・build 通過 / コンソールエラー 0

        **P9-3（マスター 6 本 + アコーディオン UI。上段 17 / 詳細 21）**:
        31 軸は 3 プリセットの座標を並べると**実質 7 つの束**としてしか動いていない。
        たとえば `Texture grain` / `Silhouette` / `Fragment character` は
        (0.82, 0.70, 0.85) / (0.60, 0.50, 0.70) / (0.12, 0.08, 0.15) と 3 枚ともほぼ同じ順で、
        **3 本を別々に触っているが、やりたいことは 1 つ**だった。

        **マスターは状態を持たない**（`unifiedMasters.ts`）。`UnifiedAxes` に欄は増えず、
        配下への**写像関数 1 本**として実装してある —
        `apply()` が配下を一斉に書き、`read()` が配下から位置を逆算する。
        したがって**詳細を直接触ればそちらが常に優先**され、マスターの表示がそちらへ付いてくる。
        `normalizeAxes`・プリセット・決定論はマスターの存在を知らないまま動く
        （実測: マスター導入の前後で 4 プリセットの画素統計が**完全に一致**）。

        マスター 6 本: **Spread**（量。偏りは `Aspect`、`Anchor pull` と生の `Spread X/Y` は詳細）/
        **Time**（`Attack` を畳み込み、`Decay`・`Stagger` は詳細）/
        **Colour lock**（`Hue coherence` + `Hue stickiness`）/
        **Core**（`Core size` + `Core shape` + `Core bloom`）/
        **Membrane**（`Membrane scale` + `Event membrane`）/
        **Material**（`Texture grain` + `Silhouette` + `Fragment character`）。

        **束ねなかったもの**（束ねると到達できる見え方が減る）:
        `Hue depth` は `Colour lock` に入れない — 要素どうし（coherence）と要素の中（depth）は
        彩度という 1 つの観測量を**逆向き**に動かすので、束ねると打ち消し合って何も起きない
        （実測 0→1 で coherence は 0.260 → 0.576、depth は 0.61 → 0.38）/
        `Blur` は `Core` に入れない（核の半径だけでなく膜・破片・靄・光条の縁にも効くので、
        「シャープな縁のまま大きい核」が作れなくなる）/
        `Membrane–Beam` は `Membrane` に入れない（「膜が優勢だが画面基準」が作れなくなる）/
        `Skeleton` と `Beam length` は束ねない（spatial は 0.12 + 0.60 ＝
        **常設の十字なしで長い閃光**、optics は 1.0 + 1.0。1 本にすると前者が消える）/
        `Spread X` と `Spread Y` は 1 本に潰さない（実測で sx / sy は独立に動く）/
        `Material` の詳細 3 本は必ず残す（`Texture grain` は核へ**加算**、
        `Silhouette` は核に**効かない** ＝ 核への効き方が逆）。

        **UI をまとまりごとのアコーディオンにした**（`ExpressionParamBase` に
        `group` / `detail` を足しただけなので、他の表現の宣言は 1 行も変わらない）。
        明るさ 1 / 配置・空間 2 / 時間 2 / 光学 1 / 色 2 / 構成 8 / 動き 1 ＝ **常時見えるのは 17 本**、
        **詳細 21 本**は各まとまりの中の折りたたみへ（既定は閉）。
        閉じているだけで**つまみは 1 つも減っていない**。
        実測: `Spread` × `Aspect` で `spreadX/spreadY` の面を覆えること（(1.00, 0.33) /
        (0.27, 1.00) / (0.02, 0.08)）/ `Material` を 1 → 0 で配下 3 本が
        (0.82, 0.70, 0.85) → (0.12, 0.08, 0.15) へ追従 /
        そのあと `Texture grain` を直接 0.9 にするとマスターの表示が 0.333 へ動く（詳細が優先）/
        4 プリセットの画素統計は**マスター導入の前後で完全一致** /
        lint・build 通過 / コンソールエラー 0

        **P9-4（結線を 8 本へ）**:
        繋ぎ先が 20 本あっても選べないので、**音で動かして意味がある 8 本**へ絞った。
        **Emission（発光 All）/ Intensity / Spread / Core / Blur /
        Hue（色相 H）/ Channel balance / Density**。

        外したもの: **`Dispersion`**（実測 0→1 で点灯 0.256 → 0.288・彩度 +0.05 と効きが最小）と
        **`Depth spread`**（同 白 0.128% → 0.197% のみ）。どちらも軸としては残っている。
        入れなかったもの: 時間の形（`Strobe` / `Attack` / `Decay` / `Stagger` / `Hue stickiness`）は
        音で揺らすと「音への反応」ではなく**反応の仕方が揺れる**ことになり因果が読めなくなる。
        質感（`Texture grain` / `Silhouette` / `Fragment character` / `Core shape` / `Membrane scale`）は
        1 曲の中で変わると別の表現に見える。

        **マスターへ繋げるようにした。** `Spread` と `Core` は軸ではなくマスターへ繋ぐので、
        **1 本の音が配下をまとめて動かす**（実測: `Core ← Rise` で
        `Core size` / `Core shape` / `Core bloom` が (0.45, 0.30, 0.30) → (0.58, 0.52, 0.43) へ同時に動く）。
        変調は**描画用の写しの上へ**書くので `this.axes` は触らず、
        「基準値 ± 変調」の契約がマスターでもそのまま成り立つ（結線を外すと基準値へ戻る）。
        マスターは状態を持たないので、基準値は毎フレーム配下から逆算する
        （詳細を直接動かしても結線の中心がそこへ付いてくる）。
        色相 H は下流の時間規律（確認時間・最短保持）を持つため、これまでどおり
        `OpticsAudioDrive` 側の 1 本へ繋ぎ、行は増やさず `Colour lock` の行に添えた。

        **「結線しても動かない軸」を塞いだ**（分析で洗い出した配線の穴）:
        `Core bloom` は `this.axes` の直読みだったので `effectiveAxes()` 経由へ /
        `Density` は 2 経路（`advanceEventMembranes` と `applyDensity`）とも結線値が通るようにし、
        `applyDensity()` を毎フレーム呼ぶようにした。
        **結線に出しているのは、実際に動く 8 本だけ**である。

        実測（既定・深さ +1）: `Intensity ← Volume` で平均 52.7 → 68.4 /
        `Spread ← Onset` で広がり (0.167, 0.161) → (0.190, 0.179)・点灯 0.708 → 0.805 /
        `Blur ← Treble` で 52.7 → 60.1 / `Density ← Bass` で層 45.5 → 47.7 /
        `Core ← Rise` で白 0.119% → 0.758% / `Channel balance ← Onset` で
        RGB (27.4, 52.4, 46.9) → (20.6, 36.5, 46.3)・彩度 0.525 → 0.615 /
        `Hue ← Treble` で RGB (36.2, 61.5, 44.2) / `発光 All ← Volume` で 52.7 → 15.8。
        **4 プリセットの画素統計は再読込をまたいで完全一致**
        （53.472 / 57.877 / 68.920 / 11.879）/ 無音 = 黒 / 白の予算は不変 /
        lint・build 通過 / コンソールエラー 0

        **P9-5（Light 系の実験表現をセレクトから隠す）**:
        Light Unified へ収斂させる途中なので、それ以前の実験表現は**選べないようにした**。
        隠したのは 6 ファミリー — Light Traces / Light Traces — Core Study /
        Light Traces — Spatial Study / Light Element Lab / Light Element Lab 2 /
        Light Reactive Lab。セレクトに残るのは
        **Cymatics / Modular Pattern Field / Light Unified / Reactive Geometry** の 4 つ。

        **コードは 1 行も消していない**（D33 の判断が出るまで比較用に温存する）。
        `ExpressionFamily` に `hidden` を足し、UI が一覧から外すだけの最小の変更で、
        `createExpression` の分岐も `KNOWN_EXPRESSION_IDS` もそのまま。
        したがって `normalizeExpressionId` は**隠した id も有効なまま**受ける。
        さらに `visibleExpressionFamilies(current)` は
        **いま選ばれているものだけは一覧に残す**ので、保存済みのプリセットが
        隠した表現を指していても選択が勝手に別の表現へ飛ばない。
        実測: `expressionId: 'light-spatial-study-v1'` を保存した状態で読み込むと
        Spatial Study がそのまま描画され（コンソールエラー 0）、一覧にもその 1 つだけが現れる。
        Light Unified へ切り替えると一覧から消える。
        lint・build 通過 / コンソールエラー 0

        **P9-6（縁の締まり — `Edge contrast` / `Core focus` の 2 軸）**:
        Spatial / Reactive 期の GIF 2 本（400×400 と 400×225）を ImageDecoder で
        フレーム分解し、**現状の Light Unified（既定）と同じ物差し**（どちらも幅 200 px へ
        面積平均で縮小、平均輝度 15〜70 のフレームだけ）で比べたところ、
        「大きく滲んだ塊になっている」という見立てが数値で裏づけられた。

        | 指標 | GIF（n=12） | 現状の既定（n=8） | 比 |
        |---|---|---|---|
        | 核の半値半径 `cr50` | 18.9 px | **36.0 px** | 1.90 倍広い |
        | 核の 1/4 半径 `cr25` | 30.0 px | **51.0 px** | 1.70 倍 |
        | 頂の平ら（≥ ピーク 90% の画素率） | 0.012 | 0.032 | 実体が薄く裾が長い |
        | 隣接勾配の p90（輝度で正規化） | 0.261 | **0.098** | 2.67 倍鈍い |
        | 同 p50 | 0.093 | 0.056 | 1.67 倍 |
        | 高周波のエネルギー比 | 0.0120 | **0.0012** | **10 倍少ない** |
        | 点灯した横方向の連なり（中央値） | 4.8 px | **130.8 px** | 27 倍つながる |
        | 黒の割合 / 彩度 | 0.379 / 0.222 | 0.424 / 0.486 | — |

        **核の裾の正体は丸いハロだった。** 既定（`Blur` 0.5）では半径 0.5 の位置で
        核の明るさ 0.106 のうち **0.1053 がハロ**で、形そのものの寄与は 0.0007 しかない。
        `elementMask` の `base + halo·exp(−r²·w)` が全種別に丸い光を足していたので、
        破片も光条も「同じ丸い雲」に見え、要素どうしが溶けてつながっていた。

        直し方は**可動域を片側へ伸ばすこと**にした。`Blur` の写像は 1 か所も動かさず、
        新しい 2 本を足して、どちらも **0 が「現状のまま」**になるようにしてある。

        - **`Edge contrast`（光学・上段）** — `softEdge` の窓幅を 1/6.25 へ、ハロの量を
          15% にして広がりを 3.6 倍詰め、膜の襞と羽毛の谷を深くし（底 0.55 → 0.16 /
          0.45 → 0.12。**山は 1 のままなので明るさの頭は動かない**）、
          多角形マスクの縁を 0.3 → 0.066、素材のガンマを 1 の側へ寄せる。
        - **`Core focus`（構成・詳細）** — 超ガウスの指数を上げるが、
          `k = kBase · (kBase/ln2)^(n/nBase − 1)` と置くので**半値半径は保たれる**
          ＝ 核は小さくならず、頂が平らになって裾だけが急に落ちる。
          散乱片の裾を 3.2 倍詰め、核のハロを 18% にし、ブルームは閾値 +0.55・半径 0.2 倍。
          `Core bloom` が**強さ**、こちらが**広がり方**を持つので直交する。

        **明るさは中立化した。** 裾を消すとそのぶん暗くなり（無補正で 38.4 → 16.4 ＝ −57%）
        軸が「シャープ」ではなく「暗い」として読めてしまうので、
        こぼれていた光を形へ返す利得を置いた（`definition.edgeGainAtOne` 1.75 /
        `coreFocusGainAtOne` 2.2。核の側は `isCore` で選ぶので他の種別には掛からない）。
        **天井（白の予算）は 1 つも動かしていない。**

        実測（実音 reference.wav・5 点・幅 200 px。既定 → 2 軸とも 1）:
        平均輝度 38.4 → 32.5（−15%）/ 核の半値半径 **30.4 → 22.2 px**（GIF は 18.9）/
        高周波の比 **0.0007 → 0.0063（9 倍）**（GIF は 0.0120）/
        勾配 p90（正規化）0.0966 → 0.1212 / 同 p50 0.0533 → 0.0579 /
        頂の平ら 0.022 → 0.016 / 連なりの中央値 129 → 117。
        **どの指標も GIF の側へ寄り、高周波と核の滲みは同じ桁まで来た。**

        **既定と 4 プリセットは 1 画素も動いていない。** 合成した解析値で 240 フレームを
        進めてから画素をハッシュする再現テストを書き、**変更前のコード（stash）と
        変更後で完全一致**することを確認した — 既定 `4532ae8a` / 合計 2702295、
        Spatial `7b1ebb50` / 661330、Lab2 `43843302` / 347285。
        新しい 2 本は式の中で `mix(1.0, X, 軸)` と `pow(k, n/nBase − 1)` の形にしてあり、
        軸 0 では前者が 1.0、後者が `pow(x, 0.0) = 1.0` を返すので**恒等式に戻る**
        （分岐は 1 つも足していない）。3 プリセットには `edgeContrast: 0` /
        `coreFocus: 0` を明記したので、プリセットを選ぶと必ず現状の座標へ戻る。
        無音 = 黒も維持（2 軸とも 1 のまま 30 秒ぶん時計を進めて最大チャンネル 0・層 0）。
        **隠した表現・他表現は差分ゼロ**（触ったのは Light Unified の 3 ファイルだけ）。
        lint・build 通過 / コンソールエラー 0
        **P9-7（床を割る — `Isolation` 軸）**:
        P9-6 の宿題（縁を締めても要素が剥がれない）に決着をつけた。
        **残っていたのは縁ではなく面積**である。画面全体を覆う靄 1 枚と、
        可視範囲から逆算した大きな膜が重なって**明るい床**を張っており、
        どこにでも薄く光が乗っているので、要素の縁をいくら立てても
        隣との谷が黒へ落ちない。

        物差しを引き直した（**この節の数値は前節とは別の物差し**）。
        合成した解析値・512×512 で 300 フレーム進め、幅 200 px へ縮小して
        平均輝度 > 1 のフレームだけを取り、点灯（輝度 > 8/255）した
        横方向の連なりの中央値を測る。同じ関数で参照 GIF も測ると、
        `dev-ref-light-1.gif`（400×226・307 フレーム）は **7 px**（前節の物差しでは 4.8 px）。

        `Isolation`（0 = 一枚の床 ⇄ 1 = 独立した板）は 6 つを 1 本で動かす。
        どれも `mix(1, X, 軸)` の形で、**軸 0 では厳密に 1 倍**。

        - 靄の量 8% / 靄の板の大きさ 62%（＝ 最下段そのものを引く）
        - 常設の膜の量 12% / 枚数 45%（曲に関係なく張られている側を引く）
        - 打撃ごとの膜の濃さ 2.2 倍（引いた床のぶんをイベント側へ移す）
        - 膜 1 枚の**面積** 46%（位置は触らないので**散らばりは保ったまま板だけが小さくなる**）
        - 1 打撃が生む枚数 60% / 同時に生きられる枚数 50%（＝ **同じ場所への重なりの上限**）

        面積の縮小は明るさの中立化（`membraneLightGain`）へは**通していない**。
        通すと縮めたぶんだけ明るくなり、重なりがまた床を張ってしまう。

        実測（既定 → `Isolation`）:

        | 指標 | GIF | 軸 0（現状） | 0.5 | 1 | 1 + 縁 + 焦点 + 素材 |
        |---|---|---|---|---|---|
        | 連なりの中央値 | **7** | **182** | 131.5 | **68.3** | **15.5** |
        | 連なりの平均 | 84.2 | 161.9 | 106.1 | 60.6 | 32.2 |
        | 点灯した画素の割合 | 0.647 | 0.829 | 0.488 | 0.272 | 0.184 |
        | 黒の割合 | 0.244 | **0.000** | 0.043 | 0.374 | 0.725 |
        | 高周波の比（×10⁻³） | 0.516 | 0.129 | 0.698 | 3.95 | 16.1 |
        | 勾配 p90（正規化） | 0.106 | 0.068 | 0.174 | 0.541 | 1.384 |
        | 平均輝度 | 46.6 | 64.5 | 37.1 | 17.2 | 9.6 |

        **軸 1 だけで連なりは 182 → 68 px（−62%）**、既存の `Edge contrast` /
        `Core focus` / `Texture grain` と併せると **15.5 px** まで来て、
        GIF の 7 px と同じ桁に入る。**黒が 0.000 から 0.374 へ立ち上がる**のが本質で、
        これまで「画面のどこにも黒が無い」状態だったのが、板と板のあいだが黒へ落ちる。

        濃さ 2.2 倍は実測で決めた。1.45 では軸の上側が「板が離れる」ではなく
        **「膜が消える」**になり（32×32 に落とすと板が読めない）、2.8 では重なりが
        戻って連なりが 68 → 115 px に緩んだ。

        **P9-8（同時発光 — `Band unison` 軸）**:
        `BandLightEventDetector` は最初から Bass / Mid / Treble を独立に判定しているのに、
        後段の帯域選択（`relativeStrengthFloor = 1`）が**素のフラックスが最大の 1 本**しか
        通していなかったので、1 打撃の中の同時性が捨てられていた
        （Core Study の実測では 1 打撃で 3 帯域同時発火が 48% / 2 帯域が 33%）。

        軸は floor を `1 → 0.12` へ連続に動かす。0 にはしない — 鳴っていない帯域の
        微小なフラックスまで拾って「いつも 3 個」になり、出来事として読めなくなる。

        - 主コアは**素のフラックスが最大の帯域**が出す（`winningBand` で見分ける。
          `siblingIndex` は Bass/Mid/Treble の並び順なので主役の印にならない）。
          floor 1 では出るイベントが 1 本だけなので、この分け方は現状と 1 ビットも変わらない
        - 追加コアの明るさは **主コアの脈動 × (0.35 + 0.65 × 素のフラックス比)**。
          比をそのまま使うと 12% の帯域が 12% の明るさになって同時性が見えないので下限を置く。
          主コアの脈動に掛けるので、「発光 (All)」を別のソースへ繋ぎ替えても一緒に付いてくる
        - 位置は帯域ごとのシードから `burstAnchor` が引く（**主コアとは別の場所**）。
          大きさは帯域そのもの（Bass 1.32 / Mid 1 / Treble 0.66 倍。軸 0 では 3 本とも 1 倍）。
          色は `hueOf` を通すので `Hue coherence` の契約に乗る（全体 1 色なら核どうしも同じ色相）
        - **枠は帯域ごとに 1 つ。** 主コアが 1 スロットしか持たない（新しい打撃が前を上書きする）
          のと同じ規律にした。seed で持つと尾が溜まって**同時に 9 個**になり、
          同時発光ではなく残像の山になる（実測で確認して直した）
        - **骨格・閃光・扇は主コアにだけ付ける。** 十字も扇も「そのバーストの原点」を指す層で、
          コアの数だけ生やすと画面が格子になる。向きの組（`ARM_SETS`）も扇の個体差も
          **打撃 1 つにつき 1 組**しか無く、帯域ごとには定義されていない。
          追加コアは自分の中のフレア（`CORE_SHAPE_PARAMS`）だけを持つ

        実測（同時に出ているコアの数。300 フレーム）:

        | 軸 | 最大 | コアが出ているフレームの平均 | 内訳 [0/1/2/3 個] | 白の割合 | 白へ届いてよい層 |
        |---|---|---|---|---|---|
        | 0 | **1** | 1.00 | 10 / 290 / 0 / 0 | 0.0136 | 2 |
        | 0.5 | 4 | 2.43 | 10 / 112 / 0 / 120 | 0.0129 | 5 |
        | 1 | 4 | 2.79 | 10 / 7 / 105 / 120 | 0.0079 | 5 |

        **白の予算は保たれている** — 白い画素の割合は 0.0136 → 0.0079 と
        むしろ**減る**（追加コアは主コアより暗く、別の場所に立つので重ならない）。
        白へ届いてよい層は最大 5 枚で、`UNIFIED.reserve.core` を 4 → 6 に上げてあるから
        層の上限（48）で切られても核は 1 枚も落ちない。**枠は「先取りの上限」であって
        事前確保ではない**ので、核が 4 枚以下しか無い軸 0 では 6 でも 4 でも 1 層も動かない
        （合計は 46 で上限より小さいまま）。

        **既定と 4 プリセットは 1 画素も動いていない。** 合成した解析値で 300 フレーム
        進めてから画素をハッシュする再現テストで、**変更前のコード（stash）と変更後が完全一致** —
        既定 `d2708400` / 合計 184325209、Spatial `361dfe58` / 341320466、
        Reactive `4ea4257f` / 149597196、Lab2 `e88e126d` / 53842557。
        3 プリセットには `isolation: 0` / `bandUnison: 0` を明記した。
        無音 = 黒も維持（2 軸とも 1 のまま 30 秒ぶん時計を進めて合計 0・層 0）。
        負荷は `update()` の CPU 時間が 0.29〜0.43 ms/フレームで**軸によらず変わらない**
        （層が減るぶん軸 1 のほうがむしろ速い）。
        **隠した表現・他表現は差分ゼロ**（触ったのは Light Unified の 4 ファイルだけで、
        `OpticsAudioDrive` の `bandFloor` は既定 1 ＝ Element Lab 2 の挙動は 1 ビットも変わらない）。
        lint・build 通過 / コンソールエラー 0

        **P9-9（上段の全パラメーターを結線可能にする）**:
        結線先は 8 本（発光 All / Intensity / Spread / Core / Blur / H / Channel balance /
        Density）しかなく、**上段に出ている行の半分以上が音へ繋げなかった**。
        アコーディオンを開かずに見えている行は**全部**繋げるようにした（**21 行**）。
        折りたたみの中（詳細の軸）は対象外で、そちらはマスター経由で動く。

        | まとまり | 結線できる上段の行 |
        |---|---|
        | 明るさ | 発光 All / Intensity |
        | 配置・空間 | Spread（マスター）/ Depth |
        | 時間 | Time（マスター）/ Strobe |
        | 光学 | Blur / Edge contrast |
        | 色 | Colour lock（＝ 色相 H）/ Channel balance |
        | 構成 | Core・Membrane・Material（マスター）/ Membrane–Beam / Skeleton / Beam length / Density / Haze floor / Isolation / Band unison |
        | 動き | Motion |

        **一覧を手で書かない。** `UNIFIED_LOOK_PARAMS` は `AXIS_DECLS` から
        `detail !== true` を引いて作る。「常に見えている行はすべて音へ繋げる」という
        規則そのものが書いてあるので、上段の軸を足したときに**繋げない行が生まれない**。

        **繋いでも動かない行を作らないための下ごしらえのほうが本体だった。**
        軸の値を読む場所が `this.axes`（スライダーの位置）と `effectiveAxes()`
        （結線を通した値）に割れていて、前者を読んでいる軸は繋いでも動かない。
        読む場所を**1 フレーム 1 枚の写し（`this.look`）へ一本化**し、
        時間の形（`advanceDrive` の Attack / Decay / Strobe / Stagger）・
        打撃ごとの膜（`eventMembrane` / `decay` / `stagger` / `isolation`）・
        検出器へ渡す敷居（`bandUnison`）・密度・色の粘りを全部そこから読ませた。
        さらに**光学クロックの速さ**（`Strobe` → `tickRateOf`）は設定のときだけ
        渡していたので毎フレーム渡すようにした（繋いでも明滅の速さが変わらなかった）。

        実測: 21 行に 1 本ずつソースを繋いで 240 フレーム進め、画素ハッシュを
        繋ぐ前と比べた。**21 行すべてで絵が変わった。** 効きの大きい順に
        Membrane–Beam（平均輝度 −41.1）/ 発光 All（−42.3）/ Isolation（−28.5）/
        Intensity（+23.9）/ Strobe（−18.6）/ Density（−17.3）/ Blur（+16.8）/
        Time（+10.4）で、小さいものでも Skeleton（+0.7）・Motion（+0.1）・
        Band unison（+0.0 だが同時コア数が増えてハッシュは変わる）まで
        **ハッシュは全部動いた**。

        時間の形（`Strobe` / `Time`）を音へ繋ぐと、**音が時間の刻みを決め、その刻みで
        音を見せる**という二重の因果になる。指示どおり出してあるが、
        使い方によっては「音に合っている」ではなく「音が滑る」ように見えるはずで、
        そこは実際に触ってから判断する。

        **既定（全 None）は 1 画素も動いていない。** 4 プリセットのハッシュは
        P9-8 の値と完全一致（`d2708400` / `361dfe58` / `4ea4257f` / `e88e126d`）。
        21 行すべてを繋いだ状態でも**無音 = 黒**（30 秒で合計 0・層 0）。
        負荷は `update()` の CPU 時間 0.39 ms/フレームで変わらず。
        **他表現は差分ゼロ**（触ったのは Light Unified の 1 ファイルだけ）。
        lint・build 通過 / コンソールエラー 0

  - [ ] `Isolation` を上げても連なりは GIF の 2 倍（15.5 px 対 7 px）で、
        差は**粒**にある。GIF は点灯した画素が 0.647 と多いのに連なりが短い
        ＝ 明るい面の中を細かい濃淡が刻んでいる。こちらは面が滑らかなので、
        面積を削って黒を作る方向でしか連なりが縮まない。素材（`Texture grain`）の
        刻みを深くする側にもう 1 本要るか未決
  - [ ] Light Element Lab 2 — 音由来の seed → `seed`（断片・カーテンの散らばり）は未配線。
        Bass / Mid / Treble → R / G / B の発色駆動、オフセット量と非相関量を
        どの特徴に結ぶかは未決
  - [ ] Spatial Study の次段階 — 空間の手掛かり（グリッドや床）/ カメラのゆっくりした動き /
        軌跡の太さを音へ結ぶか / バーストの自己励起（クラスター化）を入れるか
  - [ ] 同時発光時の位置設計（2D 側）— floor を下げると複数 Core が同じ centroid に重なる（未決）
  - [ ] band-demo.wav のハイハットを帯域制限する — 現状 `hatAt()` が無フィルタの白色雑音で、
        6〜12 秒の「Treble 単独」区間でも 3 帯域のフラックスがほぼ同値（0.325/0.307/0.309）。
        帯域の切り分け検証には高域だけに絞った音が要る
  - [ ] Light Traces — Core Study の段階的な音写像を検証する。
        発生 → 明るさ → 横位置 → 帯域別 Onset → 縦位置 → サイズ → 色 → 移動と軌跡、の順に
        1 関係ずつ追加する。帯域別 Onset 以降は Bass / Mid / Treble の独立した発光イベントを
        同時に生存可能にし、1 画面・1 発光へ制限しない。位置は純粋な `Math.random()` ではなく、
        音響特徴または音由来の決定論的シードを使い、同じ音で再現できる散らばりを目指す
- [ ] G5 リアルタイム入力（VJ）とパフォーマンス最適化

## 共通の完了条件

各実装タスクは、次の条件を満たして完了とする。

- `npm run lint` が成功する
- `npm run build` が成功する
- 既存の表示を意図せず変更していない
- Audio、Three.js、DOM のリソースを適切に破棄する（RenderTarget を含む）
- 表現・Effect の責務を混在させない
- 新しい Effect を単独で有効・無効化でき、無効時は元の表示を維持する
- 質感の数値は `TUNING` を経由させ、コード中に直接書かない
