# DESIGN

Audio Artwork Lab の設計仕様。2026-07-24 に方針を確定。

このドキュメントは「何をどう作るか」を定める。日々の作業規約は `AGENTS.md` / `AI_RULES.md`、要点の索引は `CLAUDE.md` を参照する。

---

## 1. プロダクトの方針

- **「何でも作れるツール」にはしない。** 何でもできるようにすると After Effects / TouchDesigner の劣化版になる。作れる範囲を意図的に限定し、その中の質を上げる。
- 目的は「SNS 投稿を速く作る」ではない。**1 個のとびきり良いものを作るために、ツールから作る。**
- **恣意性を消す。** 人が「こうしよう」と意図した感じを出さない。作り手も理由が説明できない結果になるのが良い。
- **音楽が起点。音楽がなければ何も生まれない。**
- 部品は表現から逆算する。Effect の数を増やすこと自体は目的ではない。
- フラクタルは実装しない（After Effects に標準・独創性がない）。単なる ASCII 表現も採らない。

---

## 2. 操作フロー（3 段パイプライン）

```
① 生成      音楽 → 数学的な幾何学パターン（サイマティクス）
                ↓
② 表現      閾値を調整 → 表現方法を選ぶ（ミニマルな図形 / 光と波 / グラフ）
                ↓
③ エフェクト  VHS ノイズ / 色のテーマ / 揺らぎ / 空間性・奥行き / トランジション
```

**①と②の分離がこのプロダクトの核。** ①は図形ではなく**場（スカラー場）**を出力し、②が閾値で切って描画方法を選ぶ。同じ生成結果が、表現を差し替えるだけで別のグラフィックになる。

---

## 3. アーキテクチャ

### Field × Renderer をシェーダーで合成する

```
Field（場）      GLSL の  float field(vec2 p)  を提供
                      ↓  実行時に文字列結合して 1 本のフラグメントシェーダーを生成
Renderer（表現） GLSL の  vec3 render(vec2 p)  が field() を呼んで色を決める
                      ↓
全画面クアッド 1 枚に描画
                      ↓
Effect チェーン（既存の EffectComposer をそのまま使う）
```

Field を N 個・Renderer を M 個作れば **N×M 通り**の絵が出る。コード量は N+M しか増えない。

### 線幅の均一化

節線を描く際は距離正規化を用いる。場の傾きが変わっても線の太さが一定になり、シェーダーだけで細く美しい線が引ける。CPU でジオメトリを作る必要はない。

```glsl
float v = field(p);
float d = abs(v) / max(length(vec2(dFdx(v), dFdy(v))), 1e-5);
float line = 1.0 - smoothstep(0.0, uThreshold, d);
```

WebGL2 を前提とする（`dFdx` / `dFdy` が標準で使える）。

### ディレクトリ

```
src/
├── core/          流用（Camera は全画面クアッド方式では実質不要）
├── engine/        新規。Field × Renderer を合成しシェーダーを組む
├── fields/        新規 ①。Cymatics.ts など
├── renderers/     新規 ②。MinimalShape.ts / LightWave.ts / Graph.ts
├── effects/       流用 ③。BaseShaderEffect・EffectPipeline・既存 Effect
├── audio/         流用（解析を追加）
└── ui/            Generator パネルを Field / Renderer 選択に作り直す
```

---

## 4. 音 → パラメータの写像（恣意性の排除）

**原則：人は値を決めない。人は範囲と感度だけを決める。値は音が決める。**

### L1 連続写像 — 音の「量」が図形の量を動かす

| 音 | 図形 |
|---|---|
| 音量 | 閾値（節線の太さ・砂の溜まり量） |
| スペクトル重心（明るさ） | 場の細かさ |
| 低域 | 変形・揺れの強さ |

### L2 量子化写像 — 音の「高さ」が図形の状態を跳ばす

支配的な周波数を検出し、**整数次数 n, m へ量子化**する。音程が変わると図形が連続ではなく別の形へ跳ぶ。これは実際のクラドニ図形の物理と一致する挙動であり、演出ではない。

### L3 ハッシュ写像 — 音の「出来事」が図形の種類を決める（核心）

オンセットを検出した瞬間の**スペクトル形状そのものをハッシュしてシードにする**。シードが対称性・配置・変種を決定する。

```
スペクトル形状 → ハッシュ → シード → 図形の種類・配置
```

- 同じ音なら必ず同じ図形が出る（音から生まれている・再現性がある）
- 違う音なら予測できない図形が出る（ランダムに見える・人が決めていない）
- **`Math.random()` は使わない。乱数源は音そのもの。**

### 音色と持続

- スペクトル平坦度（ノイズ性）→ 図形の崩れ具合。キック / ハイハット / パッドで崩れ方が変わる
- 持続時間 → 蓄積。鳴り続けるほど線が濃くなり、止むと褪せる

### 追加する解析

現行の `volume / bass / mid / treble / beat` に加えて実装する。

`dominant frequency`（音程）/ `spectral centroid`（明るさ）/ `spectral flatness`（ノイズ性）/ `onset`（立ち上がり）/ `sustain`（持続）

### 調整記録（D4 は都度調整する前提）

- **2026-07-24 フィードバック**: 音で全体の形が「パッと切り替わりすぎる」。実物のクラドニ板の映像のように、**同じ形状の中で変化しながら移行していく**見え方にする。
  - モード切替は瞬時の差し替えではなく、2 つの場を混ぜながら約 2 秒かけて移行する（共振間で複数モードが重なる実際の板の挙動とも一致する）
  - シード（構図の引き直し）はクールダウンを設けて頻度を抑え、回転・中心は数秒かけて滑らかに追従させる
  - 線は実物の砂のように、芯に密に積もり周辺に粒が散る質感で描く

### UI 原則

**次数 n・m を直接操作するスライダーは出さない。** 露出するのは範囲（min/max）と感度のみ。UI の側から恣意性を入れられない構造にする。

---

## 5. 確定事項（2026-07-24）

| # | 論点 | 決定 |
|---|---|---|
| D1 | デザインレイヤー（画像・テキスト・図形） | **コードは残すがコメントアウトし、UI から見えなくする。** 主導線から外す |
| D2 | Field × Renderer のシェーダー合成 | **採用** |
| D3 | 最初に作る数 | **Field 1（サイマティクス）× Renderer 3（ミニマルな図形 / 光と波 / グラフ）** |
| D4 | 音 → パラメータの写像 | **上記 3 層方式。** 開発しながら都度調整する前提 |
| D5 | 音楽なしの動作 | **音楽なしでは動かさない。** 無音時は何も生成されない |
| D6 | 奥行き | **場の側で作る**（多層の場、輝度と焦点への写像）。透視投影は導入しない |
| D7 | トランジション | **後回し。** ①②が動いてから着手 |
| D8 | Preset 後方互換 | **捨てる。** 新形式 v4 とし、旧形式は読み込み時に破棄して初期状態に戻す |
| D9 | ブランチ | **`agent/audio-reactive-mvp` で継続**（Vercel の固定 URL で確認できるため） |

D4 と D6 は、実際の見え方を見ながら調整する前提。

---

## 6. 既存資産の扱い

| 資産 | 判定 |
|---|---|
| `effects/`（BaseShaderEffect・Pipeline・既存 Effect） | **そのまま流用。**最大の資産 |
| `audio/` | 流用し、解析を追加 |
| `core/`（Canvas・Renderer・App・AnimationLoop） | 流用 |
| `ui/StudioShell` `RecordingController` `QualityMonitor` `AudioControls` | 流用 |
| `generators/` | **作り直し**（生成と表現が一体で新設計に載らない） |
| `compositions/` | **作り直し**（神クラス継承を軽いレシピに置換） |
| `ui/StudioControls` の Generator セクション | **作り直し**（SineWave 決め打ちのため） |
| `modifiers/` | 実質 Effect なので `effects/` へ整理 |
| `ui/LayerEditor`（1729 行） | **コメントアウトして温存**（D1） |

### ③に必要な Effect（逆算結果）

| 要求 | 状況 |
|---|---|
| 色ズレ / 色ノイズ / スキャンライン / ピンボケ | RGB Split・Grain・Scan Drift・Blur で既存流用可 |
| トラッキングノイズ帯 / 下部ジッター / テープ劣化 | 不足。上記と統合し **VHS 1 個**にまとめる |
| 色のテーマ | Palette Map はあるが**横断概念への昇格**が必要 |
| 揺らぎ | Warp で充足 |
| 空間性・奥行き | 場の側で対応（D6） |
| トランジション | 不足。後回し（D7） |

**新規に必要な Effect は実質 2〜3 個。**

---

## 7. 実装順序

| # | 内容 | 検証できること |
|---|---|---|
| 1 | `engine/`（Field × Renderer のシェーダー合成） | 骨格が通るか |
| 2 | 解析の追加（dominant freq / centroid / flatness / onset / sustain） | 写像の材料が揃うか |
| 3 | Field: サイマティクス + L1・L2 写像 | ①が音に反応するか |
| 4 | Renderer: ミニマルな図形 + 閾値 | ②の核 |
| 5 | L3 ハッシュ写像 | 恣意性が消えるか |
| 6 | Renderer: 光と波 / グラフ | 差し替えで絵が変わるか |
| 7 | 既存 Effect チェーンの接続 | ③が乗るか |
| 8 | VHS 統合 Effect / 色テーマ昇格 | 質感の完成 |
| 9 | 奥行き → トランジション | 拡張 |

**5 の時点で一度立ち止まる。** ここで「これは良い」と思えなければ、Field や Renderer を増やしても良くならない。

---

## 8. 確認用音源（D5 の帰結）

音楽必須のため、無音では画面が黒のままになる。これは**正しい挙動**であり、不具合と区別できるようにしておく必要がある。そのため確認用の音源をリポジトリに置く。

### 配置と差し替え

```
public/audio/reference.wav    既定の確認用音源。ここを差し替えれば別の音源で確認できる
```

- **ファイルパスを固定**し、同名で上書きするだけで音源を入れ替えられるようにする。コード変更を不要にする。
- パスは 1 箇所の定数に置き、ハードコードを散らさない。
- **ファイルが存在しなくてもアプリは正常に起動する**こと（読み込み失敗時は通常の無音状態＝黒画面に落ちる）。ユーザーが音源を削除・差し替え中でも壊れない。
- 既存のファイル選択 UI・ドラッグ＆ドロップからの読み込みは従来どおり使える。確認用音源は「起動時に手早く確認するための既定値」であって、唯一の入力経路ではない。

### 既定音源の内容

初期の既定音源は**合成音**とする（サイン波のスイープ＋帯域を分けたノイズのビート）。理由は 2 つ。

- 著作権の懸念がない
- **周波数・音色・立ち上がりを狙って動かせる**ため、L1〜L3 の写像が正しく効いているか切り分けて検証できる。実楽曲では「反応していないのか、写像が間違っているのか」を分離しにくい

実楽曲での確認は、ユーザーが `reference.wav` を差し替えるか、UI から読み込んで行う。
