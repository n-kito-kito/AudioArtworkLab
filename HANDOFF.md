# HANDOFF — Light Unified 2（引き継ぎ）

作成 2026-08-03。**この文書は「なぜそうなっているか」を残すためのもの。**
コードから読み取れることは最小限にし、会話でしか決まっていない判断を優先して書く。

---

## 0. 進め方の規約（最重要・最初に読むこと）

2026-08-01 に**大きな作り替えを一気に走らせて画面が真っ白になり、5 コミットを巻き戻した**。
その反省から確立した規約。**これを破ると同じ失敗をする。**

1. **1 変更 = 1 スクリーンショット = ユーザーの目。** 複数の段をまとめて実装しない。
   1 段終わったら止めて見てもらい、OK が出るまで次へ進まない。
2. **エージェントは見た目の良し悪しを判断しない。**
   「暗いから明るくする」「スカスカだから足す」は禁止。
   必要だと感じたら**変更せずに報告して止まる**。判断はユーザーが行う。
3. **検証は必ず実音のピークで測る。**
   合成音の単発打撃や平均値は嘘をつく（実際、それで白飛びを見逃した）。
   `public/audio/reference.wav` を再生し、**最も明るいフレーム**で飽和画素率と黒割合を見る。
4. **構造を変える前にブランチを切る。** 捨てられる単位にしておく。
5. **数字の報告だけを信用しない。** 変更前後の同じ再生位置のスクリーンショットを並べる。

### 作業ディレクトリと Git の約束

- 作業は **worktree `/Users/nakaekito/Documents/Appli/AudioArtworkLab-reactive`**。
  メインリポジトリ `/Users/nakaekito/Documents/Appli/AudioArtworkLab` は**参照のみ・変更禁止**。
- **`main` へ push しない。** main は Vercel の Production を動かす。
- **未追跡の GIF（`public/dev-ref*.gif`）と音楽ファイルをコミットしない。** ユーザーの資産。
  コミット前に必ず `git status` で確認する。
- `Math.random()` / `Date.now()` は使わない（決定論のため）。

---

## 1. Current state

| | |
|---|---|
| ブランチ | `agent/light-reactive-lab` |
| 最新コミット | `f8cfdcf` Grow the cores and membranes from the band light events |
| 未コミット変更 | **無し**（作業ツリーはクリーン。未追跡の GIF を除く） |
| 確認すべき Expression | **Light Unified 2** |

起動:

```bash
cd /Users/nakaekito/Documents/Appli/AudioArtworkLab-reactive && npx vite --port 5174
```

`http://localhost:5174` → Expression セレクトで `Light Unified 2` →
左パネル下の開発ブロック（`Audio Analysis (dev)`）に 11 本の軸が出る。

主要ファイルは **`src/expressions/LightUnified2.ts` の 1 枚だけ**（約 1,140 行）。
既存表現とはコードを共有していない（検出器 `src/engine/bandLightEvents.ts` のみ再利用）。

### セレクトに出ている表現

Cymatics / Modular Pattern Field / Light Unified / **Light Traces — Spatial Study** /
**Light Element Lab 2** / **Light Reactive Lab** / Reactive Geometry / **Light Unified 2**

太字の 3 つは**目標の見え方を持つ参照実装**。隠してあるのは Light Traces / Core Study /
Light Element Lab（V1）で、コードは温存してある。

---

## 2. Current objective

### なぜ作り直したか

先行する `Light Unified`（35 軸）は、**「手続きが形を作り、素材はその内側の濃淡」**という前提で
組まれていた。この前提の上では:

- 素材は永遠に光の外形を決められない（円形の窓が必ず勝つ）
- 滲みを足すと必ず円形のハロになる
- 軸を足しても足しても「コアの周りに円状の光」から抜けられない

調整では抜けられない構造の問題だったので、**前提を反転した新表現**を白紙から作り始めた。

### 目指している表現

**Spatial Study / Reactive Lab / Element Lab 2 の 3 つの見え方を、
スライダーの調整だけで連続的に行き来できる 1 つの表現。**

「どれか 1 つに寄せる」のではない。3 つがパラメーター空間の 3 つの座標になり、
その**中間に今まで見たことのない見え方が現れること**が価値。

### 最重要のリファレンス

- `public/dev-ref-light-*.gif`（未追跡・コミット禁止・読み取りのみ）
- 実装としての参照は **`LightSpatialStudy.ts` + `spatialMapping.ts`**
  （素材が形を作る `macro layer` の式がここにある）

### 再現しようとしている特徴

黒地に、**素材由来の不定形な膜と筋**が広がり、中心に白熱がある。
色は緑・ピンク・紫が場所ごとに分かれて存在する（混ざって乳白色にならない）。
画面の 6〜7 割が黒。

---

## 3. 決定的な発見（これを知らないと必ず遠回りする）

### 「引っ掻き傷のような筋」は、筋を描く式から出ていない

3 表現（Spatial / Reactive / Lab2）のコードを全部読んで確認した事実:

- **「羽毛 / barb」という語は 3 表現のどのファイルにも 1 件も無い**
- 3 表現で「引っ掻き傷」という語が出るのは、**すべて「避けるべき症状」としてのコメント**
  （例: `spatialMapping.ts` 「ここが大きいと引っ掻き傷・爪痕に見えるので 2 割以下に落とした」）

**筋の正体は、素材（`prismAtlas` の 10 枚）を細長い板に貼ったときに、
素材の中の線が拡大されて現れる副産物。** 手続きで筋を描いてはいけない。

先行 Unified は `fragmentCharacter`（羽毛モード）という**手続きの模写**でこれを再現しようとして、
補正が補正を呼ぶ連鎖に陥っていた。**Light Unified 2 では手続きで筋を描かない。**

### 3 表現の部品の対応表

| 役割 | Spatial | Reactive | Lab 2 |
|---|---|---|---|
| **素材が形の膜** | `macro layer` 2〜5枚（**最長寿命 0.35〜1.8s**） | core/sheet/haze が全部これ | **無し**（素材は乗算グレインのみ） |
| 中心の白熱 | 手続きガウス 3 層 | **素材が形** + 白い芯を加算 | 手続きの楕円 + 素材を加算 |
| 広い膜 | `plane`（多角形 SDF） | `sheet` 1〜5枚（素材が形） | `curtain` 常設 3 枚（手続き） |
| 靄 | 無し（macro が兼ねる） | `haze` 1枚（素材のみ） | `haze` 常設 2 枚（手続き） |
| 貫通線 | `ray` 1〜4本 | `ray` 0〜2本 | `beam` 常設 3 枚（中央十字固定） |
| 破片 | `needle`/`arc` 最大30個 | **無し** | `veil` 1〜4枚（4族 SDF） |

**性格の差**: Reactive は 4 種のうち 3 種が「素材が形」/ Lab2 は 7 種すべて素材が濃淡だけ /
Spatial はその中間（手続きの transient + 素材の macro 膜の 2 段構え）。

---

## 4. Implemented（現在の実装）

### 見た目

- **膜**: 式は `色 × 素材輝度 × 緩いビネット × 強度`。**輝度の源は素材ただ 1 つ。**
  手続きの形・ハロ・ガウスを足していないので、**素材が 0 の画素は厳密に 0**。
  板の縦横比を 0.45〜2.2 で決定論的にばらつかせ、**細長い板が素材の線を引き伸ばす**
  （＝筋の正体を、式ではなく形状で再現している）。
- **コア**: `素材の輝度 × 補正利得 + 手続きの楕円 × (1 − Core form)`。
  **`Core form = 1` で手続きの芯が完全に消え、素材だけが光る**（Spatial 的）。
  `0` で楕円が主役（Lab2 的）、中間で「素材が形 + 白い芯が加算」（Reactive 的）。
  色は**白のみ**で、彩度は共有の色相から乗る。
- **消し方**: コアの有無は**スケール**で切り替える。`Core size = 0` で板のスケールが 0 になり、
  ラスタライズされる画素が 1 つも無くなる（輝度で消す項は持たない）。

### 音との反応

- 検出は**既存の `BandLightEventDetector` をそのまま再利用**（新しい解析はゼロ）。
- **打撃 1 個 → コア 1 個（減衰 0.22s）+ 膜 `Membranes` 枚（減衰 0.95s）。**
- 位置は `event.eventIndex` のハッシュから決まる**起点**。同じ音源なら毎回同じ場所。
  素材・切り取り・回転・反転も同じ番号から引く。
- 振幅 = `clamp(age/attack) × exp(-age/decay) × event.strength`（分岐なし・ストロボなし）。
- **色**: centroid と帯域バランス（treble − bass）→ 色相、1 次遅れ 0.25s で追従。
  **コアと膜は同じ色相の実体を共有**し、作る場所は `resolveTint()` の 1 箇所だけ。
- 同時上限: コア 10 / 膜 30（溢れたら古いものから捨てる）。
- **無音 = 黒**（PRD D5）。停止 8 秒後に点灯 0・最大値 0・インスタンス 0 を実測済み。

### 3D 構造

- 固定カメラ（動かさない）。膜とコアは z を持つが、**現時点で遠近の作り込みはしていない**。
- 描画は膜 1 ドロー（instanced）+ コア 1 ドロー。

### 光学処理

- **ブルーム・トーンマップ・ソフトニー天井・ハロは一切入っていない。**
  加算合成のみ。白は重なりから生まれる想定。
- 素材の読み方は膜・コアとも同じ（黒浮きの敷居 smoothstep → ガンマ）。0 は 0 のまま通る。

### UI

開発ブロックに 11 軸（`getExpressionParams()`）。すべて連続な混合係数で、
`if (axis > 0.5)` のような分岐はどこにも無い。

| 軸 | 0 ⇄ 1 |
|---|---|
| Crop | 狭い＝素材の線が筋になる ⇄ 広い＝細かい濃淡 |
| Scale | 小さい ⇄ 画面より大きい |
| Softness | 鋭い＝明部だけ残る ⇄ 霧状＝暗部まで一様 |
| Carve | 緩いビネット＝素材が形 ⇄ 硬い円窓＝外形で切る |
| Anchor | 画面内に散る ⇄ 起点（コア）から生まれる |
| Membranes | 1 打撃で生む枚数 |
| Core form | 手続きの芯 ⇄ 素材が形 |
| Core size | **0 = コアを消す** ⇄ 大 |
| Saturation | 0 = 白 ⇄ 1 = 色が濃い |
| Sensitivity | 発火の感度 |
| Intensity | 全体の強度 |

---

## 5. Current visual result

### うまくいっている点

- **素材が形になっている。** 円形のハロが 1 つも無く、膜の外形は素材の筋そのもの。
- `Anchor` の A/B が明確（0.85 で 1 点から放射状に膜が伸びる / 0 で画面全体に散る）。
- コアと膜の色が揃って移り変わる（t≈20s で紫 → 青）。
- **白飛びなし**: 全曲 62 フレームで全チャンネル飽和のピークが **0.0012%**。
- **無音 = 黒**が厳密。

### リファレンスとまだ異なる点

- **要素の種類が足りない**（靄・破片・貫通線・扇が未実装。膜とコアだけ）。
- 遠近・奥行きの作り込みが無い。
- 配置に構図の設計が無い（seed で散るだけ。anti-clustering も構図タイプも無い）。

### 確認されている問題

1. **`Core form = 1`（素材だけ）のとき画面が暗い。**
   白へ届く層が無くなるため。膜の重なりで白を作る設計だが、現状の膜 4〜6 枚・強度 1 では届かない。
   **枚数・強度・エンベロープの調整で解くべきで、天井や利得を足して解いてはいけない。**
2. **Lab2 的の状態（小さく硬い切り取り）が暗い。**
   敷居が高いと素材の大半が落ちるため（素材の平均輝度は 0.017〜0.066）。
   Lab2 の本来の姿は「素材は濃淡・手続きの形が支配」なので、
   **手続き側の部品（破片・貫通線）を足したときに解決する可能性がある。**

### 見た目を確認する手順

1. `npx vite --port 5174` → `http://localhost:5174`
2. Expression = `Light Unified 2`
3. 左パネルの `Load audio file` か、既定で読まれる `reference.wav` を再生
4. 開発ブロックの軸を動かす。3 つの質感の目安:
   - **Spatial 的**: crop 0.22 / scale 0.62 / softness 0.5 / carve 0.35 / anchor 0.75 / core form 0.85
   - **Reactive 的**: crop 0.45 / scale 1.0 / softness 0.95 / carve 0.08 / anchor 0.15 / core form 0.55
   - **Lab2 的**: crop 0.92 / scale 0.18 / softness 0.06 / carve 0.88 / anchor 0.95 / core form 0
5. 停止して**黒に戻る**ことを確認

確認用音源: `public/audio/reference.wav`（24 秒）、`public/audio/band-demo.wav`（30 秒・帯域別の検証用）。
実楽曲は `/Users/nakaekito/Downloads/InALifelongSleepFix.wav` と `FunzoueFix.wav`
（**コミット禁止**。一時コピーして使い、必ず削除する）。

---

## 6. Important decisions（確定した設計判断）

| 論点 | 決定 |
|---|---|
| **1 Onset と発光の関係** | 1 打撃 = 1 つの光の出来事。コア 1 個 + 膜 N 枚が同じイベントから生まれる。鳴りっぱなしでは再発火しない |
| **Light Burst と複数 Light Element** | 1 イベントから複数の要素が生まれる（現在はコア + 膜）。今後、靄・破片・貫通線も同じイベントに紐づける |
| **発光位置** | イベント番号のハッシュから決まる**起点**。同じ音源で同じ位置（決定論） |
| **中心配置と Scatter** | どちらか一方が正解ではなく **`Anchor` という連続軸**（散る ⇄ 起点から生まれる）。3 表現がそれぞれ別の配置戦略を持っていたため |
| **白い中心核と虹色の周辺光** | **白い芯は常に存在しなくてよい。** Spatial には独立したコア部品が無く、素材の膜が強く光って中心に見えていた。`Core form = 1` で芯が完全に消えるのが正しい |
| **Volume / Onset / Flux と発光数・明るさ** | 打撃の強さ（`event.strength`）が振幅を決める。発光数は `Membranes` 軸（音には未接続）。**音量で発光数を変える設計はまだ入れていない** |
| **カメラ** | **固定。** 動かさない（VJ 用途で酔わせない・恣意性を出さない） |
| **決定論的な乱数** | **維持。** `Math.random()` / `Date.now()` は使わない。同じ音源・同じ設定で同じ絵になること |
| **手続きで筋を描かない** | 筋は素材を細長い板に貼った副産物として出す（§3 の発見） |
| **明るさの扱い** | 天井・ハロ・ブルーム・トーンマップを安易に足さない。加算合成と素材の輝度で成立させる |

参照すべき PRD の決定（**メインリポジトリ側にあり、この worktree には反映されていない**）:
**D28**（Simple Rules, Complex Behaviors）/ **D29**（分解して検証）/ **D30**（イベント時に特徴を確定）/
**D31**（決定論 Seed + 履歴回避）/ **D32**（Prismatic Burst）/ **D33**（Study は製品ルールの例外）/
**D34**（品質条件）。加えて **D5**（無音 = 黒）/ **D17**（範囲・質感は焼き込み）/ **D20**（表示中の図形を動かさない）。

---

## 7. Temporary decisions（暫定・未確定）

| 項目 | 状態 |
|---|---|
| **コアの素材側の補正利得 3.3** | 素材の輝度が 0.017〜0.3 しかなく、これが無いと `Core form` が「消える軸」になるため入れた。**厳密には「素材の輝度そのまま」ではない。** 音接続後の見え方を見て残すか外すかを決める、と保留中 |
| 減衰時間（コア 0.22s / 膜 0.95s） | 仮の値。3 表現の実測（Spatial の膜 0.35〜1.8s、Reactive の haze 0.7〜2.8s）とは合っていない |
| 同時上限（コア 10 / 膜 30） | 仮。負荷は未計測 |
| 色相の写像（centroid + 帯域バランス） | 素直な連続。3 表現は**離散状態 + ヒステリシス**で「緑の回・赤の回」を作っていた。まだ入れていない |
| 3 つの質感の軸値 | §5 の目安は暫定。プリセットとして固めていない |

---

## 8. Rejected ideas（採用しなかった案と理由）

| 案 | 却下の理由 |
|---|---|
| 先行 Unified を調整して直す | 「手続きが形・素材は濃淡」という前提自体が原因で、調整では抜けられなかった |
| 既存の膜に軸を足して素材の形を出す（`Material form` 軸案） | `mix` で混ぜる限り中途半端な状態しか作れず、軸 1 本が 6〜7 役を持つ超軸になる。**新しい部品として足す方が確実**（ユーザーの判断） |
| 手続きで筋（羽毛・barbs）を描く | 3 表現に存在しない。補正の連鎖を呼ぶ。§3 参照 |
| Intensity の上限を上げて明るくする | 2026-08-01 に実行して**実音のピークで飽和 4〜9% の白飛び**。5 コミット巻き戻した |
| `Core form` を `mix`（どちらか一方）にする | Reactive の「素材が形 + 白い芯が加算」が作れない。**加算構造**に変更済み |
| コアを透明度で消す | スケールで消す方式に変更（`Core size = 0` で板ごと消える） |

---

## 9. Next task（優先順・最大 5 件）

各項目は**独立して実装し、1 つ終わるごとに止めてユーザーに見てもらう**（§0 の規約）。

### 1. 靄（床）を足す

- **目的**: 画面をまとめる最下段。膜とコアが黒地に浮いて孤立するのを防ぐ
- **変更予定**: `src/expressions/LightUnified2.ts`
- **完了条件**: 靄の有無で「要素の孤立感」が変わることがスクショで分かる。
  黒割合が 6 割を下回らない。無音 = 黒が保たれる
- **壊してはいけない**: 膜・コアの式、既存 11 軸の意味、無音 = 黒、決定論

### 2. 破片を足す

- **目的**: 不規則さの担当。Lab2 的な「小さく硬い切り取り」の主役
- **変更予定**: 同上
- **完了条件**: Lab2 の `veil` 4 族（shard / sliver / plate / chip）に相当する形が出る。
  **羽毛・barbs の式は作らない**（§3）。素材は乗算で削るだけ
- **壊してはいけない**: 同上 + §3 の原理（筋は素材から出す）

### 3. 貫通線・放射スパイクを足す

- **目的**: 打撃の鋭さの担当
- **変更予定**: 同上
- **完了条件**: **本数を強く抑える**（3 表現とも最大 2〜4 本）。多いと「斬撃」になり全体が崩れる。
  強い打撃のときだけ出ること
- **壊してはいけない**: 同上。中央十字に固定しない（Lab2 の `beam` は固定だったが、
  それは Lab2 の性格であって既定にすべきではない）

### 4. 明るさの釣り合いを決める

- **目的**: §5 の問題 1・2（`Core form = 1` と Lab2 的が暗い）を解く
- **変更予定**: 同上
- **完了条件**: 3 つの質感すべてで、実音のピークで**飽和が数 % 以下・黒が 6 割以上**。
  **ユーザーと一緒に決める**（エージェントが単独で判断しない）
- **壊してはいけない**: 天井・ハロ・ブルームを安易に足さない。まず枚数・強度・エンベロープで解く

### 5. 3 つの質感をプリセットとして固める

- **目的**: `Spatial 風 / Reactive 風 / Lab2 風` を選べるようにする（軸値の一括代入のみ）
- **変更予定**: 同上
- **完了条件**: セレクトで 3 つを切り替えて、それぞれの質感になる。
  **ボタンで見え方を切り替える実装にはしない**（プリセットは「スライダーに値を入れるだけ」）
- **壊してはいけない**: 軸の連続性（プリセット間をスライダーで行き来できること）

**やらないこと（今回のスコープ外）**:
カメラ移動 / 汎用ノードエディター / 他表現への展開 / main への push / 本番デプロイ。

---

## 10. Verification（検証の状況）

### 実行済み

- `npm run lint` / `npm run build` — **通過**（最新コミット時点）
- ブラウザ確認: Expression 切り替え、`Anchor` の A/B、`Core form` 0 / 0.55 / 1、
  `Core size = 0` でコアが消えること、`Core seed` 相当（イベント番号）で素材が変わること
- **白飛び**: 実音全曲 62 フレーム抽出で全チャンネル飽和のピーク **0.0012%**
- **無音 = 黒**: 停止 8 秒後にサンプル 426,409 画素中、点灯 0 / 最大値 0 / インスタンス 0
- コンソールエラー: **0**
- 既存表現の回帰: Cymatics V1 / Spatial Study / Light Unified / Light Element Lab 2 に
  切り替えて描画されることを確認

### 未確認

- **FPS を測っていない**（体感で問題は出ていないが数値が無い）
- ドローコール数の実測
- 画角 16:9 / 9:16 での確認（1:1 のみ確認）
- 実楽曲（`InALifelongSleepFix.wav` / `FunzoueFix.wav`）での確認
- 長時間再生での安定性（インスタンスの溜まり方）
