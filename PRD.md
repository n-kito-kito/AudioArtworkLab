# PRD — Audio Artwork Lab

音楽に反応するジェネラティブアートの制作ツール。

このドキュメントは **何を・なぜ作るか**（要求）を定める。
**どう作るか**（設計・アーキテクチャ）は [DESIGN.md](./DESIGN.md)、
実装の地図は [CLAUDE.md](./CLAUDE.md)、進捗は [ROADMAP.md](./ROADMAP.md) を参照する。

最終更新: 2026-07-30

---

## 1. プロダクトの目的

**音を起点に、自分たちのユニークネスを宿した映像を生み出すツール。
イベントの VJ と、制作した音楽のリリース（SNS・配信）で使う Other Spectrum 自身の道具であり、
生まれた映像は押しつけずに置いておく触媒として、
Healthy Psychedelia（感覚の健康・ユニークネスの尊重）を目に見える形にする。**

（2026-07-27 の定義。目的は今後も考え続け、更新する。
思想の一次情報は `docs/other-spectrum-healthy-psychedelia-dossier.pdf`）

- **使うのは自分たち（O/S）。** 配布は現時点で考えない（コードの品質を配布物として
  担保できる確信がまだないため）。ただし将来の可能性としては**保留**とし、
  **完成度は配布に耐える水準を目指す**。UI は自分たちが分かれば良く、そのぶん削れる
- **ツールは像を描かない。像を結ぶのは音だけ。** ユニークネスは実行時の操作ではなく
  **道具の作り込みに宿る** —— どの表現を作るか、どんな質感を良しとするか、
  「好き」の集積が焼き込まれている
- **映像は正解を含まず、解釈を観客に返す。** 気づいた人が自分の感じ方を自分で取り戻す。
  能動的な啓蒙はしない（Under-promote と同型の態度）。押しつけないこと
  （恣意性を消すこと）自体が、触媒としての態度である
- **表現の条件は「音から生まれたまま」であること。** 物理現象そのものに価値があるのでは
  ない。サイマティクスはそれを満たす一つ目の表現にすぎない。表現を増やすときの採用基準も
  「物理っぽいか」ではなく「音から必然として生まれた見え方か」
- **表現（Expression）は数を競わずに育てる。** 数個に限定はしない（今後も何種類か
  追加していく）が、多さを価値にしない。切り替えながら音から絵を生み出す
- 良い映像を届けて良い反応をもらうこと（「その映像好きです」「VJ かっこいい」）が、
  O/S の認知・ブランドの継続の起点になり、最終的に自分たちが幸せに
  生きるための一つの手段である

### 位置づけ

> **1 つの傑出した作品を生むために、ツールから作る。**

リリース周りの SNS 投稿にも使うが、「速く量産する」ことを目的にはしない。
その目的設定にすると、できあがるものが凡庸になる。作れる範囲を絞り、
届ける 1 本 1 本の質を上げる。

立ち位置は「何も考えなくても結果が出る自動生成」と「何でも自分で作る編集ツール」の
**間**である。制約は表現を狭めるためではなく、迷いを減らし発見を増やすための設計
（[PRODUCT_PHILOSOPHY.md](./PRODUCT_PHILOSOPHY.md)）。

### 成功の判定（未確定）

「VJ ができる」「投稿できる」は現時点でほぼ可能になるため、判定として機能しない。
目指す状態は、**自分たちのユニークネスを表した映像が、外から好意的な反応を得ること**。
これを判定可能な形にする定義は未解決（§11）。

### 作成の指針（North Star）

> **「Teenage Engineering が VJ ツールを作ったら、こうなるかもしれない」**

内部で方向を確認するための North Star であり、キャッチコピーでも見た目の指示でもない。
見た目は Other Spectrum 独自のものにし、表面的な引用はしない（§2）。参考にするのは
考え方だけ:

- 作り手の美意識を隠さないこと
- 限られた仕組みの中に深さを作ること
- 説明を読まなくても触ってみたくなること
- 制約がストレスではなく、遊び方として感じられること
- 機能数ではなく、体験全体に一貫性があること
- 使う人が、与えられた仕組みを自分のものにできること

### 当面のゴール

「サイマティクス 1 表現の完成度を高める」段階は **2026-07-27 に達成**とした。
ここからのゴールは複数並行で持つ。

1. **表現の追加** — 2 つ目の Expression（採用基準は「音から必然として生まれた見え方か」）と、
   表現の切り替え
2. **VJ で使える状態にする** — フルスクリーン出力・ライブ入力での実践。
   反応の調整は実際に触りながら育てる（§11）
3. **UI を思想の主従に合わせる** — Expression > 反応の調整 > Effect の序列を
   画面が語るようにする（§3.2。現状は Effect stack が最も目立ち、逆になっている）
4. **実楽曲での音反応の仕上げ** — 複数の実曲で L1〜L3 の効き方を調整する（自動較正を含む）
5. **Prismatic Light の完成** — プリズム光を、音から生成される一つの作品として
   完成させる（§8-2 の品質条件を満たす。2026-07-30 時点の中心作業）

---

## 2. やらないこと（Non-goals）

**最も重要な節。** ここが曖昧になるとプロダクトが崩れる。

| やらないこと | 理由 |
|---|---|
| 何でも作れる汎用ツールにする | After Effects / TouchDesigner の劣化版にしかならない |
| 表現の種類を無制限に増やす | 「これしか作れない」と決め切ったほうが質が上がる |
| Effect の数を増やすこと自体を目的にする | 部品は表現から逆算する。不要な Effect は作らない |
| フラクタルを実装する | After Effects に標準機能があり、独創性がない |
| 単なる ASCII 表現を実装する | ひねりがなければ面白くない |
| 人が図形を直接デザインできるようにする | 恣意性が出る（次節） |
| 音楽なしで動かす | 音楽が起点。無音では何も生成しない |
| ボタンを押すだけのビジュアライザーにする | 音への反応を調整し、曲の顔を探す余白が体験の中心にある |
| 参照元（Teenage Engineering 等）の外見を引用する | 参考にするのは考え方（制約の中の深さ・触りたくなる体験）だけ。表面の引用は模倣になる |

---

## 3. 中核となる原則

### 3.1 恣意性を消す

人が「こうしよう」と意図した感じを出さない。**作り手も、なぜこうなったか説明できない**
結果になるのが良い状態である。

役割はこう分かれる。

```
作り手（制作時）… 範囲・質感の定数をキュレーションして焼き込む
音楽（実行時）  … 像を結ぶ。生成に関わる値は音が決める
人（実行時）    … 閾値と聴かせ方を決める。像をどう切り出すか、どの音にどれだけ反応させるか
```

境界線は「**生成と閾値**」である。

- **像を結ぶのは音だけ。** 次数・対称性・構図・崩れ——像そのものを描く値に、実行時の人は一切触れない
- **人が決めてよいのは閾値。** 表現の選択・色のテーマ・奥行き・エフェクト——生成された像を
  どう切り出して見せるかの調整であり、像そのものを作る操作ではない
- **「音のどこに・どれだけ反応させるか」は聴かせ方に属する（D24）。** 曲によって穏やかにも
  激しくも聴かせるため、反応先と感度は VJ 中にリアルタイムで触れるようにする。
  これは像を描く操作ではなく、板がどの音に耳を澄ますかの選択である。
  それ以外の UI はできるだけシンプルに保つ
- **ズームは開発時に良い見え感を探すための機能。** 主 UI には置かない
- **範囲と質感は作り手が制作時に焼き込む内部定数。** UI には出さない。調整はチューニング
  モード（開発用）で行い、確定したら定数にする。これは恣意ではなく、作り手の焼き込みである
- **曲ごとの差は自動較正で吸収する。** 音量・帯域バランスの違いは移動窓の正規化で吸収し、
  どんな曲でも写像が成立するようにする（G3 で実装）
- 乱数源は音そのもの。`Math.random()` は使わない。同じ音なら必ず同じ像になり（再現性）、
  違う音なら予測できない像になる

### 3.2 創造性の所在と生成の構造 — Simple Rules, Complex Behaviors

このツールの創造性は「何でもできること」からは生まれない。
**すでに存在している音楽の中に、どのような像が眠っているかを探すこと**から生まれる。

生成の構造は「**小さなルールの重なり**」である（2026-07-30・D28）。

```
音楽
  ↓ 特徴パラメータを抽出（volume / bass / mid / treble / centroid / flatness / onset / sustain …）
音のパラメータ × 表現のパラメータ を小さなルールで結ぶ
  （例: onset → 発生 / volume → 大きさ / centroid → 太さ / seed → 個体差）
  ↓ ルールの重なりが像を生む
Expression（表現）
  ↓ 質感を加える
Effect  →  無限の見え方
```

- **1 本のルールは単純に保つ。** 複雑さはルールの中に書き込まず、
  ルールの重なりと音の複雑さから生まれさせる（Simple Rules, Complex Behaviors）
- **表現を作る = その表現のパラメータ語彙を設計し、音との結びつけをキュレーションすること。**
- **視覚要素と音への反応は、分解して別々に検証し、最後に一体化する（D29）。**
  光なら「発光という最小単位」から要素を一つずつ確かめ、音の接続も 1 段階ずつ足す。
  大枠の絵を先に作って音を流し込む進め方も、全要素を一度に音へ繋ぐ進め方もとらない
- ルールの粒度・単位は確定要素ではない。実践しながら育てる

- **最大の変数は音楽。** 同じ Expression でも曲が変われば別の像が出る。機能を増やさなくても、
  入力される音楽の数だけ結果がある
- **反応の調整**は、同じ曲から別の表情を発見するための余白（低音を拾うか、高音の揺れを
  拾うか）。今後 VJ の実践の中で種類を育てる（§11）
- **Effect は主役ではない。** Expression と音の関係から生まれた像に、最後に質感を加えるもの。
  Effect を積み上げることがツールの中心になってはいけない
- 目指す体験は、使ううちに「この曲は低音より高音に反応させた方がいい」という理解が
  使い手の中に育っていくこと

原文は [PRODUCT_PHILOSOPHY.md](./PRODUCT_PHILOSOPHY.md)（2026-07-27 の一次資料）。

### 3.3 原則は共通、方針は表現ごと

ツール全体を縛るのは §2（やらないこと）と §3.1（恣意性を消す）だけである。
それ以外の美学・音への反応のさせ方・品質基準は**表現ごとに定義する**。
今後サイマティクスとは全く別のグラフィック表現も増やすため、
特定の表現の方針を全体の方針として書かない。

サイマティクス（表現 1）に固有の方針は次のとおり。**他の表現を縛らない。**

#### サイマティクスの方針 a — 美しさは「模様」ではなく「境界」に宿る

模様そのもの（X 字・円・格子・干渉縞）は数学的に単純で説明可能である。
それらを魅力的に見せているのは、**粒子が境界に集まり、密度差だけで輪郭が浮かび上がる質感**
である。したがってこの表現内の開発優先順位は、境界を粒子密度で描くレンダリング品質が
最優先で、新しい模様を増やすのはその次。

#### サイマティクスの方針 b — 音は「形」ではなく「状態」を変える

音で図形そのものを切り替えるのではなく、同じ図形の状態を変化させる。
（この考え方は他の表現でも使える可能性があるが、採用は表現ごとに判断する）

| 音 | 変わるもの |
|---|---|
| 周波数構成 | 主振動モード＝図形の種類。切替は瞬時で、砂が再配置されて新しい像になる |
| 音量 | 励振の強さ＝どれだけ砂が跳ねるか（図形の種類は変えない） |
| オンセット | 砂が一斉に跳ね、旧配置が崩れて再配置が促される |
| ノイズ性 | モードの崩れ |

プリズム光（Prismatic Light）に固有の方針は次のとおり。**他の表現を縛らない。**

#### プリズム光の方針 a — 光を要素に分解して作る

光は **Core / Sheet / Haze / Ray / Prism / Depth / Envelope** の要素に分解して
設計・検証する（D29）。要素は別々の光ではなく、**1 つの音イベントから生まれた
1 つの Prismatic Burst** として、同じ発生源・同じ Seed・同じ色の系統を共有する（D32）。

#### プリズム光の方針 b — 音イベント発生時に光の特徴を確定する

音の値を毎フレーム形・色・位置へ直接流し込まない（D30）。
発光イベントが発生した瞬間に、素材・色・クロップ・大きさ・太さ・位置・奥行き・
面の傾き・寿命・構図タイプを**すべて確定**する。発生後に変化してよいのは、
Envelope（Attack / Hold / Decay）による光量の推移と、発生時に決めた
拡大・ごく弱い面内の動きだけ。カメラ方向（Z）へは移動させない。

#### プリズム光の方針 c — バリエーションは決定論で作る

`Math.random()` は使わない。色・位置・角度・奥行き・素材の組み合わせは
**音イベント固有の Seed から決定論的に生成**し、同じ音源・同じイベント列からは
同じ結果が再現される（§3.1 と同じ原則）。直近の履歴を持ち、同じ構図・素材・
角度・色相帯の連続と、同じ位置への集中・左右の偏りを避ける（D31）。

#### プリズム光の方針 d — 素材画像は「輝度マスク」である

プリズム素材（10 枚）は完成画像として表示するものではなく、
**シェーダーで変形・合成するための輝度マスク**である。クロップ・回転・反転・
色・歪みは音由来の Seed が決める。素材は D17 の「作り手が焼き込む質感」に属する。

#### プリズム光の方針 e — Study / Lab は開発用の例外

Core Study・Spatial Study・Element Lab・Reactive Lab などの Study / Lab 群は、
要素と音反応を分解して検証するための**開発用の例外**であり、
製品としては最終的に **1 表現（1 見え方）へ統合する**（D16 / D33）。
本番表現は無音時に黒（D5）。無音でも光を固定表示してよいのは開発用 Study だけ。

#### プリズム光の方針 f — 技術上の前提はサイマティクスと異なる

サイマティクスは真上から見た板（正射影・奥行きなし）、プリズム光は
**固定 Perspective カメラの 3D 空間**（手前・中間・奥の配置。カメラは動かさない）。
描画・奥行き・カメラの設計は表現ごとに選ぶ。実装の詳細は DESIGN を参照。

---

## 4. 想定する使い方

1. 音楽を読み込む（既定の確認用音源、またはユーザーの楽曲）
2. 再生する → 音に反応して絵が立ち上がる（サイマティクスでは砂が集まって像が現れる）
3. 表現を選ぶ（サイマティクス V1/V2・Modular・Light Traces・Reactive Geometry ほか。
   開発用の Study / Lab 群は最終的に統合する — §3.3 プリズム光の方針 e）
4. 表現ごとの調整を行う（**持つ機能は表現ごとに異なる**。全表現に共通の調整項目は
   存在しない前提で設計する。サイマティクスが持つのは色のテーマのみで、**奥行きは持たない**）
5. エフェクトを重ねる
6. PNG または動画で書き出す（動画は MP4。非対応環境のみ WebM へフォールバック）

無音では画面は黒のまま。これは不具合ではなく仕様である。

---

## 5. 操作フロー（パイプライン）

```
① 解析      音楽 → 特徴パラメータの抽出
                ↓
② 接続＝生成  音のパラメータ × 表現のパラメータを小さなルールで結ぶ（§3.2）
                ↓
③ 表現      ルールの重なりが 1 つの見え方として現れる（1 表現 = 1 見え方）
                ↓
④ エフェクト  質感の調整（VHS ノイズ / 色のテーマ / 揺らぎ / トランジション）
```

**1 つの表現は 1 つの見え方だけを持つ（D16）。** 見え方の選択肢は UI に出さない。
ユーザーが選ぶ単位は「表現」であり、表現を切り替えると生成も見え方も一体で変わる。

内部実装としては、生成（場）と描画を分離した構造を保っている。これは表現を
増やすときの部品であって、ユーザーへ露出する選択肢ではない（DESIGN.md 参照）。

---

## 6. 各段への要求

### ① 生成（Field）

**共通の要求（すべての表現）:**

- 音を起点に絵を生成する。**数学的な幾何学パターンであることは必須ではない。**
  生成の原理（物理シミュレーション・数式・その他）は表現ごとに選ぶ
- 生成に関わる値は音が決める（§3.1）

**サイマティクス（表現 1）においては:**

- **Granular Plate Model（D18 / D21）**: 完成した節線や図形を直接描かない。画面全体を
  上から見た一枚の板として扱い、不可視の振動場によって砂（密度場）が移動し、
  節線に集積することで像が現れる。評価基準は「図形を描いている」ではなく
  「砂が自発的に集まって図形が現れた」ように見えること
- **砂の運動は自己捕捉ランダムウォーク（D21）。** 板の加速度が重力を超えた場所で
  砂粒は跳ね上げられ、着地位置がずれる。腹では跳ね続け、節では跳ねないので
  そこで止まって溜まる。すなわち **移動度 ∝ 振動振幅** であり、砂は速度を蓄えない。
  勾配に沿って「流す」実装は誤りで、反応が鈍くなる
- **モードは瞬時に切り替わる（D21）。** 駆動周波数が変われば定在波は数ミリ秒で
  新しいモードへ移る。中間形状は存在しないため、場を補間してはならない。
  目に見える遷移は砂が再配置される過程そのものである
- 画面は**完全な左右対称かつ上下対称**を基本とし、4 回対称 / 8 回対称 / 放射対称も
  生成できる。どの対称性になるかは**音が決める**

### ② 表現（Expression）

**共通の要求:**

- **1 表現 = 1 見え方。** 見え方の選択肢は設けない
- **表現ごとに持つ調整機能を宣言する。** 全表現に共通の調整項目は存在しない前提で
  UI・プリセットを設計する
- 表現は今後増やす。サイマティクスとは全く別のグラフィック表現もあり得る。
  切り替え時はトランジションで移行する

**サイマティクス（表現 1）においては:**

- 見え方は「粒子が集まって境界が浮かぶ、ざらざらした粒子状」で固定する
- 既定の色はモノクローム（黒背景・白粒子）を正とする
- 持つ調整機能は**色のテーマのみ**。奥行きは持たない（D25）。ズームは開発用（§3.1）

### ③ エフェクト（Effect）

**表現から逆算して必要なものだけを実装する。** 数を増やすこと自体は目的ではない。

| 要求 | 方針 |
|---|---|
| VHS ノイズ | 実機では連動して起きる現象なので **1 つの Effect に統合**し、「劣化度」マスター 1 本で全体が汚れる |
| 色のテーマ | Effect の 1 つではなく**横断概念**。テーマを替えると全体の色が一括で変わる |
| 揺らぎ | 既存の Warp で充足 |
| 空間性・奥行き | 要否は**表現ごとに決める**。サイマティクスでは使わない（D25） |
| トランジション | リニア（クロスフェード）とモーフィングの 2 種 |

すべての Effect は単独で ON/OFF でき、無効時は元の表示を維持すること。

---

## 7. 音への反応（3 層写像）

**原則: 範囲と質感は作り手が制作時に焼き込む。像を結ぶ値は音が決める（§3.1）。**

表現共通の設計指針は §3.2 の「小さなルール」構造である（D28）。
以下の 3 層写像 —— 音の「量」「周波数構成」「出来事」がそれぞれ別のものを動かす ——
は**サイマティクス（表現 1）で生まれた整理であり、この表現に固有の実装**。
他の表現を縛らない（他の表現では、結ぶルールの束をそれぞれ設計する）。

### L1 連続写像 — 音の「量」が板の状態を動かす

音量が励振（粒子の移動速度）、持続が定着、ノイズ性が散乱を動かす。

**振動場そのものは音量で連続変形させない（D20）。** 実物の板の節線は剛体的で、
音が大きくても図形が波打ったり伸縮したりはしない。音量で変わるのは
「粒子がどれだけ活発に動くか」であって「図形の形」ではない。

### L2 共振写像 — 音の「周波数構成」が固有モードを励起する（D19）

単一の支配周波数では選ばない。板は共振周波数と帯域を持つ**固有振動モード群**
（16 種・トポロジーの異なる族）を持ち、スペクトル全体が各モードの帯域へ
どれだけエネルギーを注ぐかで励起量が決まる。最も強いモードが節線の構造を決め、
2 番目のモードは副モードとして限定的に混ざる。

- 切り替えにはヒステリシス・確認時間・最短保持を設け、微小な変化では同じ模様が
  揺れ、明確な音楽的変化でだけ別の構造へ移る（二段階の反応）
- 共振域の間では場が弱まり、模様は不安定になる（周波数スイープで共振域ごとに
  異なるモードが現れる）
- 正規化は対数周波数・高域プリエンファシス・全帯域分布へのシェアで行い、
  低域偏重の楽曲でも高次モードが選ばれ得る
- **プリエンファシスは帯域内正規化の「後」に掛ける。** 重みへ織り込むと
  正規化で相殺され、モード選択が低音の基音に張り付く（実際に起きた不具合）

### L3 ハッシュ写像 — 音の「出来事」が図形の種類を決める（核心）

オンセットを検出した瞬間の**スペクトル形状そのものをハッシュしてシード**にする。
シードが向きと変種を決定する。

```
スペクトル形状 → ハッシュ → シード → 次に現れる図形の向き・変種
```

**向きはモードごとに固定し、表示中の図形は回転させない（D20）。**
シードが決めるのは「次に現れる図形」の向きであり、切替のタイミングでのみ適用する。
表示中の図形を回し続けると、構造の変化ではなく単なる揺れとして見える。

### プリズム光（Prismatic Light）における基本ルール

小さなルールの束（§3.2）。現時点の基本対応は次のとおり（実践しながら育てる）。

| 音の特徴 | 光への反映 |
|---|---|
| 周波数帯ごとの立ち上がり | 光イベントの発生 |
| 立ち上がりの強さ | 発光の強さ |
| 音量 | 光全体の大きさ |
| 音の重心（centroid） | 光膜の細さ・太さ |
| 音の持続（sustain） | 光が残る時間 |
| 音のノイズ性（flatness） | 質感の複雑さ |
| 低・中・高域の割合 | Sheet / Haze / Ray の構成比 |
| 音イベント固有の Seed | 色・位置・角度・奥行き・素材の組み合わせ |
| 音の新規性（novelty） | 層数と空間的な広がり |

**これらを一度に検証しない。** 次の 4 段階を開発上の必須検証工程とする（D29）。

```
1. Trigger    出現タイミングだけを確認する
2. Texture    質感だけを確認する
3. Variation  重複回避と多様性を確認する
4. Composite  すべてを合わせて作品として確認する
```

### 必要な音声解析

`volume` / `bass` / `mid` / `treble` / `beat` /
`pitch`（音程）/ `centroid`（明るさ）/ `flatness`（ノイズ性）/ `onset`（立ち上がり）/ `sustain`（持続）

---

## 8. 品質基準（Cymatics Visual Style）

**この節はサイマティクス（表現 1）に固有。** 他の表現はそれぞれ自分の品質基準を持つ。

サイマティクス表現が満たすべき品質。

### 構成

- 画面は黒背景・白い砂のみで構成する（線は描かない）
- **画面の 90% 以上が黒**。明るい部分は 10% 程度
- 余計な装飾を入れない

### 境界（最重要）

境界は一本線ではなく、**粒子密度によって形成する**。

```
理想ではない:  ────────      （ベクター線）
理想ではない:  ・ ・ ・ ・ ・   （まばらな点）
理想:          ░░░██████░░░   （密度の高い帯）
```

エッジは**くっきりさせない**。必ずぼそぼそ・ざらざら・粒状にする。

### 粒子

- 非常に細かい（1 画素以下）
- 白・半透明
- **常に微細に揺れる。完全停止しない**
- ランダムノイズをただ撒くのではなく、密度分布に従って配置する

### アニメーション

- **場は瞬時に切り替わる。** 時間がかかるのは砂の再配置だけ（D21）
- 旧配置は 0.5 秒ほどで崩れ、新しい節線へ集まっていく過程が見える
- ビートだけ一瞬反応する

**動くのは粒子であって、図形ではない（D20）。** 図形（節線の構造）は
モードが切り替わるまで静止している。回転させない・波打たせない・伸縮させない。
常時の「揺れ」は粒子側のジッターだけが担う。

これを守らないと、構造が変わっていないのに画面が動き続け、
「音量で揺れているだけ」に見える。

### 禁止事項

- ベクター線だけで描く
- シャープすぎる輪郭
- 完全な幾何学図形
- 突然の切り替え
- **表示中の図形を回転・波打たせ・伸縮させる（D20）**
- RGB カラー（主表現において）
- 強いグロー
- パーティクルを飛ばす

### 意識するキーワード

Particle Density / Symmetry / Grain / Morphing / Interference /
Standing Wave / Organic Geometry / Soft Boundary / White Dust / Slow Evolution

---

## 8-2. 品質基準（Prismatic Light）

**この節はプリズム光に固有。** 完成条件を次のとおり定める（D34。
「GIF に近い」「綺麗」という主観ではなく、この条件で判定する）。

### 一体性

- Core・Sheet・Haze・Ray は、**同じ光源から生まれた一つの現象**として見える
- 中心光と周囲の光で、同じシェーダー・色・質感の言語を共有する
- 白は直接指定せず、**複数色の重なりによって自然に発生**させる

### 構成

- **太く広い半透明の光膜（Sheet）を主役**にする
- Haze で画面全体へ光が届く印象を作る（ただし発光していない領域は黒を保つ）
- Ray は常時出さず、必要な音イベントで**補助的に**出す
- 線は基本的に直線。うねりはごく小さくする

### 空間

- 手前・中間・奥の配置で奥行きを作る（発生時に固定）
- 光をカメラ方向へ移動させない
- 奥行きは遠近・大きさ・重なり・面の傾きで感じさせる

### 時間

- 音イベント発生時に形・色・位置・寿命を確定し、その後は**光量の推移だけ**を動かす
- 生の音量を毎フレーム形状や位置へ直接入力しない
- 無音時は黒（D5）

### 禁止

- 強い音でも白飛びで画面の広い範囲が白くならない
- 斬撃・引っかき傷・レーザー・2D 素材の貼り付けに見えない
- 円形ガウスの点光源だけの表現に戻さない

---

## 9. 技術的な制約

| 項目 | 要求 |
|---|---|
| 動作環境 | ブラウザ（WebGL2） |
| フレームレート | リアルタイム 60fps |
| 音源 | ローカルファイル（MP3 / WAV 等）、マイク入力 |
| 出力 | PNG、MP4 録画（非対応環境は WebM フォールバック） |
| 保存 | 設定の自動保存、プリセットの JSON 入出力 |
| 堅牢性 | 極端な値でも NaN・黒画面にしない。無効な Effect は元表示を維持 |

---

## 10. 現在の実装状況（2026-07-30）

### 完成しているもの

| 領域 | 内容 |
|---|---|
| 表現 | サイマティクス V1/V2（Granular Plate Model・固有モード 16/18 種）/ Modular Pattern Field / Light Traces（2D 過渡光学イベント）/ Reactive Geometry |
| 開発用 Study / Lab | Light Traces — Core Study（音と光の因果の計測器）/ Spatial Study（3D プリズム光の作業場）/ Element Lab（静的な光要素）/ Reactive Lab（Trigger〜Composite の段階検証）。§3.3 プリズム光の方針 e により最終的に 1 表現へ統合する |
| プリズム光の基盤 | 帯域別スペクトルフラックス + 局所適応オンセット検出 / イベント結合 / Prismatic Burst（Core・Sheet・Haze・Ray）/ 固定 Perspective カメラの 3D（手前・中間・奥）/ プリズム素材 10 枚の輝度マスクアトラス / 決定論 Seed と履歴回避 |
| 演奏面 | 反応の調整 3 スライダー（Bass/Mid/Treble）/ 画角 7 比率（D26）/ Output ウィンドウ（第 2 画面フルスクリーン） |
| Effect | 14 種（VHS 統合を含む）+ 色のテーマ 4 種（横断概念） |
| 音声解析 | 10 種の特徴量 + 帯域ごとのピーク追従自動較正（+ 表現側の帯域別フラックス） |
| 保存 | Preset v9（自動保存・JSON 入出力） |
| 出力 | PNG / MP4 録画（非対応環境は WebM） |
| 開発ツール | `?tune=1` / `?debug=1` / Audio Analysis (dev) セクション |
| 公開 | 本番 https://audio-artwork-lab.vercel.app/ は `main` を追う。開発ブランチは Preview のみ |

### 未着手・今後の検討

- **プリズム光の統合** — Study / Lab 群を §8-2 の品質条件を満たす 1 表現へ収斂させる
- **実楽曲での感度調整（G3）** — サイマティクスは未着手。プリズム光は局所適応で一部先行
- UI の本設計（ユーザーがデザイン → 実装）とキーボードショートカット
- 表現間のトランジション
- 曲構造の検出（トランジションの自動発火）
- パフォーマンス計測と最適化（モバイル対応を含む）

---

## 11. 未解決の論点

| 論点 | 状況 |
|---|---|
| VJ 体験の言語化 | 使用場面の最優先は O/S イベントの VJ だが、「VJ で使える状態」の具体（場・操作・出力条件・機材接続）は未言語化。別途相談して詰める（2026-07-27） |
| 成功の判定の定義 | 「動く・投稿できる」は判定にならない。「好意的な反応が O/S 認知の起点になる」を判定可能な形にする定義が未確定（2026-07-27） |
| 反応の調整の育て方 | 基本形（Bass/Mid/Treble の励振ゲイン 3 本）は実装済み。速さ・残り方・ピークの立て方などをどこまで増やすかは、**要求として固定せず、VJ を実際に触りながら判断する**（VJ という行為の理解がまだ不十分なため。2026-07-27） |
| 質感の詰め | 実楽曲で「良い感じ」との評価（2026-07-25）。ここから帯の太さ・粒の質感・集積の速さを `?tune=1` で追い込む段階 |
| 実楽曲でのモード切替の頻度・感度 | 合成スペクトルでは検証済み（リード出入りで 1 秒以内に構造が変わり、キック連打では乱発しない）。複数楽曲での比較調整が残っている（G3） |
| 副モード混合の適量 | 現在は控えめ（上限 0.4）。節線がぼやけない範囲でどこまで混ぜるか未確定 |

新しい論点が生まれたらこの節に追記し、決まったら §12 へ移す。

---

## 12. 決定履歴

| # | 論点 | 決定 | 日付 |
|---|---|---|---|
| D1 | デザインレイヤー | コードは残すが UI から外す | 2026-07-24 |
| D2 | Field × Renderer のシェーダー合成 | 採用 | 2026-07-24 |
| D3 | 最初に作る数 | Field 1 × Renderer 3 | 2026-07-24 |
| D4 | 音 → パラメータの写像 | 3 層方式（都度調整する前提） | 2026-07-24 |
| D5 | 音楽なしの動作 | 動かさない。無音は黒画面 | 2026-07-24 |
| D6 | 奥行き | 場の側で作る。透視投影は導入しない | 2026-07-24 |
| D7 | トランジション | ①②の完成後に着手 | 2026-07-24 |
| D8 | Preset 後方互換 | v1〜v3 は破棄。以後はバージョンを上げて migration | 2026-07-24 |
| D9 | ブランチ | `agent/audio-reactive-mvp` で継続 | 2026-07-24 |
| D10 | 境界の描画 | 線ではなく粒子密度で描く。最優先で品質を上げる | 2026-07-25 |
| D11 | 対称性 | 音が対称性の種類を選ぶ。画面に対して厳密に対称 | 2026-07-25 |
| D12 | ズーム | 図形の一部を画面いっぱいに寄せられる | 2026-07-25 |
| D13 | 既定の色 | モノクロームを正とする。Palette Map は Effect として温存 | 2026-07-25 |
| D14 | 恣意性の適用範囲 | ジェネレーター（表現）に適用する。Effect は細かく調整してよい。まずジェネレーターの質を上げる | 2026-07-25 |
| D15 | デザインレイヤー | UI からタブごと撤去し、右パネルは Effect のみ。コードは温存 | 2026-07-25 |
| D16 | 表現の単位 | 1 表現 = 1 見え方。Renderer 選択 UI は廃止し、場 × 描画は内部部品とする | 2026-07-25 |
| D17 | 楽器のモデル | 境界は「生成と閾値」。像を結ぶのは音だけ。範囲・感度は作り手が焼き込む内部定数とし UI に出さない。曲ごとの差は自動較正で吸収（G3）。ズームは開発用とし主 UI から外す | 2026-07-25 |
| D18 | 生成方式 | Granular Plate Model。振動場 → 粒子の密度場シミュレーション（GPU ping-pong・保存形フラックス）で、粒子が節線に集積して像が現れる。音写像は volume→励振と移動速度 / sustain→定着 / onset→浮遊と再配置 / flatness→散乱 に更新 | 2026-07-25 |
| D19 | モード選択 | 単一支配周波数の量子化を廃止。固有モードバンク（16 種）へのスペクトル励起で主・副モードを決める。ヒステリシス + 確認時間 + 最短保持の二段階反応。音量はモードを決めない | 2026-07-25 |
| D20 | 図形の剛体性 | 表示中の図形は回転・波打ち・伸縮をさせない。向きはモードごとに固定し切替時のみ新図形へ適用。低域による場のうねりは既定でほぼ無効。常時の揺れは粒子側のジッターだけが担う | 2026-07-25 |
| D21 | 砂の物理 | 場の補間を廃止しモードは瞬時に切替。砂は慣性を持たず「移動度 ∝ 振動振幅」で動き、節で止まって溜まる。勾配の急さでは速さを決めない（腹の頂点で取り残されるため）。輸送・拡散とも保存形フラックスで総量を保つ | 2026-07-25 |
| D22 | サイマティクス V2 | モードライブラリを自由端の正方形板の固有モード（Waller 近似: 2 つの梁モードの結合、結合の符号が対称性の族を決める）へ刷新した V2 を追加。定義域を板全体に一致させ（縁 = 自由端）、中央の支持点は固定端として必ず節にする。板の個体差（微小な異方性・励振点のずれ）は決定論的な固定値で持つ。V1 は `cymatics-v1` として完全温存し状態を共有しない（旧プリセットは V1 として読む）。**質感（TUNING）も版ごとに持つ** — モードの体系が違えば同じ砂の量では同じように見えないため、それぞれの最良の状態どうしで比べる。開発期間中のみ Expression セレクトに V1/V2 を併置して切り替える（左右分割はしない）。収斂後は D16（1 表現 = 1 見え方）へ戻す | 2026-07-26 |
| D23 | モード切替の跳ね上げ（V2 のみ） | 駆動周波数が変われば板は過渡的に大きく鳴り、節に溜まっていた砂まで跳ね上げられる。切替の瞬間だけ「移動度の下限を外す・寄る向きを反転する・等方的に強く散らす」を約 0.25 秒かけ、模様が模様へ変形するのではなく**一度散ってから新しい節へ集まり直す**ようにする。粒子は生成・消滅させず既存の砂だけを動かす（総量は保存）。過渡の間は分割数を上げる — CFL により 1 サブステップで運べる量が texel/dt に縛られ、上げないと散らばりが上限に張り付くため。V1 は跳ね上げを起こさない。**現時点のチューニングでは切っている**（`releaseTime: 0`）。機構は残してあり、長さを戻せばそのまま効く | 2026-07-27 |
| D24 | 目的の再定義と演奏面 | 目的を「O/S の活動（イベントの VJ・制作した音楽のリリース）で使う映像を、音から生み出すための自分たちの楽器」と定義（一旦。今後も更新）。使うのは自分たちで、配布プロダクトではない。思想を担うのはツールでなく映像（後日「触媒として置く」へ言い直し。布教という語は姿勢に合わないため撤回）。VJ 中に触れるのは表現の切替・テーマ・エフェクトに加え、**「音のどこに・どれだけ反応させるか」（反応先と感度）**。曲によって穏やかにも激しくも聴かせるための演奏操作であり、像そのものは引き続き音だけが決める。それ以外の UI はできるだけシンプルに保つ。表現の採用基準は「物理っぽいか」ではなく「音から必然として生まれた見え方か」 | 2026-07-27 |
| D25 | 共通原則と表現別方針の分離 | ツール全体を縛るのは §2 と §3.1 のみ。美学・音への反応のさせ方・品質基準は表現ごとに定義し、サイマティクス固有の内容（境界の美・状態変化・Granular Plate Model・対称性・3 層写像の具体）は「サイマティクスにおいては」と明示して書く。**表現ごとに持つ調整機能を宣言する**（サイマティクスは色のテーマのみ。奥行きは持たない）。動画書き出しは WebM から **MP4 へ移行する方針**。「少数のキュレーション済みモード」の記述は意味が不明瞭のため削除（反応の性格づけは D24 の演奏面が置き換えた） | 2026-07-27 |
| D26 | 画角は長方形の板で対応 | 1:1 / 16:9 / 9:16 / 4:3 / 3:4 / 3:2 / 2:3 を選べる。切り取りや余白ではなく**板そのものがその比率の長方形になる**。場は物理座標（面積 1 正規化・半辺長 √r × 1/√r）で評価し、節線間隔は縦横で等しい — 図形は伸縮せず、長辺に多くの節が並ぶ。シムの解像度も比率で割り振り、テクセルは物理的に正方形を保つ（CFL・拡散の等方性）。長方形の板は 90° 回転で自分に重ならないため、構図の向きは 180° 単位に落とす。画角の切替は板の取り替えであり、砂は撒き直しになる。モード表の共振周波数は当面据え置き（様式化された値のため）。演奏面（D24 案 1）は帯域 → 砂の活発さの 3 スライダーとして実装 | 2026-07-27 |
| D27 | 制作思想の保存と語の整理 | 思想の一次資料を [PRODUCT_PHILOSOPHY.md](./PRODUCT_PHILOSOPHY.md) に原文のまま保存し、PRD には判断に使う要点だけを写す（§1・§2・§3.2）。創造性の所在を「音楽 × Expression × 反応の調整 × Effect → 無限の組み合わせ」と定義し、最大の変数は音楽、Effect は主役ではないとする。**「楽器」という語は今後使わない**（音から映像を作るツールであり、演奏するものではない。過去の決定履歴の表記は歴史として残す）。Expression は数を競わないが数個にも限定しない（今後も追加していく）。配布は現時点で考えない（コード品質の担保への確信がないため）が将来の可能性として保留し、**完成度は配布に耐える水準を目指す**。非目標に「ボタンを押すだけのビジュアライザー」「参照元（Teenage Engineering 等）の外見引用」を追加。反応の調整の拡張は要求として固定せず VJ の実践の中で判断する | 2026-07-27 |
| D28 | 生成の構造 | **Simple Rules, Complex Behaviors** を全表現共通の生成原理として採用。音の特徴パラメータと表現のパラメータを 1 対 1 の小さなルールで結び、ルールの重なりが表現を生む（音楽 → 特徴抽出 → 接続 → Expression → Effect）。従来の「大枠の表現を先に作り、音への反応を後から調整する」進め方を置き換える。§3.2・§5 を更新し、§7 の 3 層写像はサイマティクス固有の実装として位置づけ直した。Light 系（Core Study の段階検証以降）がこの原理の最初の実践。ルールの粒度・単位は未確定で、実践しながら育てる | 2026-07-30 |
| D29 | 分解して検証する開発方針 | 視覚要素と音への反応を分解して別々に検証し、最後に一体化する。光は Core / Sheet / Haze / Ray / Prism / Depth / Envelope の要素へ、音の接続は **Trigger → Texture → Variation → Composite** の 4 段階へ分解し、この順を開発上の必須検証工程とする（§7） | 2026-07-30 |
| D30 | 音イベント発生時に光の特徴を確定する | 音の値を毎フレーム形・色・位置へ直接流し込まない。発光イベントの瞬間に素材・色・クロップ・大きさ・太さ・位置・奥行き・傾き・寿命・構図を確定し、以後は Envelope による光量の推移と発生時に決めた動きだけが変化する。カメラ方向（Z）へは移動させない | 2026-07-30 |
| D31 | 決定論的な Seed と直近履歴回避 | バリエーションは音イベント固有の Seed から決定論的に生成し、同じ音源・同じイベント列で同じ結果を再現する。直近履歴により同じ構図・素材・角度・色相帯の連続と位置の偏りを避ける | 2026-07-30 |
| D32 | Prismatic Burst | 複数レイヤー（Core・Sheet・Haze・Ray）を、1 つの音イベントから生まれた 1 つの Prismatic Burst として扱う。同じ発生源・同じ Seed・同じ色の系統を共有し、「別々の光が同時に表示されている」印象にしない | 2026-07-30 |
| D33 | Study / Lab は製品ルールの例外 | 分解検証のための Study / Lab 群（Core Study / Spatial Study / Element Lab / Reactive Lab）は開発用の例外として D16（1 表現 = 1 見え方）の適用外とし、最終的に 1 表現へ統合する。本番表現は無音時に黒（D5）。無音での固定表示は開発用 Study のみ許可 | 2026-07-30 |
| D34 | プリズム光の品質条件 | 「GIF に近い・綺麗」という主観ではなく、§8-2 の明文化された条件（一体性・膜が主役・直線基本・静的な奥行き・重なりから生まれる白・白飛び禁止・斬撃に見えない等）で完成を判定する | 2026-07-30 |
